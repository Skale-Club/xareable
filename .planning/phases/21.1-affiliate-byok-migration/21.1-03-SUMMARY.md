---
phase: 21.1-affiliate-byok-migration
plan: 03
subsystem: api
tags: [openrouter, gemini, ai-gateway, billing, caption-quality, carousel, enhancement]

# Dependency graph
requires:
  - phase: 21.1-01
    provides: getOpenRouterApiKey/selectImageApiKey helpers, verify-phase-21.1.ts harness, profiles.openrouter_api_key column
provides:
  - caption-quality.service.ts callGeminiForCaption + ensureCaptionQuality accept an optional openRouterApiKey, forwarded to all 3 internal caption calls
  - carousel-generation.service.ts CarouselGenerationParams.openRouterApiKey threaded through callCarouselTextPlan (master plan) and the CRSL-09 ensureCaptionQuality call
  - enhancement.service.ts EnhancementParams.openRouterApiKey threaded through runPreScreen and generateEnhancementCaption, fail-closed PreScreenUnavailableError contract preserved
affects: [21.1-04, 21.1-05, 21.1-06, 21.1-07]

# Tech tracking
tech-stack:
  added: []
  patterns: ["optional per-request provider key with `params.key || config.PLATFORM_KEY` fallback — mirrors the established `imageApiKey?: string` precedent from Phase 12"]

key-files:
  created: []
  modified:
    - server/services/caption-quality.service.ts
    - server/services/carousel-generation.service.ts
    - server/services/enhancement.service.ts

key-decisions:
  - "Followed the plan's interface precedent exactly — openRouterApiKey placed alongside the existing imageApiKey? field in each params interface, same optional-with-fallback shape."
  - "Fail-closed pre-screen contract (PreScreenUnavailableError on missing key) preserved byte-for-byte; only the key source changed (openRouterApiKey || config.OPENROUTER_API_KEY instead of bare config read)."

patterns-established:
  - "Text/planning gateway calls resolve their OpenRouter key via `params.openRouterApiKey || config.OPENROUTER_API_KEY` at the call site, never overwriting the platform fallback semantics."

requirements-completed: [GATE-06]

# Metrics
duration: 8min
completed: 2026-07-27
---

# Phase 21.1 Plan 03: Thread openRouterApiKey through caption-quality, carousel, and enhancement services Summary

**Three service interfaces (caption quality, carousel master-plan, enhancement pre-screen/caption) now accept a per-request OpenRouter key that overrides the platform env key, completing 4 of the 6 gateway text-call sites for affiliate billing attribution.**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-07-27T16:23:35Z (session start)
- **Completed:** 2026-07-27T16:29:27-04:00 (last task commit)
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments
- `caption-quality.service.ts`: `callGeminiForCaption` and `ensureCaptionQuality` both accept `openRouterApiKey?: string`; all three internal caption calls (first pass, retry, repair) forward it.
- `carousel-generation.service.ts`: `CarouselGenerationParams.openRouterApiKey` threads into `callCarouselTextPlan`'s gateway branch and the single CRSL-09 `ensureCaptionQuality` call.
- `enhancement.service.ts`: `EnhancementParams.openRouterApiKey` threads into both `runPreScreen` and `generateEnhancementCaption`; the fail-closed `PreScreenUnavailableError` throw on a missing key is unchanged.

## Task Commits

Each task was committed atomically:

1. **Task 1: caption-quality.service.ts — thread openRouterApiKey through all 3 caption calls** - `0a58a89` (feat)
2. **Task 2: carousel-generation.service.ts — master plan + caption call** - `eb88c8b` (feat)
3. **Task 3: enhancement.service.ts — pre-screen + caption** - `d4d9679` (feat)

**Plan metadata:** (this commit, see final_commit step)

## Files Created/Modified
- `server/services/caption-quality.service.ts` - `callGeminiForCaption`/`ensureCaptionQuality` accept and forward `openRouterApiKey`
- `server/services/carousel-generation.service.ts` - `CarouselGenerationParams.openRouterApiKey` threaded through master-plan + caption calls
- `server/services/enhancement.service.ts` - `EnhancementParams.openRouterApiKey` threaded through pre-screen + caption calls; fail-closed contract preserved

## Decisions Made
None — followed the plan's interface precedent (mirroring `imageApiKey?: string`) exactly as specified.

## Deviations from Plan

None - plan executed exactly as written. All acceptance-criteria greps passed on first attempt for all three tasks; `npm run check`, `scripts/verify-phase-21.1.ts --only=svc-caption/svc-carousel/svc-enhancement` (3 PASS each), and `scripts/verify-phase-21.ts` (no regression) all green.

### Parallel-execution git race (documented per Phase 21 precedent, not a deviation from plan content)

This plan ran as one of two parallel executor agents (21.1-02, 21.1-03) in the same working directory (no worktree isolation). During Task 1's commit, the concurrent 21.1-02 agent's `git add` transiently swept my staged `caption-quality.service.ts` changes into their own commit (`1affc91`, message `feat(21.1-02): OpenRouterImageProvider honors input.apiKey`). The other agent self-corrected via `git commit --amend` (new hash `8c7e0d9`, containing only their intended file, `image-provider.ts`), which returned my changes to the working tree unstaged. I re-staged and committed them cleanly as `0a58a89`. No content was lost at any point — verified via `git diff HEAD` (empty) both before and after the correction. Same precedent as the documented races in Phase 21 (21-03, 21-07, 21-08, 21-09).

## Issues Encountered
None beyond the parallel-execution git race documented above, which resolved itself without intervention on my side (the other agent's self-correction restored a clean single-file commit for each task).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All three service interfaces now accept `openRouterApiKey`; zero bare `const orKey = config.OPENROUTER_API_KEY;` reads remain in the three files.
- Wave 3 (plans 21.1-04, 21.1-05, 21.1-06) can now supply the resolved affiliate key from routes (`generate.routes.ts`, `carousel.routes.ts`, `enhance.routes.ts`, `edit.routes.ts`, `posts.routes.ts`) via `selectImageApiKey`/`getOpenRouterApiKey` (21.1-01) into these newly-threaded params.
- No blockers.

---
*Phase: 21.1-affiliate-byok-migration*
*Completed: 2026-07-27*

## Self-Check: PASSED

All 3 modified files found on disk; all 3 task commit hashes (0a58a89, eb88c8b, d4d9679) found in git history.
