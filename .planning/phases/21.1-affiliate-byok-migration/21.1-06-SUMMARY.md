---
phase: 21.1-affiliate-byok-migration
plan: 06
subsystem: api
tags: [openrouter, gemini, ai-gateway, billing, transcription, caption-quality]

# Dependency graph
requires:
  - phase: 21.1-01
    provides: getOpenRouterApiKey helper, verify-phase-21.1.ts harness, profiles.openrouter_api_key column
  - phase: 21.1-03
    provides: caption-quality.service.ts ensureCaptionQuality accepting an optional openRouterApiKey
provides:
  - transcribe.routes.ts affiliate-aware OpenRouter key gate + resolved key threaded into aiGatewayTranscribe
  - posts.routes.ts remake-caption affiliate-aware OpenRouter key gate + resolved key threaded into ensureCaptionQuality
  - Closes the seventh and final affiliate-facing AI surface (GATE-06 complete across all 7 call sites)
affects: [21.1-07]

# Tech tracking
tech-stack:
  added: []
  patterns: ["affiliate key gate resolved before checkCredits/deductCredits so the error is never a mid-flight 401/500", "Gemini key resolution scoped to non-affiliates only where the only remaining consumer is the GATE-07 direct rollback branch"]

key-files:
  created: []
  modified:
    - server/routes/transcribe.routes.ts
    - server/routes/posts.routes.ts

key-decisions:
  - "Followed the plan's interface precedent exactly — getOpenRouterApiKey(profile) called on the affiliate branch, openRouterApiKey || config.OPENROUTER_API_KEY fallback shape at each gateway call site."
  - "geminiApiKey narrowed to `\"\"` for affiliates in both files (no video/direct-Google path in either route) — its only remaining consumer is the GATE-07 \"direct\" rollback branch's x-goog-api-key header, an accepted documented limitation from 21.1-RESEARCH Pitfall 2."

patterns-established: []

requirements-completed: [GATE-06]

# Metrics
duration: 6min
completed: 2026-07-27
---

# Phase 21.1 Plan 06: transcribe.routes.ts + posts.routes.ts remake-caption affiliate OpenRouter gate Summary

**Voice transcription and caption-remake — the last two affiliate-facing AI surfaces — now gate on and bill to the affiliate's own OpenRouter key instead of the Gemini profile.api_key field, completing all 7 of 7 GATE-06 call sites.**

## Performance

- **Duration:** ~6 min
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- `transcribe.routes.ts`: profile SELECT now includes `openrouter_api_key`; affiliate gate resolves `getOpenRouterApiKey(transcribeProfile)` before `checkCredits`, returning the exact SC3 error string as a 400 if missing; the `routing === "openrouter"` gateway branch now prefers the affiliate's own key (`openRouterApiKey || config.OPENROUTER_API_KEY`) instead of a bare platform env read; Gemini key resolution narrowed to non-affiliates only (its sole remaining consumer is the GATE-07 direct-rollback branch).
- `posts.routes.ts`: `POST /api/posts/:id/remake-caption` — the seventh, previously-unenumerated affiliate-facing AI surface — profile SELECT now includes `openrouter_api_key`; the inline Phase-12.3 tier gate replaced with an affiliate-aware `getOpenRouterApiKey(profile)` resolution; `openRouterApiKey` threaded into the `ensureCaptionQuality` call (21.1-03's interface).
- Verified via `grep -rn "config.OPENROUTER_API_KEY" server/routes/ server/services/` that every remaining bare-read hit is either a comment, the right-hand side of a `||` fallback, or the single `const key = config.OPENROUTER_API_KEY;` line inside `image-provider.ts`'s `requireOpenRouterKey()` — no other affiliate-reachable bare reads exist anywhere in the codebase.

## Task Commits

Each task was committed atomically:

1. **Task 1: transcribe.routes.ts — affiliate gate + gateway key** - `0bd8767` (feat)
2. **Task 2: posts.routes.ts remake-caption — affiliate gate + caption key** - `ad510c0` (feat)

**Plan metadata:** (this commit, see final_commit step)

## Files Created/Modified
- `server/routes/transcribe.routes.ts` - Affiliate gate resolves OpenRouter key up front; gateway branch prefers affiliate key over platform env; Gemini key resolution scoped to non-affiliates (GATE-07 rollback path only)
- `server/routes/posts.routes.ts` - `remake-caption` handler's inline tier gate replaced with affiliate-aware OpenRouter key resolution; key threaded into `ensureCaptionQuality`

## Decisions Made
None — followed the plan's interface precedent (mirroring the established `getOpenRouterApiKey`/`ensureCaptionQuality` shapes from 21.1-01/21.1-03) exactly as specified.

## Deviations from Plan

None - plan executed exactly as written. All acceptance-criteria greps passed on first attempt for both tasks; `npm run check`, `scripts/verify-phase-21.1.ts --only=route-transcribe` (3/3 PASS), `--only=route-remake-caption` (2/2 PASS), and `scripts/verify-phase-21.ts` (43/43, no regression) all green.

### Parallel-execution note (not a content deviation)

This plan ran as one of three parallel executor agents (21.1-04, 21.1-05, 21.1-06) in the same working directory (no worktree isolation). No git races were encountered on this plan's two commits — `git status` was checked immediately before each commit and only this plan's intended file (`transcribe.routes.ts`, then `posts.routes.ts`) was ever staged; foreign files modified by the other two agents (`carousel.routes.ts`, `generate.routes.ts`, `edit.routes.ts`, `enhance.routes.ts`) were left untouched in the working tree at each commit point.

## Issues Encountered
None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All 7 affiliate-facing AI surfaces (generate, edit, carousel, slide-edit, enhance, transcribe, remake-caption) now resolve and bill to the affiliate's own OpenRouter key. GATE-06's core call-site migration is complete.
- Plan 21.1-07 (Settings UI: relabel Gemini field as video-only, remove OpenAI field, add OpenRouter field) can now proceed — the server-side contract it needs (`profiles.openrouter_api_key`, `getOpenRouterApiKey`) has been consumed by every route that needs it.
- No blockers.

---
*Phase: 21.1-affiliate-byok-migration*
*Completed: 2026-07-27*

## Self-Check: PASSED

All 2 modified files found on disk; both task commit hashes (0bd8767, ad510c0) found in git history.
