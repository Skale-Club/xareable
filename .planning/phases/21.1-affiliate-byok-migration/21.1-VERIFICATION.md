---
phase: 21.1-affiliate-byok-migration
verified: 2026-07-27T17:03:21Z
status: human_needed
score: 3/3 success criteria code-verified (1 requires live operator sign-off before close-out)
human_verification:
  - test: "Apply supabase/migrations/20260727000000_profiles_openrouter_api_key.sql to the live Supabase project"
    expected: "profiles.openrouter_api_key column exists in production; api_key and openai_api_key columns still present and unaffected"
    why_human: "Requires Supabase SQL editor / CLI access to the live project; not executable from the sandboxed dev environment"
  - test: "SC1 — sign in as a real affiliate, confirm exactly two key cards (OpenRouter, then Gemini video-only), save/reload/rotate the OpenRouter key"
    expected: "Both cards render, save round-trips through profiles.openrouter_api_key / profiles.api_key, rotation works without re-login"
    why_human: "Requires a live authenticated affiliate session against a real Supabase project"
  - test: "SC2 — generate as the affiliate with a real, funded second OpenRouter account and confirm the affiliate's balance drops while the platform balance does not; then point the model slug at a bogus value and confirm clean failure with no fallback to platform billing"
    expected: "usage_events attributes cost to the affiliate's own OpenRouter account; simulated failure does not fall through to the platform key"
    why_human: "Requires a real, funded second OpenRouter account and a live billing round-trip — cannot be simulated statically"
  - test: "GATE-08 affiliate video regression — with both keys set, generate + edit one video (must succeed); clear only the Gemini key and confirm the exact pre-SSE 400 video message while an image generation still succeeds"
    expected: "Video succeeds with both keys; video fails cleanly with the documented message when only the Gemini key is missing; image generation is unaffected"
    why_human: "Video is a paid, live direct-Google Veo call — no CI credentials, and the whole point of the check is proving a live billed feature wasn't broken"
  - test: "Non-affiliate regression — a regular user and an admin generation both still succeed on the platform key"
    expected: "No behavior change for non-affiliate traffic"
    why_human: "Requires live accounts/session to exercise end-to-end; not simulable from static source alone"
---

# Phase 21.1: Affiliate BYOK Migration Verification Report

**Phase Goal:** Admin/affiliate bring-your-own-key accounts migrate from Gemini/OpenAI keys to OpenRouter keys — provisioning and rotation work end-to-end, and every generation made with an affiliate's key is billed to that affiliate's OpenRouter account, never the platform balance. Old key columns are retained dead (additive migration, no lockout). A locked exception (21.1-CONTEXT.md) keeps affiliates on their existing Gemini key for the GATE-08-frozen direct-Google video path.
**Verified:** 2026-07-27T17:03:21Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

All code-level work for GATE-06 is complete, wired, and independently re-derived from source (not just trusted from SUMMARYs or the harness). One task — 21.1-07 Task 3, an operator live-credential sign-off — was explicitly deferred by user decision and is already recorded as pending in `21.1-HUMAN-UAT.md`. That deferral is reflected here as `human_needed`, not as a blocking gap: nothing in the code is missing, stubbed, or unwired; the remaining work is exercising real external accounts (a second funded OpenRouter account, a live Supabase SQL editor apply, and a paid Veo call).

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Affiliate can be provisioned with, and rotate, their own OpenRouter key via `profiles.openrouter_api_key`; old columns retained dead, no lockout | ✓ VERIFIED (code) / ? UNCERTAIN (live DB apply) | Migration file is additive (`ADD COLUMN IF NOT EXISTS`), `profileSchema` keeps `api_key`/`openai_api_key`/`openrouter_api_key` all present; Settings UI has working save handler (`handleSaveOpenrouterApiKey` → `profiles.update({ openrouter_api_key })`). Migration has NOT yet been applied to the live Supabase project (operator step, Task 3) |
| 2 | A generation made with an affiliate's OpenRouter key is billed to that affiliate's account, never the platform balance | ✓ VERIFIED (code) / ? UNCERTAIN (live billing round-trip) | All 7 affiliate-facing AI surfaces (generate, edit, carousel generate + slide-edit, enhance, transcribe, remake-caption) resolve `getOpenRouterApiKey(profile)` for affiliates and thread the resolved key — never a bare `config.OPENROUTER_API_KEY` — into `createGeminiService`, `selectImageApiKey`, `ensureCaptionQuality`, `CarouselGenerationParams.openRouterApiKey`, `EnhancementParams.openRouterApiKey`, and the transcribe gateway call. `selectImageApiKey` guards Pitfall 3 (image calls no longer silently inherit an empty `geminiApiKey`). Live provider-pinning / simulated-failure billing round-trip is the pending operator step |
| 3 | Affiliate without a configured OpenRouter key gets the exact clear error message instead of a silent 401 mid-SSE | ✓ VERIFIED | `getOpenRouterApiKey` returns `{ key: "", error: "Affiliate accounts must configure their own OpenRouter API key in Settings before generating." }`; every route's gate returns this as a pre-SSE `res.status(400).json(...)` before any stream/provider work begins (`generate.routes.ts` L234-245, `edit.routes.ts` L174-179, `carousel.routes.ts`, `enhance.routes.ts`, `transcribe.routes.ts`, `posts.routes.ts` L371-377). Fixture test `test-affiliate-key-resolution.ts` proves the string and the no-leak property with 9/9 PASS |
| 4 (locked exception) | Affiliates keep using their existing Gemini key for the GATE-08-frozen direct-Google video path — not a gap | ✓ VERIFIED | `generate.routes.ts` and `edit.routes.ts` both re-resolve `geminiApiKey` non-fatally after the OpenRouter gate (`if (ownApiKey) { const videoKeyRes = await getGeminiApiKey(profile); ... }`), then hard-block only `isVideo`/`isVideoPost` requests with a pre-SSE 400 pointing at the Settings field; `generateVideo({ ..., apiKey: geminiApiKey })` is otherwise unchanged. Settings UI retains the Gemini card, relabeled "Gemini API Key (video only)", same `data-testid="input-gemini-api-key"` and save handler as before |
| 5 | `getGeminiApiKey`/`getOpenAIApiKey`/`usesOwnApiKey` are byte-unchanged (no regression to the existing key-resolution pattern) | ✓ VERIFIED | Read directly from `server/middleware/auth.middleware.ts` L220-297 — identical shape/strings to the pre-phase pattern; `getOpenRouterApiKey` and `selectImageApiKey` are pure additions appended afterward |

**Score:** 3/3 ROADMAP success criteria are code-complete and independently verified against source; SC1's DB-apply and SC2's live-billing half, plus the GATE-08 video regression, remain an explicit, already-documented human/operator checkpoint (21.1-HUMAN-UAT.md, deferred by user decision — not a code gap).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `supabase/migrations/20260727000000_profiles_openrouter_api_key.sql` | Additive `ADD COLUMN IF NOT EXISTS openrouter_api_key` | ✓ VERIFIED | Confirmed via direct read — additive, drops nothing, comments document retention rationale |
| `shared/schema.ts` | `profileSchema.openrouter_api_key` nullable/optional; `api_key`/`openai_api_key` retained | ✓ VERIFIED | L51-53 all three present |
| `server/middleware/auth.middleware.ts` | `getOpenRouterApiKey` + `selectImageApiKey` exported, mirroring `getGeminiApiKey`/`getOpenAIApiKey` | ✓ VERIFIED | L317-371, exact SC3 string, `usesOwnApiKey` gate, platform tier reads `config.OPENROUTER_API_KEY` |
| `scripts/verify-phase-21.1.ts` | GATE-06 harness, `--only=<tag>` filter | ✓ VERIFIED, WIRED | Runs clean: 54/54 PASS |
| `scripts/test-affiliate-key-resolution.ts` | No-network fixture test | ✓ VERIFIED, WIRED | Runs clean: 9/9 PASS |
| `server/services/image-provider.ts` | `OpenRouterImageProvider` prefers `input.apiKey` | ✓ VERIFIED | L295, L317: `apiKey: input.apiKey \|\| requireOpenRouterKey()` |
| `server/services/gemini.service.ts` | `GeminiService(apiKey?, openRouterApiKey?)` + `createGeminiService` forwards it | ✓ VERIFIED | L117-129, L902-904; both OpenRouter branches (L449, L707) use `this.openRouterApiKey \|\| config.OPENROUTER_API_KEY` |
| `server/services/caption-quality.service.ts`, `carousel-generation.service.ts`, `enhancement.service.ts` | `openRouterApiKey` threaded through all internal AI calls | ✓ VERIFIED | Harness `svc-caption`/`svc-carousel`/`svc-enhancement` tags all green; spot-read confirms pattern |
| `server/routes/generate.routes.ts`, `edit.routes.ts` | Affiliate-aware gateway gate + GATE-08 video carve-out | ✓ VERIFIED (independently re-read, not just harness-trusted) | Full gate + carve-out logic read directly (see truths 3-4 evidence) |
| `server/routes/carousel.routes.ts`, `enhance.routes.ts` | Affiliate-aware gate in both handlers (generate + slide-edit) | ✓ VERIFIED | Direct grep confirms `getOpenRouterApiKey`/`selectImageApiKey` in both handlers of `carousel.routes.ts` (L145-166, L691-706) and `enhance.routes.ts` (L131-152) |
| `server/routes/transcribe.routes.ts`, `posts.routes.ts` | Affiliate-aware gate, 7th call site (remake-caption) | ✓ VERIFIED | Direct read confirms `getOpenRouterApiKey` gate + `openRouterApiKey` threaded to `ensureCaptionQuality`/gateway transcribe call |
| `client/src/pages/settings.tsx` | Exactly 2 affiliate key cards (OpenRouter + video-only Gemini); OpenAI card + provider radio removed | ✓ VERIFIED | Direct read L551-587 (OpenRouter card) and L588+ (Gemini card, relabeled, same `data-testid`); no `RadioGroup`/`openaiApiKey`/`image_provider` UI references remain |
| `client/src/lib/translations/pt.ts`, `es.ts` | New OpenRouter + video-only Gemini strings | ✓ VERIFIED (via harness `ui-settings` tags + SUMMARY cross-file grep evidence) | Not independently re-read line-by-line (low risk — pure string dictionary, harness + SUMMARY grep evidence sufficient) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `generate.routes.ts` affiliate gate | `getOpenRouterApiKey` | `if (ownApiKey)` branch | ✓ WIRED | Confirmed L234-245; non-affiliate branch still uses `getGeminiApiKey` verbatim |
| `generate.routes.ts` video branch | `getGeminiApiKey` (unchanged) | non-fatal re-resolution + `isVideo` guard | ✓ WIRED | L268-271, L370-380 — pre-SSE 400, actionable Settings pointer |
| `generate.routes.ts` image call | `OpenRouterImageProvider.generate` `input.apiKey` | `selectImageApiKey(...)` | ✓ WIRED | L569-574 |
| `generate.routes.ts` `createGeminiService` | `GeminiService.openRouterApiKey` | second positional arg | ✓ WIRED | L449: `createGeminiService(geminiApiKey, openRouterApiKey)` |
| `edit.routes.ts` `ensureCaptionQuality` | `caption-quality.service.ts` | `openRouterApiKey` param | ✓ WIRED | L610-612 |
| `carousel.routes.ts` (both handlers) | `carousel-generation.service.ts` / `image-provider.ts` | `openRouterApiKey` param / `selectImageApiKey` | ✓ WIRED | L154-166 (generate), L698-706 (slide-edit), L350/L933 (`selectImageApiKey`) |
| `enhance.routes.ts` | `enhancement.service.ts` | `openRouterApiKey` param | ✓ WIRED | L140-152, L324-335 |
| `transcribe.routes.ts` | `ai-gateway.service.ts transcribe()` | `orKey = openRouterApiKey \|\| config.OPENROUTER_API_KEY` | ✓ WIRED | L151 |
| `posts.routes.ts` remake-caption | `caption-quality.service.ts` | `openRouterApiKey` field | ✓ WIRED | L372-396 |
| `settings.tsx` `handleSaveOpenrouterApiKey` | `profiles.openrouter_api_key` | direct Supabase `.update()` | ✓ WIRED | L369-380, real network call, not a stub |
| `settings.tsx` OpenRouter/Gemini cards | `usesOwnApiKey(profile)` | conditional render | ✓ WIRED | L551, L588 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `settings.tsx` OpenRouter card | `openrouterApiKey` state | `profile?.openrouter_api_key` (initial) → `handleSaveOpenrouterApiKey` (Supabase update, not a stub write) | Yes | ✓ FLOWING |
| `generate.routes.ts`/`edit.routes.ts` `openRouterApiKey` | resolved via `getOpenRouterApiKey(profile)` | live `profiles` row select (`is_admin, is_affiliate, api_key, openai_api_key, image_provider, openrouter_api_key`) | Yes | ✓ FLOWING |
| Gateway calls (`ai-gateway.service.ts`) | `apiKey` param | threaded from route → service → gateway, never re-reads `config.OPENROUTER_API_KEY` when an affiliate key is present | Yes | ✓ FLOWING |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|--------------|-------------|--------|----------|
| GATE-06 | 21.1-01..07 (all 7 plans declare it) | Admin/affiliate BYO keys migrate to OpenRouter keys, additive migration, key resolution mirrors `getGeminiApiKey` | ✓ SATISFIED (code) / pending operator sign-off for full close-out | All 3 ROADMAP success criteria are independently code-verified above; live DB-apply + live billing round-trip + video regression are the deferred, documented Task 3 checkpoint |

No orphaned requirements: REQUIREMENTS.md maps only GATE-06 to Phase 21.1, and GATE-06 is declared in every plan's frontmatter. All requirement IDs accounted for.

**Note (process finding, not a code gap):** `.planning/REQUIREMENTS.md` line 17 currently shows `- [x] **GATE-06**` (and the requirements-map table shows "Complete"), but this was flipped to `[x]` in commit `218ed5d` ("docs(21.1-01): complete GATE-06 foundation plan") — i.e., after only the foundation plan (21.1-01) landed, six plans and the Task 3 operator sign-off before the full milestone was actually done. This directly contradicts 21.1-07-SUMMARY.md's own explicit instruction: *"Do not mark GATE-06 complete based on this summary alone"* and its frontmatter `requirements-completed: []`. The requirements doc should be corrected to reflect "Pending operator sign-off" until Task 3 closes, rather than "Complete." Flagging for awareness — not treated as a code gap since it doesn't affect the actual implementation.

### Anti-Patterns Found

None. Scanned all 13 modified/created source files (`server/routes/*.ts` x6, `server/middleware/auth.middleware.ts`, `server/services/*.ts` x5, `client/src/pages/settings.tsx`) for TODO/FIXME/HACK/placeholder/not-yet-implemented markers and empty-return stubs — zero matches beyond legitimate HTML `placeholder=` input attributes.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full GATE-06 harness | `npx tsx scripts/verify-phase-21.1.ts` | 54/54 PASS | ✓ PASS |
| No-network key-resolution fixture | `npx tsx scripts/test-affiliate-key-resolution.ts` | 9/9 PASS | ✓ PASS |
| Phase 21 regression | (embedded in harness, `[regression]` tag) | verify-phase-21.ts exits 0 | ✓ PASS |
| TypeScript compile | `npm run check` | clean, no errors | ✓ PASS |
| Live migration apply, live billing round-trip, live video regression | N/A — requires real external credentials | not run | ? SKIP → routed to human_verification |

### Human Verification Required

See YAML frontmatter `human_verification` block. Summary: this is exactly the deferred 21.1-07 Task 3 checkpoint, already recorded as pending in `21.1-HUMAN-UAT.md` (status: partial, 0/7 passed, 7 pending) by explicit user decision. It covers:
1. Applying the migration to the live Supabase project.
2. SC1 live provisioning/rotation check (two-card UI, save/reload/rotate).
3. SC3 live error-shape check across all 7 affiliate-facing AI surfaces.
4. SC2 live billing-attribution round-trip + simulated-failure provider-pinning check.
5. GATE-08 affiliate video regression (both-keys success, Gemini-key-cleared failure message, image-still-works no-lockout proof).
6. Non-affiliate regression (regular user + admin unaffected).

None of these gaps are code defects — every code-level artifact, wiring, and gate backing them has been independently verified against source in this report.

### Gaps Summary

No code gaps found. All must-haves across all 7 plans (01-07) are present, substantive, and wired, independently re-derived from source rather than trusted from SUMMARY claims or harness output alone — including a direct read of the generate/edit video carve-out and the Settings UI two-card layout, per the verification brief. The only outstanding item is the previously-deferred, user-approved Task 3 operator sign-off (live migration apply + SC1/SC2/SC3 live checks + video regression), which is a human/operational checkpoint, not an implementation gap. A documentation inconsistency was found (REQUIREMENTS.md prematurely marking GATE-06 complete after only the foundation plan) and is flagged above for correction but does not affect the phase's code-readiness verdict.

---

*Verified: 2026-07-27T17:03:21Z*
*Verifier: Claude (gsd-verifier)*
