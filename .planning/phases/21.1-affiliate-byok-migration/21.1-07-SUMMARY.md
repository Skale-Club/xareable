---
phase: 21.1-affiliate-byok-migration
plan: 07
subsystem: ui
tags: [openrouter, gemini, settings, i18n, affiliate, gate-closure]

# Dependency graph
requires:
  - phase: 21.1-01
    provides: profiles.openrouter_api_key column, profileSchema field, getOpenRouterApiKey/selectImageApiKey, verify-phase-21.1.ts harness
  - phase: 21.1-02..21.1-06
    provides: all 7 affiliate-facing AI surfaces (generate, edit, carousel, slide-edit, enhance, transcribe, remake-caption) gated on the affiliate's own OpenRouter key
provides:
  - Affiliate Settings UI with exactly two API-key cards — OpenRouter (new) + Gemini relabeled video-only
  - Full GATE-06 static/functional gate green (54/54 checks) with zero regression on Phase 21 (43/43)
  - Embedded MANUAL/LIVE VERIFICATION RUNBOOK in scripts/verify-phase-21.1.ts for the operator
affects: [22, 23, 24, 25, 26]

# Tech tracking
tech-stack:
  added: []
  patterns: ["translation-entry retention check before deleting a dictionary key — grep for ALL call sites across the client tree, not just the file being edited, before removing a t() string"]

key-files:
  created: []
  modified:
    - client/src/pages/settings.tsx
    - client/src/lib/translations/pt.ts
    - client/src/lib/translations/es.ts
    - scripts/verify-phase-21.1.ts

key-decisions:
  - "Retained the bare \"Gemini API Key\" translation entry in pt.ts/es.ts even though it was inside the block being replaced — client/src/pages/affiliate-dashboard.tsx (a separate, out-of-scope legacy page) still calls t(\"Gemini API Key\") for its own key field; deleting the dictionary entry would have silently broken pt/es translation there without touching that file."
  - "Confirmed (via grep across client/src) that every other AI-Image-Provider-block translation string (\"AI Image Provider\", \"Choose which provider generates your images...\", \"Use platform default\", \"Gemini\", \"OpenAI (gpt-image-2)\", \"Save Provider Preference\", and its toast strings) had zero remaining call sites after the settings.tsx edit, so all were safely deleted per the plan's explicit instruction."
  - "Did not run npm run build until Task 2's verification step, per plan's overall <verification> list (item 5) — resolves the standing STATE.md blocker \"npm run build not yet validated end-to-end since the merge\"; build now confirmed green (dist/index.cjs + dist/public emitted, no errors)."

patterns-established: []

requirements-completed: []  # GATE-06 NOT marked complete — Task 3 (operator sign-off) is still pending; see "Status" below.

# Metrics
duration: 8min (Tasks 1-2 only; Task 3 not started)
completed: PARTIAL — Tasks 1-2 of 3 complete, 2026-07-27
---

# Phase 21.1 Plan 07: Affiliate Settings UI (OpenRouter card + video-only Gemini) + phase-gate closure Summary

**STATUS: PARTIAL COMPLETION — Tasks 1 and 2 are done and committed (code-complete, full GATE-06 gate green); Task 3 is a blocking operator checkpoint that has NOT been performed and is NOT being fabricated here.**

Affiliate Settings now shows exactly two API-key cards — a new "OpenRouter API Key" card (`profiles.openrouter_api_key`) and the retained Gemini card relabeled "Gemini API Key (video only)" — with the OpenAI key card and AI Image Provider radio card fully removed; the full GATE-06 static/functional harness is green (54/54) with Phase 21 unregressed (43/43) and `npm run build` validated end-to-end for the first time since the merge. The plan's Task 3 — operator sign-off on migration application, live SC1/SC2/SC3 checks, and the affiliate video regression — requires a real second funded OpenRouter account, Supabase SQL editor access, and a paid Veo call, none of which are available in this execution environment.

## Performance

- **Duration:** ~8 min (Tasks 1-2)
- **Tasks:** 2 of 3 completed (Task 3 is a blocking checkpoint, not started)
- **Files modified:** 4

## Accomplishments
- `client/src/pages/settings.tsx`: added the OpenRouter API key card (state, handler `handleSaveOpenrouterApiKey`, JSX — first card, gated on `usesOwnApiKey(profile)`); relabeled the retained Gemini card's title/description/label to the exact video-only strings while keeping `data-testid="input-gemini-api-key"`, `handleSaveGeminiApiKey`, and all other Gemini-card internals byte-unchanged; deleted the OpenAI key card, the AI Image Provider radio card, their handlers/state, and the now-unused `RadioGroup`/`RadioGroupItem` import.
- `client/src/lib/translations/pt.ts` + `es.ts`: added the 5 new OpenRouter strings and 3 new video-only Gemini strings; removed 14 orphaned OpenAI/image-provider strings confirmed (via cross-file grep) to have zero remaining call sites; retained the bare `"Gemini API Key"` entry because `affiliate-dashboard.tsx` (out of this plan's scope) still depends on it.
- `scripts/verify-phase-21.1.ts`: ran the full unfiltered harness — 54/54 PASS, zero fixes needed (every check written in 21.1-01..06 matched the real implementation exactly); appended the MANUAL/LIVE VERIFICATION RUNBOOK comment block (migration-apply step, SC1 provisioning/rotation, SC3 error shape, SC2 billing attribution + simulated-failure provider-pinning, the affiliate-video-regression carve-out, non-affiliate regression, and the documented GATE-07 rollback limitation) verbatim as specified — a pure addition after the final `console.log` (`git diff --stat` shows only `+90` lines, zero deletions/modifications to any existing check).
- Ran `npx tsx scripts/verify-phase-21.ts` (43/43, unregressed), `npx tsx scripts/test-affiliate-key-resolution.ts` (9/9), `npm run check` (clean), and `npm run build` (clean — first full build validation since the post-merge concern flagged in STATE.md Blockers).

## Task Commits

Each task was committed atomically:

1. **Task 1: Settings UI — OpenRouter card, video-only Gemini relabel, delete OpenAI + image-provider cards** - `17914de` (feat)
2. **Task 2: Close the phase gate + append the manual/live runbook** - `c172db7` (docs)

**Task 3: Operator verification (migration, SC1/SC2/SC3, affiliate video regression)** — NOT STARTED. This is a `checkpoint:human-verify` task with `gate="blocking"` requiring real credentials (second OpenRouter account, Supabase SQL editor, paid Veo call) not available in this execution environment. See "Checkpoint Details" below.

**Plan metadata:** this commit (STATE.md/ROADMAP.md/SUMMARY.md only — Task 3 still pending, so this is NOT a phase-closure commit)

## Files Created/Modified
- `client/src/pages/settings.tsx` - OpenRouter key card (new, first) + Gemini card relabeled video-only; OpenAI card, image-provider radio card, and their handlers/state removed; unused `RadioGroup` import removed
- `client/src/lib/translations/pt.ts` - New OpenRouter + video-only-Gemini strings; orphaned OpenAI/image-provider strings removed; bare "Gemini API Key" retained for affiliate-dashboard.tsx
- `client/src/lib/translations/es.ts` - Same as pt.ts, Spanish strings
- `scripts/verify-phase-21.1.ts` - MANUAL/LIVE VERIFICATION RUNBOOK comment block appended (pure addition, no check modified)

## Decisions Made

See `key-decisions` in frontmatter — the translation-retention decision (keeping bare `"Gemini API Key"` because of `affiliate-dashboard.tsx`) is the one non-obvious call made during execution; everything else followed the plan's literal instructions.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Retained a translation dictionary entry the plan's literal code block would have deleted**
- **Found during:** Task 1
- **Issue:** The plan's literal replacement block for `pt.ts`/`es.ts` (lines 515-526 / 453-464) does not include the bare `"Gemini API Key"` entry. The plan's own action text explicitly instructs: "Before deleting any OTHER dictionary entry (`"Gemini API Key"`, ...), grep for remaining call sites ... and remove ONLY the entries with zero remaining usages." Grepping the whole `client/src` tree found `client/src/pages/affiliate-dashboard.tsx:173` still calls `t("Gemini API Key")` for its own (out-of-scope, unrelated) legacy key field. Applying the literal replacement block as written would have silently broken pt-BR/es translation on that page.
- **Fix:** Re-added `"Gemini API Key": "Chave de API Gemini"` (pt) / `"Clave de API de Gemini"` (es) immediately after the new Phase 21.1 blocks, with a comment explaining why it's retained.
- **Files modified:** `client/src/lib/translations/pt.ts`, `client/src/lib/translations/es.ts`
- **Verification:** Confirmed via `grep -rn '"Gemini API Key")' client/src` both before and after the edit that the entry's sole other consumer (`affiliate-dashboard.tsx`) is unaffected.
- **Committed in:** `17914de` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 — bug/translation-regression prevention)
**Impact on plan:** Necessary correctness fix within the plan's own stated scope-check instruction; no scope creep — no other file was touched.

## Issues Encountered
- The plan's Task 2 acceptance-criteria companion check (`grep -c "config.OPENROUTER_API_KEY" server/middleware/auth.middleware.ts` expected to equal exactly `2`) actually returns `3` in the current codebase — the third hit is a prose comment inside `getOpenRouterApiKey`'s JSDoc (line 311, written in plan 21.1-01: "...all gateway call sites already read config.OPENROUTER_API_KEY..."), not a third code read. The two functional reads (`if (!config.OPENROUTER_API_KEY)` guard and `return { key: config.OPENROUTER_API_KEY }`) are unchanged and correct. This is a pre-existing comment from a prior plan (21.1-01), out of this plan's `files_modified` scope (`scripts/verify-phase-21.1.ts` only), and is not an automated check embedded in the harness itself (`verify-phase-21.1.ts` passed 54/54 with no such check) — it was only a manual/informational acceptance-criteria line in the plan text. Logged here rather than "fixed" since editing `auth.middleware.ts` is outside this task's file scope and the comment is accurate prose, not a functional defect.

## User Setup Required

**External/live verification required — Task 3 has NOT been performed.** See "Checkpoint Details" below for the full operator runbook (also embedded at the bottom of `scripts/verify-phase-21.1.ts`).

## Next Phase Readiness

- All code-level GATE-06 work is done: two-card affiliate Settings UI, full harness green, Phase 21 unregressed, `npm run check` + `npm run build` both clean.
- **Phase 21.1 is NOT yet closable.** Task 3 (operator sign-off) must run before GATE-06 can be marked validated in PROJECT.md/REQUIREMENTS.md or before Phase 21.1's ROADMAP checkbox is checked. Do not mark GATE-06 complete based on this summary alone.
- Downstream phases (22-26) depend on Phase 21 (already complete), not on Phase 21.1's UI plan specifically — they are not blocked by Task 3's pending status, but the milestone cannot be considered fully shipped until it resolves.

---
*Phase: 21.1-affiliate-byok-migration*
*Completed: PARTIAL (Tasks 1-2 only) — 2026-07-27*

## Self-Check: PASSED

All 4 modified files found on disk; both task commit hashes (17914de, c172db7) found in git history. Task 3 correctly NOT recorded as complete.

## Checkpoint Details (Task 3 — not performed)

See the "CHECKPOINT REACHED" report returned to the orchestrator for the full structured checkpoint (type: human-verify, gate: blocking). Summary: the operator must (1) apply `supabase/migrations/20260727000000_profiles_openrouter_api_key.sql` via the Supabase SQL editor, then (2) follow the 7-step runbook embedded at the bottom of `scripts/verify-phase-21.1.ts` (SC1 provisioning/rotation, SC3 error shape, SC2 billing attribution, SC2 simulated-failure provider pinning, affiliate video regression, non-affiliate regression, and the documented GATE-07 rollback limitation) using a real, funded second OpenRouter account. On resume, a continuation agent should record the operator's per-step outcome under a "Manual verification (SC1/SC2/SC3 + video regression)" heading appended to this file, and only then proceed to mark GATE-06 complete / check the Phase 21.1 ROADMAP box, or otherwise stop and capture the failure verbatim per the plan's Task 3 instructions.
