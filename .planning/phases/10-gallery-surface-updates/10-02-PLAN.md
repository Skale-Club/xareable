---
phase: 10-gallery-surface-updates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/lib/translations.ts
autonomous: true
requirements: [GLRY-01, GLRY-02, GLRY-03]
requirements_addressed: [GLRY-01, GLRY-02, GLRY-03]

must_haves:
  truths:
    - "All 12 new gallery i18n strings exist in pt and es dictionaries"
    - "Param-based keys ({n}, {total}) follow the existing pattern"
    - "TypeScript compiles cleanly after additions"
  artifacts:
    - path: "client/src/lib/translations.ts"
      provides: "12 new gallery.* translations for pt and es"
      contains: '"Carousel · {n}"'
  key_links:
    - from: "client/src/pages/posts.tsx (Plan 03)"
      to: "client/src/lib/translations.ts"
      via: "t(\"Carousel · {n}\", { n: count })"
      pattern: "Carousel · \\{n\\}"
    - from: "client/src/components/post-viewer-dialog.tsx (Plan 04)"
      to: "client/src/lib/translations.ts"
      via: "t(\"Slide {n} of {total}\", { n, total })"
      pattern: "Slide \\{n\\} of \\{total\\}"
---

<objective>
Add the 12 new gallery i18n strings (Carousel · N badge, Enhanced badge, Draft badge, slide viewer strings, accessibility labels) to `client/src/lib/translations.ts` for both PT and ES dictionaries (EN keys are the source-of-truth strings themselves per the project's flat-key i18n pattern).

Purpose: Plans 03 (gallery tile rendering) and 04 (slide viewer + SSE error handling) consume these translations. Adding them as a separate Wave 1 plan unblocks both downstream plans to run in parallel in Wave 2.

Output: Two new blocks of 12 entries each appended to the PT and ES dictionaries inside `translations.ts`. EN dictionary unchanged (it is empty by design — `t()` falls back to the key itself).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-gallery-surface-updates/10-UI-SPEC.md
@client/src/lib/translations.ts
@CLAUDE.md

<interfaces>
<!-- The translations file is a flat-key dictionary keyed by English string. -->
<!-- Phase 9 already added a "// Phase 9 — Carousel & Enhancement creator strings" section in both pt (line 503) and es (line 974). -->
<!-- Phase 10 follows the same convention: add a new section comment, then the 12 entries. -->

```typescript
// shape (line 1-4 of translations.ts):
type TranslationDictionary = Record<SupportedLanguage, Record<string, string>>;
export const translations: TranslationDictionary = {
  en: {},     // empty — t() falls back to the English key itself
  pt: { ... }, // append new keys here
  es: { ... }, // append new keys here
};
```

`t()` is called with the English string as key; param interpolation uses `{n}` / `{total}` braces and is handled by the runtime translation function (existing pattern — see "Slide {n} failed" at line 536).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add 12 gallery i18n strings to PT and ES dictionaries</name>
  <files>client/src/lib/translations.ts</files>
  <read_first>
    - client/src/lib/translations.ts (READ lines 500-550 to see the existing Phase 9 carousel section in pt; READ lines 970-1020 for the same section in es; the new keys must be inserted in the same flat-key style, ideally directly after the Phase 9 block)
    - .planning/phases/10-gallery-surface-updates/10-UI-SPEC.md (section "i18n Keys Summary", table at lines 388-407)
  </read_first>
  <action>
    Open `client/src/lib/translations.ts`. There are exactly two locations to modify: the `pt` dictionary block and the `es` dictionary block. The `en` dictionary stays `{}`.

    **In the `pt` block (currently starts at line 8, the Phase 9 carousel section is at lines 503-560):**
    Locate the last entry of the Phase 9 carousel/enhancement creator block (approximately line 560 — find the section comment `// Phase 9 — Carousel & Enhancement creator strings` and walk to its last entry). Immediately AFTER the last Phase 9 entry, insert this block of 13 lines (1 comment + 12 entries):

    ```typescript
        // Phase 10 — Gallery surface updates
        "Carousel · {n}": "Carrossel · {n}",
        "Enhanced": "Aprimorado",
        "Draft": "Rascunho",
        "Slide {n} of {total}": "Slide {n} de {total}",
        "Loading slides…": "Carregando slides…",
        "No slides available.": "Nenhum slide disponível.",
        "Could not load slides": "Não foi possível carregar os slides",
        "Please try again.": "Por favor, tente novamente.",
        "Image post": "Postagem de imagem",
        "Video post": "Postagem de vídeo",
        "Carousel post": "Postagem de carrossel",
        "Enhancement post": "Postagem aprimorada",
    ```

    **In the `es` block (Phase 9 carousel section at lines 974-1030):**
    Locate the last entry of the Phase 9 block in the `es` dictionary. Immediately after it, insert:

    ```typescript
        // Phase 10 — Gallery surface updates
        "Carousel · {n}": "Carrusel · {n}",
        "Enhanced": "Mejorado",
        "Draft": "Borrador",
        "Slide {n} of {total}": "Diapositiva {n} de {total}",
        "Loading slides…": "Cargando diapositivas…",
        "No slides available.": "No hay diapositivas disponibles.",
        "Could not load slides": "No se pudieron cargar las diapositivas",
        "Please try again.": "Por favor, inténtalo de nuevo.",
        "Image post": "Publicación de imagen",
        "Video post": "Publicación de video",
        "Carousel post": "Publicación de carrusel",
        "Enhancement post": "Publicación mejorada",
    ```

    **Critical rules:**
    - Use the EXACT English keys shown above (case, punctuation, spacing). The middle dot in "Carousel · {n}" is U+00B7 (·), not U+2022 (•) — copy-paste exactly.
    - The ellipsis in "Loading slides…" is U+2026 (…), single character, not three dots.
    - Do NOT modify the `en: {}` block — it stays empty.
    - Do NOT add these keys anywhere else in the file. Each English key must appear exactly twice in the file (once in `pt`, once in `es`).
    - Do NOT remove or reorder any existing entries.
    - "Previous" and "Next" already exist (used by the gallery pagination); do NOT re-add them.
    - "Caption" already exists (used by the post viewer); do NOT re-add it.

    No new imports needed.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"Carousel · {n}"' client/src/lib/translations.ts` returns exactly `2` (one pt, one es).
    - `grep -c '"Enhanced":' client/src/lib/translations.ts` returns exactly `2`.
    - `grep -c '"Draft":' client/src/lib/translations.ts` returns exactly `2`.
    - `grep -c '"Slide {n} of {total}"' client/src/lib/translations.ts` returns exactly `2`.
    - `grep -c '"Loading slides…"' client/src/lib/translations.ts` returns exactly `2`.
    - `grep -c '"No slides available."' client/src/lib/translations.ts` returns exactly `2`.
    - `grep -c '"Could not load slides"' client/src/lib/translations.ts` returns exactly `2`.
    - `grep -c '"Image post":' client/src/lib/translations.ts` returns exactly `2`.
    - `grep -c '"Video post":' client/src/lib/translations.ts` returns exactly `2`.
    - `grep -c '"Carousel post":' client/src/lib/translations.ts` returns exactly `2`.
    - `grep -c '"Enhancement post":' client/src/lib/translations.ts` returns exactly `2`.
    - `grep -c "// Phase 10 — Gallery surface updates" client/src/lib/translations.ts` returns exactly `2` (one pt, one es section comment).
    - PT translation contains "Carrossel · {n}" (verifiable via grep).
    - ES translation contains "Carrusel · {n}" (verifiable via grep).
    - `en: {}` block remains empty: `grep -A 1 "en: {}," client/src/lib/translations.ts` shows the next non-empty entry is `pt:`.
    - `npm run check` exits with code 0.
  </acceptance_criteria>
  <done>
    All 12 new gallery strings present in both `pt` and `es` dictionaries with the correct translations; `en` dictionary unchanged; TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
- All 12 English keys appear exactly twice in `translations.ts` (once per non-EN locale)
- PT/ES section comments mark the Phase 10 block in both dictionaries
- `npm run check` passes
</verification>

<success_criteria>
- 24 new dictionary entries (12 PT + 12 ES) committed
- No existing keys modified
- Param syntax `{n}` and `{total}` preserved verbatim across translations
</success_criteria>

<output>
After completion, create `.planning/phases/10-gallery-surface-updates/10-02-SUMMARY.md` documenting:
- 12 new gallery keys added to PT and ES dictionaries
- Section comments mark the Phase 10 block
- Pattern matches Phase 9 carousel/enhancement section
</output>
