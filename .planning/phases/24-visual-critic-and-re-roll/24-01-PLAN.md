---
phase: 24-visual-critic-and-re-roll
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-phase-24.ts
  - shared/schema.ts
  - server/services/ai-gateway-settings.service.ts
autonomous: true
requirements: [CRIT-01, CRIT-02, CRIT-03, CRIT-04, CRIT-05]

must_haves:
  truths:
    - "Running `npx tsx scripts/verify-phase-24.ts --only=self-test` exits 0 and proves the harness itself is not vacuous"
    - "Running the full harness exits non-zero with honest, specific failures for the code plans 24-02..24-06 have not written yet"
    - "A `critic` value is a legal FallbackCallClass and a legal ai_models key"
    - "A generation_logs row with event_kind='visual_critic' passes Zod validation"
  artifacts:
    - path: "scripts/verify-phase-24.ts"
      provides: "Phase 24 gate harness, 6 --only tags"
      min_lines: 300
    - path: "shared/schema.ts"
      provides: "aiModelsSchema.critic + generationLogSchema.event_kind 'visual_critic'"
      contains: "visual_critic"
    - path: "server/services/ai-gateway-settings.service.ts"
      provides: "FallbackCallClass widened with 'critic' + DEFAULT_FALLBACKS.critic"
      contains: "critic"
  key_links:
    - from: "scripts/verify-phase-24.ts"
      to: "server/services/ai-gateway.service.ts"
      via: "readSafe + isFallbackWorthy regex re-execution"
      pattern: "isFallbackWorthy|aborted"
    - from: "shared/schema.ts aiModelsSchema"
      to: "server/routes/style-catalog.routes.ts getStyleCatalogPayload"
      via: "styleCatalogSchema.safeParse default backfill (no migration)"
      pattern: "critic: z\\.string\\(\\)\\.default"
---

<objective>
Install the Phase 24 verification harness (the Wave 0 Nyquist requirement) and the three
zero-risk type/enum widens every later plan in this phase compiles against.

Purpose: nothing in Phase 24 can be verified until `scripts/verify-phase-24.ts` exists, and
nothing can be written until `"critic"` is a legal call class and `ai_models.critic` /
`event_kind: "visual_critic"` are legal values. This plan is deliberately the only Wave 1
plan so that Wave 2 can run three plans in parallel against a stable contract.
Output: a red-but-honest phase gate + three additive schema/type widens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-visual-critic-and-re-roll/24-CONTEXT.md
@.planning/phases/24-visual-critic-and-re-roll/24-RESEARCH.md
@.planning/phases/24-visual-critic-and-re-roll/24-VALIDATION.md

<interfaces>
<!-- Contracts this plan widens. Executor should use these directly — no codebase exploration needed. -->

server/services/ai-gateway-settings.service.ts (current, lines 19-33):
```typescript
export type CallClass = "planning" | "image" | "transcription";
export type RoutingMode = "openrouter" | "direct";
export type FallbackCallClass = "text" | "image" | "transcription";

const DEFAULT_ROUTING: Record<CallClass, RoutingMode> = { planning: "openrouter", image: "openrouter", transcription: "openrouter" };
const DEFAULT_FALLBACKS: Record<FallbackCallClass, string[]> = { text: [], image: [], transcription: [] };
```

shared/schema.ts (current, lines 180-196):
```typescript
export const aiModelsSchema = z.object({
  image_generation: z.string().default("gemini-3.1-flash-image-preview"),
  text_generation: z.string().default("gemini-2.5-flash"),
  audio_transcription: z.string().default("gemini-2.5-flash"),
  video_generation: z.string().default("veo-3.1-generate-preview"),
  planning: z.string().default("gemini-2.5-pro"),
});
```

shared/schema.ts (current, lines 1167-1177):
```typescript
  event_kind: z.enum([
    "text_verification", "caption_quality", "subject_fidelity",
    "model_fallback", "planning_schema_failure",
  ]).nullable().optional(),
```

server/services/ai-gateway.service.ts callWithFallback (line 104) — the regex the abort
assertion re-executes verbatim:
```typescript
const isFallbackWorthy = /\b(404|410|5\d\d|model_not_found)\b/i.test(msg);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/verify-phase-24.ts — the 6-tag phase gate</name>
  <files>scripts/verify-phase-24.ts</files>
  <read_first>
    - scripts/verify-phase-23.ts lines 1-200 (the `--only=` filter scaffold, `check`/`readSafe`/`exists`/`tagActive`/`walkFiles` helpers, the "load sources ONCE with readSafe" block, and the `[self-test]` tag-literal assertion)
    - scripts/verify-phase-22.ts lines 285-331 (the PASS/FAIL summary + `process.exit(1)` convention)
    - .planning/phases/24-visual-critic-and-re-roll/24-VALIDATION.md (tag names and the per-requirement test map)
    - .planning/phases/24-visual-critic-and-re-roll/24-RESEARCH.md "Validation Architecture" section
  </read_first>
  <action>
Create `scripts/verify-phase-24.ts` by copying `scripts/verify-phase-23.ts`'s scaffold verbatim
(the `failures`/`ok` arrays, the `--only=` regex parse, `check()`, `read()`, `exists()`,
`readSafe()`, `tagActive()`, the `async function main()` wrapper, and the tail PASS/FAIL summary
that `process.exit(1)`s on any failure). Header comment must state: created by plan 24-01,
extended by plan 24-07 (which adds the 7th `[svc-cross-plan]` tag); plans 24-02..24-06 must NOT
edit this file — their job is to turn its red checks green.

Load sources ONCE at module scope with `readSafe` (NOT `read`) — every Phase 24 target file is
written by a later plan and does not exist yet:
`server/services/visual-critic.service.ts`, `server/services/ai-gateway.service.ts`,
`server/services/ai-gateway-settings.service.ts`, `server/services/observability.service.ts`,
`server/services/image-provider.ts`, `server/routes/generate.routes.ts`,
`server/routes/admin-settings.routes.ts`, `server/quota.ts`, `shared/schema.ts`,
`client/src/components/admin/post-creation/ai-models-card.tsx`.

Write exactly these 6 tags. Every check name MUST start with its tag literal in square brackets.

`[self-test]` (5 checks):
  1. "harness executes" — `check(..., true)`
  2. "harness source contains all 6 Phase 24 tag literals" — read own source, assert all of
     `[self-test]`, `[svc-critic-call]`, `[svc-reroll-logic]`, `[svc-billing-reroll]`,
     `[svc-abort-signal]`, `[svc-observability]` appear
  3. "Wave 0 unit harness scripts/test-critic-reroll-logic.ts exists" — `exists(...)` (red until 24-05)
  4. "Wave 0 live smoke scripts/verify-critic-live.ts exists" — `exists(...)` (red until 24-05)
  5. "scanner is not vacuous" — assert a deliberately absent literal
     (`"__phase24_sentinel_never_present__"`) is NOT found in `generateRouteSrc`, proving the
     grep-style checks can actually report absence

`[svc-critic-call]` (CRIT-01, 9 checks):
  1. `server/services/visual-critic.service.ts` exists
  2. exports `CRITIC_JSON_SCHEMA`
  3. schema literal contains `strict: true` AND `additionalProperties: false`
  4. schema declares all six properties: `composition_score`, `color_harmony_score`,
     `text_legibility_zone_score`, `unwanted_text_detected`, `unwanted_text_detail`, `reasoning`
  5. bounded scores use `enum: [1, 2, 3, 4, 5]` (>=3 occurrences) — 24-RESEARCH Pitfall 2
  6. schema source contains NEITHER `minimum:` NOR `maximum:` — Pitfall 2 hard rule
  7. `runVisualCritic` calls `chatCompletion` with `callClass: "critic"` — Pitfall 1
  8. uses `toOpenRouterInputReference` (imported from ai-gateway.service.js) — no hand-rolled data-URI
  9. `shared/schema.ts` `aiModelsSchema` contains a `critic:` field AND `generate.routes.ts`
     references `ai_models?.critic`

`[svc-reroll-logic]` (CRIT-02, 6 checks):
  1. visual-critic.service.ts exports `MAX_REROLL_ATTEMPTS` bound to the literal `2`
  2. generate.routes.ts contains a bounded loop `attempt <= MAX_REROLL_ATTEMPTS + 1`
  3. generate.routes.ts calls `runVisualCritic(` exactly once (count occurrences === 1)
  4. visual-critic.service.ts exports `selectFinalAttempt`
  5. visual-critic.service.ts exports `computeRerollMetadata`
  6. `spawnSync("npx", ["tsx", "scripts/test-critic-reroll-logic.ts"])` exits 0 — guard with
     `tagActive("[svc-reroll-logic]")` so the subprocess only runs when the tag is selected;
     include the child's stderr tail in the failure detail

`[svc-billing-reroll]` (CRIT-03, 6 checks):
  1. `server/quota.ts` `recordUsageEvent` signature declares `extraMetadata?: Record<string, unknown>`
  2. quota.ts metadata object spreads `...extraMetadata`
  3. quota.ts `hasGatewayMeta` also fires on a non-empty `extraMetadata`
     (regex requires `extraMetadata` to appear inside the `hasGatewayMeta` assignment expression)
  4. generate.routes.ts contains exactly ONE `recordUsageEvent(` call site
  5. BILLING INVARIANT: extract the `const realCostUsdMicros =` ... `;` expression and the
     `const gatewayRealCost =` ... `;` expression from generate.routes.ts and assert NEITHER
     contains `reroll` (case-insensitive) — the discarded-attempt cost must never reach the charge
  6. generate.routes.ts still calls `deductCredits(` with `usageEvent.cost_usd_micros` and
     `usageEvent.charged_amount_micros` (unchanged billing structure)

`[svc-abort-signal]` (CRIT-04, 9 checks):
  1. `ChatCompletionParams` declares `signal?: AbortSignal`
  2. ai-gateway.service.ts passes the signal as `chat.completions.create`'s SECOND argument —
     assert the source matches `/chat\.completions\.create\([\s\S]{0,1200}\},\s*\{\s*signal:/`
     (do this for the whole file; both `chatCompletion` and `transcribe` may match, require >=1)
  3. `GatewayImageParams` declares `signal?: AbortSignal`
  4. `callImageApi`'s `fetch("https://openrouter.ai/api/v1/images", {...})` init object contains
     `signal:` — extract the fetch call text and assert
  5. `ImageGenerationInput` (image-provider.ts) declares `signal?: AbortSignal` and
     `OpenRouterImageProvider.generate` forwards `signal: input.signal`
  6. generate.routes.ts constructs `new AbortController()` and its safety timer body calls
     `controller.abort()`
  7. generate.routes.ts threads `signal: controller.signal` into BOTH `provider.generate(` and
     `runVisualCritic(`
  8. generate.routes.ts declares `CRITIC_REROLL_BUDGET_MS` and derives its safety timeout from
     `config.GENERATION_SAFETY_TIMEOUT_MS ?? 280_000` PLUS that budget
  9. FUNCTIONAL (not grep): re-execute ai-gateway.service.ts's real fallback trigger regex
     `/\b(404|410|5\d\d|model_not_found)\b/i` against `"This operation was aborted"` and
     `"The operation was aborted"` and assert BOTH are false (an abort must not burn the
     fallback chain), and against `"model_not_found"` assert true (proves the regex is live,
     not a typo). Source the regex literal by parsing it out of `aiGatewaySrc` with
     `/const isFallbackWorthy = (\/.+\/i)\.test/` and `eval`-free reconstruction via `new RegExp`
     on the extracted pattern body — if extraction fails, FAIL the check rather than falling
     back to a hardcoded copy.

`[svc-observability]` (CRIT-05, 6 checks):
  1. observability.service.ts exports `logVisualCritic`
  2. its body inserts `event_kind: "visual_critic"` into `generation_logs`
  3. it uses `createAdminSupabase()` and wraps everything in `try { ... } catch {}` (fire-and-forget)
  4. `shared/schema.ts` generationLogSchema `event_kind` enum includes `"visual_critic"`
  5. generate.routes.ts calls `logVisualCritic(` exactly TWICE (success path + hard-fail path)
  6. FK SAFETY: the hard-fail call passes `postId: null` — assert generate.routes.ts contains
     `postId: null` within 400 characters of a `logVisualCritic(` occurrence

Do NOT weaken any check to make it pass. Every check above is EXPECTED to be red today except
the `[self-test]` group, `[svc-abort-signal]` check 9, and `[svc-billing-reroll]` check 6.
  </action>
  <verify>
    <automated>npx tsx scripts/verify-phase-24.ts --only=self-test</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/verify-phase-24.ts --only=self-test` exits 0 with 5 PASS lines
    - `npx tsx scripts/verify-phase-24.ts --only=svc-abort-signal` reports the AbortError-regex
      check (check 9) as PASS and the other 8 as FAIL
    - `npx tsx scripts/verify-phase-24.ts` exits 1, prints zero uncaught exceptions, and every
      failure line names a real, not-yet-written artifact
    - `grep -c "^check(\|  check(" scripts/verify-phase-24.ts` >= 35
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>The Phase 24 gate exists, is honestly red, and its self-test proves it is not vacuous.</done>
</task>

<task type="auto">
  <name>Task 2: Widen shared/schema.ts — ai_models.critic + event_kind "visual_critic"</name>
  <files>shared/schema.ts</files>
  <read_first>
    - shared/schema.ts lines 175-200 (aiModelsSchema, including the Phase 22 `planning` field comment)
    - shared/schema.ts lines 1160-1180 (generationLogSchema observability fields)
    - server/routes/style-catalog.routes.ts lines 17-33 (`getStyleCatalogPayload` uses
      `styleCatalogSchema.safeParse` — this is why a `.default()` backfills with NO migration)
  </read_first>
  <action>
1. Add `critic: z.string().default("gemini-2.5-flash"),` to `aiModelsSchema` immediately after the
   `planning` field. Comment it in the same voice as the `planning` field:
   - Phase 24 (CRIT-01). Dedicated slug for the multimodal visual-critic call. Deliberately NOT a
     repoint of `planning` (which is Pro-tier and runs once per generation) — the critic runs 1-3
     times per generation, so it defaults to a flash tier.
   - MUST be a slug with BOTH OpenRouter `structured_outputs` support AND `image` input modality.
   - Additive: `styleCatalogSchema.safeParse()` backfills this default on every stored row, so NO
     migration is required (same mechanism Phase 22 used for `planning`).
   - Unlike `planning`, the critic call class has NO GATE-07 "direct" rollback branch
     (24-CONTEXT.md locked decision: OpenRouter-only), so the bare-vs-prefixed slug form only has
     to satisfy `normalizeOpenRouterModelSlug` — bare `gemini-2.5-flash` becomes
     `google/gemini-2.5-flash`.

2. BEFORE committing the default slug, re-verify capability live (24-RESEARCH Pitfall 6 — the
   OpenRouter catalog demonstrably changed within a single day):
   `curl -s "https://openrouter.ai/api/v1/models?supported_parameters=structured_outputs" | grep -o '"id":"google/gemini-2.5-flash"'`
   and confirm the same entry's `architecture.input_modalities` contains `"image"`. If
   `google/gemini-2.5-flash` is absent or lacks image input, pick the cheapest slug that has both
   and record the substitution + the raw evidence in the SUMMARY. If the endpoint is unreachable
   from this environment, keep `gemini-2.5-flash` and record "catalog unreachable, default
   unverified — re-verify in 24-07's live runbook" in the SUMMARY.

3. Add `"visual_critic", // Phase 24 (CRIT-05)` to `generationLogSchema.event_kind`'s
   `z.enum([...])`, after `"planning_schema_failure"`. No migration — `generation_logs.event_kind`
   is unconstrained TEXT (confirmed: `supabase/migrations/20260508000000_generation_logs_observability.sql`
   has no CHECK constraint), same precedent as `model_fallback` and `planning_schema_failure`.

Change nothing else in this file.
  </action>
  <verify>
    <automated>npx tsx scripts/verify-phase-24.ts --only=svc-observability && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'critic: z.string().default(' shared/schema.ts` returns exactly 1 line
    - `grep -n '"visual_critic"' shared/schema.ts` returns exactly 1 line
    - `npx tsx scripts/verify-phase-24.ts --only=svc-observability` shows check 4
      (`event_kind` enum includes "visual_critic") as PASS
    - `npm run check` exits 0 (no existing StyleCatalog construction breaks — `.default()` makes
      the field optional on the INPUT type)
    - `npx tsx scripts/verify-phase-23.ts` and `npx tsx scripts/verify-phase-22.ts` still exit 0
  </acceptance_criteria>
  <done>`ai_models.critic` and `event_kind: "visual_critic"` are legal values with zero migration and zero regression.</done>
</task>

<task type="auto">
  <name>Task 3: Widen FallbackCallClass with "critic"</name>
  <files>server/services/ai-gateway-settings.service.ts</files>
  <read_first>
    - server/services/ai-gateway-settings.service.ts lines 19-33 (CallClass, FallbackCallClass, DEFAULT_ROUTING, DEFAULT_FALLBACKS)
    - server/services/ai-gateway-settings.service.ts lines 48-59 and 77-91 (getFallbackChain / setFallbackChain)
    - .planning/phases/24-visual-critic-and-re-roll/24-RESEARCH.md "Pattern 4" and "Pitfall 1"
  </read_first>
  <action>
In `server/services/ai-gateway-settings.service.ts`:

1. Widen `export type FallbackCallClass = "text" | "image" | "transcription" | "critic";`
2. Add `critic: [],` to `DEFAULT_FALLBACKS` (required — the object is typed
   `Record<FallbackCallClass, string[]>` and will not compile without it).
3. Add a comment above `FallbackCallClass` explaining the asymmetry that WILL confuse a future
   reader: `CallClass` (the GATE-07 routing union) is deliberately NOT widened with `"critic"`,
   because 24-CONTEXT.md locks the critic as OpenRouter-only with no direct-Gemini rollback path.
   `"critic"` is a fallback-CHAIN class only. Anyone adding `getCallRouting("critic")` later is
   changing a locked scope decision, not fixing an oversight.

Do NOT touch `CallClass`, `DEFAULT_ROUTING`, `getCallRouting`, or `setCallRouting`.
Do NOT touch `server/routes/admin-settings.routes.ts` — plan 24-04 owns that file's
`FALLBACK_CLASSES` widen (its current `["text","image","transcription"] as const` array is a
subset of the widened union and still compiles untouched).
  </action>
  <verify>
    <automated>npm run check && npx tsx scripts/verify-phase-21.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'FallbackCallClass = ' server/services/ai-gateway-settings.service.ts` shows the 4-member union including `"critic"`
    - `grep -n 'critic: \[\]' server/services/ai-gateway-settings.service.ts` returns 1 line
    - `grep -c 'CallClass = "planning" | "image" | "transcription"' server/services/ai-gateway-settings.service.ts` returns 1 (routing union NOT widened)
    - `npm run check` exits 0
    - `npx tsx scripts/verify-phase-21.ts` exits 0 (43/43 — GATE-04 checks unaffected)
  </acceptance_criteria>
  <done>`"critic"` is a legal fallback call class; the GATE-07 routing union is untouched and the scope fence is documented in code.</done>
</task>

</tasks>

<verification>
- `npx tsx scripts/verify-phase-24.ts --only=self-test` exits 0
- `npx tsx scripts/verify-phase-24.ts` exits 1 with only honest not-yet-written failures and zero uncaught exceptions
- `npm run check` exits 0
- Zero regression: `npx tsx scripts/verify-phase-21.ts`, `verify-phase-21.1.ts`, `verify-phase-22.ts`, `verify-phase-23.ts` all exit 0
</verification>

<success_criteria>
The Phase 24 gate harness exists with 6 tags and a non-vacuous self-test; `ai_models.critic`,
`event_kind: "visual_critic"`, and `FallbackCallClass "critic"` are all legal values; no
migration was written; no existing phase harness regressed.
</success_criteria>

<output>
After completion, create `.planning/phases/24-visual-critic-and-re-roll/24-01-SUMMARY.md`.
Record the live OpenRouter catalog re-verification result for the `ai_models.critic` default slug
verbatim (Pitfall 6) — later plans and the 24-07 runbook depend on knowing whether it was
verified or assumed.
</output>
