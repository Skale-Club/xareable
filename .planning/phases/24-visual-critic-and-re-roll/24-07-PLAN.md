---
phase: 24-visual-critic-and-re-roll
plan: 07
type: execute
wave: 5
depends_on: ["24-01", "24-02", "24-03", "24-04", "24-05", "24-06"]
files_modified:
  - scripts/verify-phase-24.ts
autonomous: false
requirements: [CRIT-01, CRIT-02, CRIT-03, CRIT-04, CRIT-05]

must_haves:
  truths:
    - "The full Phase 24 harness is green with zero weakened checks"
    - "Invariants that span files no single plan owns are asserted, not assumed"
    - "No prior phase harness regressed"
    - "An operator has confirmed the live critic call, a real re-roll, the hard-fail path, real cancellation, and the compliance query against production data"
  artifacts:
    - path: "scripts/verify-phase-24.ts"
      provides: "7th [svc-cross-plan] tag + the authoritative MANUAL/LIVE VERIFICATION RUNBOOK"
      contains: "[svc-cross-plan]"
  key_links:
    - from: "scripts/verify-phase-24.ts"
      to: "verify-phase-16/21/21.1/22/23"
      via: "spawnSync regression proof"
      pattern: "spawnSync"
    - from: "scripts/verify-phase-24.ts"
      to: "server/routes/generate.routes.ts pipeline order"
      via: "indexOf ordering assertions across critic -> crop -> typography -> logo"
      pattern: "indexOf"
---

<objective>
Close Phase 24: add the cross-plan invariant checks that no individual plan could own, embed the
authoritative live runbook, and get operator sign-off against real production traffic.

Purpose: every prior plan proved its own file. Nobody has yet proved the ORDER of the pipeline,
the absence of a bypass path around the critic, the untouched video branch, or the backward
compatibility of the six pre-existing `chatCompletion` callers. And no static check can prove the
live vision+`json_schema` combination works or that an abort really cancels a socket.
Output: a 7-tag green harness, a runbook, and a human gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-visual-critic-and-re-roll/24-CONTEXT.md
@.planning/phases/24-visual-critic-and-re-roll/24-RESEARCH.md
@.planning/phases/24-visual-critic-and-re-roll/24-VALIDATION.md

<interfaces>
scripts/verify-phase-23.ts — the `[svc-cross-plan]` precedent added by plan 23-11: 4 invariant
checks spanning files no single plan owns, including a `spawnSync`-based proof that prior phases
have not regressed, plus the embedded MANUAL/LIVE VERIFICATION RUNBOOK as a trailing block comment.
Mirror that structure exactly. `spawnSync` is already imported at the top of verify-phase-24.ts.

The 6 pre-Phase-24 `chatCompletion` call sites that must remain callClass-free:
  server/services/caption-quality.service.ts, server/services/carousel-generation.service.ts,
  server/services/enhancement.service.ts (x2), server/services/gemini.service.ts (x2)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 7th [svc-cross-plan] tag with cross-file invariants</name>
  <files>scripts/verify-phase-24.ts</files>
  <read_first>
    - scripts/verify-phase-24.ts (all of it, as built by plan 24-01)
    - scripts/verify-phase-23.ts — its `[svc-cross-plan]` block (added by 23-11) for the ordering-assertion and spawnSync-regression idioms
    - server/routes/generate.routes.ts (final state after plan 24-06)
  </read_first>
  <action>
Add a `[svc-cross-plan]` block to `scripts/verify-phase-24.ts` and add the literal
`"[svc-cross-plan]"` to the `[self-test]` required-tags array (making it 7 tags). Update the file
header comment to record that plan 24-07 added this tag.

Checks (all must be real assertions, none stubbed):

  1. PIPELINE ORDER — in `generateRouteSrc`, compute `indexOf` for `runVisualCritic(`,
     `cropToExactAspectRatio(`, `compositeTypography(`, `applyLogoOverlay(`,
     `processImageWithThumbnail(` and assert they are strictly increasing in that order. This is
     the property 24-CONTEXT.md locks ("critic scores the base image BEFORE the crop -> typography
     -> logo pipeline") and that no single plan's file-local check can see.

  2. NO BYPASS — within the image branch (slice `generateRouteSrc` from the `} else {` preceding
     `sendProgress("image_generation"` to the line containing `if (sse.isClosed())`), assert there
     is EXACTLY ONE `imageResult = ` assignment and that it reads from `attemptBuffers.get(`.
     A second assignment anywhere in that slice would mean an image can reach the pipeline without
     passing `selectFinalAttempt` — the hard-fail gate's only structural escape hatch.

  3. VIDEO FENCE (GATE-08) — slice the `content_type === "video"` branch (from
     `if (content_type === "video") {` to the `} else {` that opens the image branch) and assert it
     contains NONE of `runVisualCritic`, `controller.signal`, `criticAttempts`.

  4. BACKWARD COMPATIBILITY — read the 6 pre-Phase-24 `chatCompletion` caller files listed in
     `<interfaces>` and assert none contains the substring `callClass:` . Phase 24's `callClass`
     param is only legitimate in `visual-critic.service.ts` (and, as a default, inside
     `ai-gateway.service.ts` itself).

  5. BILLING INVARIANT, BOTH SIDES — assert (a) `server/quota.ts`'s `const pricing =` expression
     contains no `extraMetadata`, and (b) `generate.routes.ts`'s `const realCostUsdMicros =` and
     `const gatewayRealCost =` expressions contain no case-insensitive `reroll`. Together these
     prove the discarded-attempt cost cannot reach the charged amount through either layer.

  6. ZERO REGRESSION — `spawnSync("npx", ["tsx", "scripts/verify-phase-N.ts"])` for N in
     `16, 21, 21.1, 22, 23`, asserting each `status === 0`. Guard the whole block with
     `tagActive("[svc-cross-plan]")` so it only runs when the tag is selected or no filter is
     given. Include each child's last stderr line in the failure detail.

  7. CI SAFETY OF THE LIVE HARNESS — `spawnSync` `scripts/verify-critic-live.ts` with
     `OPENROUTER_API_KEY` deleted from the child env; assert `status === 0` and that stdout
     contains `SKIP`. This proves the live smoke test cannot break CI.

Do not weaken, delete, or soften any of the 41+ checks that plans 24-01..24-06 turned green. If a
check is red, fix the CODE, not the check — and if a check turns out to be genuinely wrong
(self-referential scanning of the harness's own pattern literals is the known failure mode; see
23-09's Rule-3 deviation), fix the scanner and document it as a deviation in the SUMMARY.
  </action>
  <verify>
    <automated>npx tsx scripts/verify-phase-24.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/verify-phase-24.ts --only=svc-cross-plan` exits 0 with 7 PASS lines
    - `npx tsx scripts/verify-phase-24.ts --only=self-test` exits 0 and its tag-literal check now requires 7 tags
    - `npx tsx scripts/verify-phase-24.ts` exits 0 (FULL suite green, >= 48 checks)
    - `grep -c '"\[svc-cross-plan\]"' scripts/verify-phase-24.ts` >= 1
    - no check name present in the plan-24-01 harness was removed (`git diff` shows additions only, apart from the self-test tag array and the header comment)
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>Pipeline order, the absence of a critic bypass, the frozen video branch, gateway backward compatibility, and the two-layer billing invariant are all machine-asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Embed the MANUAL/LIVE VERIFICATION RUNBOOK</name>
  <files>scripts/verify-phase-24.ts</files>
  <read_first>
    - scripts/verify-phase-23.ts (the trailing runbook block comment added by 23-11 — mirror its numbered, copy-pasteable style)
    - scripts/verify-phase-22.ts lines 304-331 (same, shorter form)
    - .planning/phases/24-visual-critic-and-re-roll/24-VALIDATION.md "Manual-Only Verifications"
    - .planning/phases/24-visual-critic-and-re-roll/24-05-SUMMARY.md and 24-06-SUMMARY.md (for the committed model slug and the charged-vs-metadata cost decision the runbook must check)
  </read_first>
  <action>
Append a trailing block comment to `scripts/verify-phase-24.ts` titled
`── MANUAL/LIVE VERIFICATION RUNBOOK (not automated — requires a funded OPENROUTER_API_KEY and the live Coolify host) ──`
with these numbered steps, each stating the exact command/query and the exact expected result:

  1. LIVE CRITIC CALL (CRIT-01).
     `OPENROUTER_API_KEY=sk-or-... npx tsx scripts/verify-critic-live.ts --image=./<a real generated PNG>`
     Expect exit 0 with all 5 assertions PASS. A `status: "unavailable"` result means the
     configured `ai_models.critic` slug does not support vision + strict `json_schema` on the
     CURRENT OpenRouter catalog (24-RESEARCH Pitfall 6) — re-check
     `https://openrouter.ai/api/v1/models?supported_parameters=structured_outputs`, pick a
     vision-capable slug, and update the model in the admin AI Models card.

  2. REAL CANCELLATION (CRIT-04).
     `OPENROUTER_API_KEY=sk-or-... npx tsx scripts/verify-critic-live.ts --image=./<png> --abort-probe`
     Expect the call to REJECT (not resolve to an "unavailable" outcome), proving the signal
     reaches the SDK's fetch rather than being a cooperative no-op.

  3. HAPPY PATH (CRIT-01, CRIT-03, CRIT-05). Generate one single-image post in staging. Then:
     `select event_kind, outcome, attempt_count, duration_ms, metadata from generation_logs where event_kind='visual_critic' order by created_at desc limit 1;`
     Expect exactly one row, `outcome='pass'`, `attempt_count=1`,
     `metadata.text_free_compliant=true`, `metadata.final_scores` populated with three integers.
     `select cost_usd_micros, charged_amount_micros, metadata from usage_events order by created_at desc limit 1;`
     Expect `metadata.reroll_attempt_count=0`, `metadata.reroll_cost_usd_micros=0`,
     `metadata.critic_outcome='pass'`.

  4. FORCED RE-ROLL (CRIT-02, CRIT-03). Generate with a prompt that reliably produces on-image
     lettering or weak composition (e.g. a vintage poster / storefront-signage concept). Expect:
     the SSE stream shows a "Regenerating for better quality (attempt 2 of 3)" progress event; the
     `visual_critic` row has `attempt_count` 2 or 3 and
     `metadata.reroll_cost_usd_micros > 0`; and CRITICALLY the matching `usage_events` row's
     `cost_usd_micros` is close to a SINGLE-attempt generation's cost, NOT 2-3x it, with the extra
     appearing only under `metadata.reroll_cost_usd_micros`. Compare against the step-3 row.

  5. HARD-FAIL PATH (CRIT-02). Force all 3 attempts to contain rendered text (temporarily point
     `ai_models.image_generation` at a text-happy model, or use an explicitly typographic prompt).
     Expect: an SSE error mentioning unwanted text; a `generation_logs` row with
     `event_kind='visual_critic'`, `outcome='hard_fail_all_attempts'`, `post_id IS NULL`,
     `status='failed'`; and NO new `posts` row and NO new `usage_events` row for that attempt
     (`select count(*) from usage_events where created_at > now() - interval '5 minutes';`).
     Restore the model setting afterward.

  6. SAFETY-TIMER CANCELLATION UNDER LOAD (CRIT-04). On staging only, set
     `GENERATION_SAFETY_TIMEOUT_MS=5000` and restart. Generate. Expect: the client receives the
     504 "Generation timed out" SSE error, the server log shows an abort/AbortError rather than a
     later-completing image response, and no `posts` row appears seconds afterward (which would
     prove the request kept running). Restore the env var and restart.

  7. COMPLIANCE RATE (CRIT-05). After at least 10 live generations:
     `select outcome, count(*) from generation_logs where event_kind='visual_critic' group by outcome;`
     Expect a sensible distribution and a computable pass rate. This is the query CRIT-05's
     "compliance rate is measurable" requirement refers to.

  8. NO-REGRESSION SWEEP. Generate one video (GATE-08 frozen path), one carousel, and one product
     enhancement. Expect all three to succeed unchanged, with NO `visual_critic` rows created for
     any of them. Also `PATCH /api/admin/ai-model-fallbacks {"call_class":"critic","chain":["gemini-2.5-flash"]}`
     returns 200 while `PATCH /api/admin/ai-gateway-routing {"call_class":"critic",...}` still
     returns 400 (the locked OpenRouter-only scope).

  If any step fails, record it in 24-07-SUMMARY.md and do NOT mark Phase 24 complete.

Also run the full regression sweep now and record the numbers in the SUMMARY:
`npx tsx scripts/verify-phase-24.ts`, `npx tsx scripts/test-critic-reroll-logic.ts`,
`npx tsx scripts/verify-phase-16.ts`, `verify-phase-21.ts`, `verify-phase-21.1.ts`,
`verify-phase-22.ts`, `verify-phase-23.ts`, `npm run check`, `npm run build`.
  </action>
  <verify>
    <automated>npx tsx scripts/verify-phase-24.ts && npx tsx scripts/test-critic-reroll-logic.ts && npm run check && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'MANUAL/LIVE VERIFICATION RUNBOOK' scripts/verify-phase-24.ts` returns 1
    - the runbook contains 8 numbered steps, each with a copy-pasteable command or SQL query
    - `npx tsx scripts/verify-phase-24.ts` exits 0 (full suite, >= 48 checks, zero weakened)
    - `npx tsx scripts/test-critic-reroll-logic.ts` exits 0
    - `verify-phase-16.ts`, `verify-phase-21.ts`, `verify-phase-21.1.ts`, `verify-phase-22.ts`, `verify-phase-23.ts` all exit 0
    - `npm run check` exits 0 and `npm run build` exits 0
  </acceptance_criteria>
  <done>The authoritative live runbook lives next to the harness it complements, and the whole static gate plus every prior phase gate is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator sign-off on the live runbook</name>
  <files>.planning/phases/24-visual-critic-and-re-roll/24-07-SUMMARY.md</files>
  <action>
STOP and hand control to the operator. Do NOT attempt to run the live runbook yourself — every
step requires the live Coolify host, the live Supabase project, and real paid OpenRouter calls.

Before pausing, print to the operator:
  1. The absolute path of the runbook: scripts/verify-phase-24.ts (trailing block comment).
  2. The static-gate result from Task 2 (full-suite check count + the five prior-phase harness results).
  3. The committed ai_models.critic default slug (from 24-01-SUMMARY.md / shared/schema.ts).
  4. The charged-vs-metadata cost decision from 24-06-SUMMARY.md, so step 3/4's SQL comparison
     has a stated expectation to check against rather than an improvised one.

After the operator responds, transcribe their per-step results verbatim into
24-07-SUMMARY.md as an 8-row table (step / expected / observed / PASS-FAIL). If any step
failed, record it and leave Phase 24 open.
  </action>
  <read_first>
    - scripts/verify-phase-24.ts (the trailing MANUAL/LIVE VERIFICATION RUNBOOK block from Task 2)
    - .planning/phases/24-visual-critic-and-re-roll/24-06-SUMMARY.md (the charged-vs-metadata cost decision to check in runbook steps 3-4)
  </read_first>
  <what-built>
    The complete Phase 24 visual critic: a `callClass: "critic"` multimodal strict-schema scoring
    call on every generated base image, a bounded 3-attempt re-roll loop with an unconditional
    hard-fail gate on unwanted rendered text and a best-of-3 fallback for soft fails, re-roll cost
    isolated to usage-event metadata, a real `AbortController` whose signal reaches the gateway's
    actual `fetch()`/SDK calls, and one `visual_critic` row per generation. Static gate:
    `npx tsx scripts/verify-phase-24.ts` green across 7 tags, plus zero regression on Phases
    16/21/21.1/22/23.
  </what-built>
  <how-to-verify>
    Execute all 8 steps of the MANUAL/LIVE VERIFICATION RUNBOOK embedded at the bottom of
    `scripts/verify-phase-24.ts`, in order, against the live Coolify host with a funded
    `OPENROUTER_API_KEY`. Steps 1-2 are single commands. Steps 3-8 require real generations plus
    the SQL queries quoted inline in the runbook.

    The three that matter most, if time is short:
      - Step 4 (forced re-roll): the usage_events row must show single-attempt cost with the extra
        in `metadata.reroll_cost_usd_micros` — this is CRIT-03's whole point.
      - Step 5 (hard-fail): no post row, no usage_events row, one `hard_fail_all_attempts` log row.
      - Step 6 (timer cancellation): the aborted request must actually stop, not finish later.
  </how-to-verify>
  <acceptance_criteria>
    - Operator has run all 8 runbook steps and recorded the observed result for each in 24-07-SUMMARY.md
    - Steps 4, 5 and 6 explicitly PASS (or a deviation is recorded with the raw evidence)
    - The operator states the resulting compliance-rate distribution from step 7
  </acceptance_criteria>
  <resume-signal>Type "approved" to close Phase 24, or paste the failing step number plus its observed output.</resume-signal>
  <done>A human has confirmed against real traffic what no static check can prove: the live vision+json_schema call works, a re-roll bills once, the hard-fail path fails cleanly, and an abort really cancels.</done>
</task>

</tasks>

<verification>
- `npx tsx scripts/verify-phase-24.ts` exits 0 across all 7 tags with zero weakened checks
- `npx tsx scripts/test-critic-reroll-logic.ts` exits 0
- `npm run check` and `npm run build` both exit 0
- `verify-phase-16.ts`, `verify-phase-21.ts`, `verify-phase-21.1.ts`, `verify-phase-22.ts`, `verify-phase-23.ts` all exit 0
- Operator sign-off recorded for all 8 runbook steps
</verification>

<success_criteria>
Phase 24's static gate is fully green including cross-file invariants, no prior phase regressed,
the live runbook is embedded and authoritative, and an operator has verified the live critic call,
a real re-roll's billing, the hard-fail path, real cancellation, and the compliance query.
</success_criteria>

<output>
After completion, create `.planning/phases/24-visual-critic-and-re-roll/24-07-SUMMARY.md` with the
per-step runbook results table and the final check counts. Phase 24 is NOT complete until Task 3
is signed off.
</output>
