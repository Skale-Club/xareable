---
phase: 24-visual-critic-and-re-roll
plan: 04
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - server/routes/admin-settings.routes.ts
  - client/src/components/admin/post-creation/ai-models-card.tsx
  - client/src/lib/translations/pt.ts
  - client/src/lib/translations/es.ts
autonomous: true
requirements: [CRIT-01]

must_haves:
  truths:
    - "An admin can change the visual-critic model slug from the AI Models card without a deploy"
    - "An admin can set a critic-specific fallback chain via PATCH /api/admin/ai-model-fallbacks"
    - "The critic selector reads correctly in pt-BR and es"
  artifacts:
    - path: "server/routes/admin-settings.routes.ts"
      provides: "FALLBACK_CLASSES includes 'critic'"
      contains: "critic"
    - path: "client/src/components/admin/post-creation/ai-models-card.tsx"
      provides: "Visual Critic model selector bound to ai_models.critic"
      contains: "updateModel(\"critic\""
  key_links:
    - from: "ai-models-card.tsx Select"
      to: "style_catalog.ai_models.critic"
      via: "updateModel('critic', value) -> setCatalog"
      pattern: "updateModel\\(\"critic\""
    - from: "PATCH /api/admin/ai-model-fallbacks"
      to: "setFallbackChain('critic', chain)"
      via: "FALLBACK_CLASSES membership check"
      pattern: "\"critic\""
---

<objective>
Expose the new critic call class on the two admin surfaces that already exist for every other
call class — the AI Models model-slug selector and the fallback-chain endpoint — so GATE-04's
"no hardcoded slugs, admin-configurable per call class" promise is true for `critic` too.

Purpose: without this, `ai_models.critic` is only changeable by editing the `style_catalog` JSON
by hand, and `PATCH /api/admin/ai-model-fallbacks` would 400 on `call_class: "critic"` even
though `setFallbackChain` now accepts it.
Output: one array widen, one selector, two translation entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-visual-critic-and-re-roll/24-CONTEXT.md
@.planning/phases/24-visual-critic-and-re-roll/24-RESEARCH.md

<interfaces>
<!-- Verbatim current contracts. -->

server/routes/admin-settings.routes.ts lines 57-58 and 100-118:
```typescript
const CALL_CLASSES: CallClass[] = ["planning", "image", "transcription"];
const FALLBACK_CLASSES = ["text", "image", "transcription"] as const;
...
router.patch("/api/admin/ai-model-fallbacks", async (req, res) => {
    ...
    if (!(FALLBACK_CLASSES as readonly string[]).includes(call_class as string) || !Array.isArray(chain) || ...) {
        return res.status(400).json({ message: "call_class must be text|image|transcription and chain must be string[]" });
    }
    await setFallbackChain(call_class as (typeof FALLBACK_CLASSES)[number], chain as string[]);
```

client/src/components/admin/post-creation/ai-models-card.tsx lines 17-25 (the local fallback
object that must gain `critic`) and lines 50-69 (the `planning` block that is the exact template
for the new selector). CardContent grid is currently `sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5`
with 5 children.

client/src/lib/translations/pt.ts:468 and client/src/lib/translations/es.ts:405 — the two
"Planning (Art Director)" entries, showing exactly where sibling entries live.

shared/schema.ts (widened by plan 24-01, available now): `aiModelsSchema.critic` with its default.
server/services/ai-gateway-settings.service.ts (widened by 24-01): FallbackCallClass includes "critic".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Accept call_class "critic" on the fallback-chain admin endpoints</name>
  <files>server/routes/admin-settings.routes.ts</files>
  <read_first>
    - server/routes/admin-settings.routes.ts lines 55-118 (CALL_CLASSES, FALLBACK_CLASSES, both gateway endpoints)
    - server/services/ai-gateway-settings.service.ts (as widened by plan 24-01)
  </read_first>
  <action>
In `server/routes/admin-settings.routes.ts`:

1. Widen `const FALLBACK_CLASSES = ["text", "image", "transcription", "critic"] as const;` and add
   a comment: Phase 24 (CRIT-01) — the critic gets its own fallback chain so a critic-model
   deprecation cannot be masked by, or leak into, the shared text chain.
2. Update the PATCH validation error message to
   `"call_class must be text|image|transcription|critic and chain must be string[]"`.
3. Leave `CALL_CLASSES` (the GATE-07 routing union) at three members. Add a comment stating that
   `critic` is deliberately absent here: 24-CONTEXT.md locks the critic as OpenRouter-only with no
   direct-Gemini rollback branch, so `GET/PATCH /api/admin/ai-gateway-routing` must keep rejecting
   `call_class: "critic"`.

The GET endpoint needs no change beyond the widened array (it already loops `for (const cc of FALLBACK_CLASSES)`),
but read it to confirm rather than assume.
  </action>
  <verify>
    <automated>npm run check && npx tsx scripts/verify-phase-21.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'FALLBACK_CLASSES = \["text", "image", "transcription", "critic"\] as const' server/routes/admin-settings.routes.ts` returns 1 line
    - `grep -n 'CALL_CLASSES: CallClass\[\] = \["planning", "image", "transcription"\]' server/routes/admin-settings.routes.ts` returns 1 line (routing union NOT widened)
    - `grep -n 'text|image|transcription|critic' server/routes/admin-settings.routes.ts` returns 1 line
    - `npm run check` exits 0
    - `npx tsx scripts/verify-phase-21.ts` exits 0 (43/43)
  </acceptance_criteria>
  <done>`PATCH /api/admin/ai-model-fallbacks {call_class:"critic"}` is accepted; the routing endpoint still rejects it, by design.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Visual Critic model selector + pt/es translations</name>
  <files>client/src/components/admin/post-creation/ai-models-card.tsx, client/src/lib/translations/pt.ts, client/src/lib/translations/es.ts</files>
  <read_first>
    - client/src/components/admin/post-creation/ai-models-card.tsx (all 149 lines)
    - client/src/lib/translations/pt.ts lines 460-475
    - client/src/lib/translations/es.ts lines 400-412
    - shared/schema.ts aiModelsSchema (for the exact `critic` default slug plan 24-01 committed — use THAT slug, not an assumed one)
  </read_first>
  <action>
1. `client/src/components/admin/post-creation/ai-models-card.tsx`:
   - Add `critic: "<the exact default slug from shared/schema.ts aiModelsSchema.critic>",` to the
     local `aiModels` fallback object (lines 17-25), with a short comment: Phase 24 (CRIT-01),
     admin-configurable via the "Visual Critic" selector below.
   - Insert a new `<div className="space-y-2">` block immediately AFTER the "Planning (Art
     Director)" block, copying that block's structure exactly (Label / Select bound to
     `aiModels.critic` with `onValueChange={(value) => updateModel("critic", value)}` /
     SelectTrigger / SelectContent / helper `<p>`):
       Label: `{t("Visual Critic")}`
       SelectItems: `gemini-2.5-flash` -> "Gemini 2.5 Flash", `gemini-2.5-pro` -> "Gemini 2.5 Pro",
                    `gemini-3.1-flash` -> "Gemini 3.1 Flash"
       Helper text (one string, used verbatim as the translation key):
       `{t("Scores every generated image before compositing (composition, color harmony, text-free compliance). Must support BOTH vision input and OpenRouter structured outputs — an unsupported model disables the quality gate.")}`
   - Change the CardContent grid class from `xl:grid-cols-5` to `xl:grid-cols-6` so all six
     selectors stay on one row at xl.
   - `updateModel`'s `field: keyof AIModels` signature already accepts `"critic"` once
     `aiModelsSchema` has the field — no type change needed. Confirm by reading, not assuming.

2. `client/src/lib/translations/pt.ts` — add both new keys next to the existing
   "Planning (Art Director)" entry:
   - `"Visual Critic": "Crítico Visual",`
   - `"Scores every generated image before compositing (composition, color harmony, text-free compliance). Must support BOTH vision input and OpenRouter structured outputs — an unsupported model disables the quality gate.": "Avalia cada imagem gerada antes da composição (composição, harmonia de cores, ausência de texto). Tem de suportar entrada de imagem E structured outputs no OpenRouter — um modelo sem suporte desativa o controlo de qualidade.",`

3. `client/src/lib/translations/es.ts` — same two keys:
   - `"Visual Critic": "Crítico Visual",`
   - `"Scores every generated image before compositing (composition, color harmony, text-free compliance). Must support BOTH vision input and OpenRouter structured outputs — an unsupported model disables the quality gate.": "Evalúa cada imagen generada antes de la composición (composición, armonía de color, ausencia de texto). Debe admitir entrada de imagen Y structured outputs en OpenRouter — un modelo sin soporte desactiva el control de calidad.",`

The English helper string must be byte-identical in all three files (it IS the lookup key).
No emojis anywhere.
  </action>
  <verify>
    <automated>npm run check && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'updateModel("critic"' client/src/components/admin/post-creation/ai-models-card.tsx` returns 1 line
    - `grep -n 'aiModels.critic' client/src/components/admin/post-creation/ai-models-card.tsx` returns 1 line
    - `grep -c '"Visual Critic"' client/src/lib/translations/pt.ts client/src/lib/translations/es.ts` returns 1 for each file
    - the long helper-text key appears exactly once in each of the 3 files and is byte-identical across them
    - `grep -n 'xl:grid-cols-6' client/src/components/admin/post-creation/ai-models-card.tsx` returns 1 line
    - `npm run check` exits 0 and `npm run build` exits 0
  </acceptance_criteria>
  <done>An admin can pick the critic model in the AI Models card in all three locales, with the model-capability warning stated in the helper text.</done>
</task>

</tasks>

<verification>
- `npm run check` exits 0, `npm run build` exits 0
- `npx tsx scripts/verify-phase-21.ts` exits 0 (43/43 — GATE-04 admin surface unaffected)
- `npx tsx scripts/verify-phase-24.ts --only=svc-critic-call` check 9's `aiModelsSchema.critic` half is PASS
</verification>

<success_criteria>
The critic is admin-configurable on both surfaces every other call class already has (model slug
+ fallback chain), in three locales, and the GATE-07 routing endpoint still correctly rejects
`critic` per the locked OpenRouter-only scope.
</success_criteria>

<output>
After completion, create `.planning/phases/24-visual-critic-and-re-roll/24-04-SUMMARY.md`.
</output>
