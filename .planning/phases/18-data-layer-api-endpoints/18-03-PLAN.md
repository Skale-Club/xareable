---
phase: 18-data-layer-api-endpoints
plan: 03
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified:
  - scripts/verify-phase-18.ts
autonomous: true
requirements:
  - REF-01
  - API-01
  - API-02
  - API-03
  - API-04

must_haves:
  truths:
    - "scripts/verify-phase-18.ts exists and runs without throwing"
    - "verify-phase-18.ts exits 0 when all 15 static assertions pass"
    - "verify-phase-18.ts exits non-zero with descriptive FAIL lines when any assertion fails"
    - "npm run check passes with zero TypeScript errors"
  artifacts:
    - path: "scripts/verify-phase-18.ts"
      provides: "Static verification harness for Phase 18 REF-01 + API-01 through API-04"
      contains: "15 check() calls"
  key_links:
    - from: "scripts/verify-phase-18.ts"
      to: "supabase/migrations/20260516000000_brand_style_references.sql"
      via: "fs.existsSync + readFileSync + string includes"
      pattern: "20260516000000_brand_style_references"
    - from: "scripts/verify-phase-18.ts"
      to: "shared/schema.ts"
      via: "readFileSync + includes checks for 4 export names"
      pattern: "brandReferencePhotoSchema"
    - from: "scripts/verify-phase-18.ts"
      to: "server/routes/brand-references.routes.ts"
      via: "fs.existsSync + readFileSync + endpoint string checks"
      pattern: "brand-references.routes.ts"
---

<objective>
Create scripts/verify-phase-18.ts — a static file-existence and string-search harness that confirms all 15 Phase 18 deliverables are in place. Then run npm run check to confirm TypeScript is clean across the whole project.

Purpose: Provides a single repeatable command (npx tsx scripts/verify-phase-18.ts) to validate the complete Phase 18 contract — migration, Zod schemas, route file, all 4 endpoint declarations, and route registration — without requiring a live Supabase connection.

Output: scripts/verify-phase-18.ts with 15 assertions. Exits 0 on success, non-zero with FAIL lines on any failure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-data-layer-api-endpoints/18-CONTEXT.md
@.planning/phases/18-data-layer-api-endpoints/18-RESEARCH.md
@.planning/phases/18-data-layer-api-endpoints/18-01-SUMMARY.md

<interfaces>
<!-- verify script structure — from scripts/verify-phase-17.ts (same pattern) -->
From scripts/verify-phase-17.ts:
```typescript
import { readFileSync, existsSync } from "node:fs";
import { resolve } from "node:path";

const ROOT = process.cwd();
let failed = 0;
const results: string[] = [];

function check(label: string, condition: boolean, hint?: string): void {
  if (condition) {
    results.push(`  ok  ${label}`);
  } else {
    failed++;
    results.push(`  FAIL ${label}${hint ? `\n       hint: ${hint}` : ""}`);
  }
}

function read(path: string): string {
  const p = resolve(ROOT, path);
  return existsSync(p) ? readFileSync(p, "utf8") : "";
}

// ... checks ...

console.log("\n=== Phase 17 Verification ===");
for (const line of results) console.log(line);
if (failed > 0) {
  console.error(`\n${failed} check(s) failed.`);
  process.exit(1);
}
console.log("\nAll Phase 17 checks passed.");
```

<!-- The 15 assertions from RESEARCH.md "Verification Script Blueprint" section -->
1. Migration file 20260516000000_brand_style_references.sql exists
2. Migration contains brand_reference_photos table creation SQL
3. Migration contains ALTER TABLE public.brands ADD COLUMN IF NOT EXISTS style_description
4. Migration contains ENABLE ROW LEVEL SECURITY for brand_reference_photos
5. shared/schema.ts exports brandReferencePhotoSchema
6. shared/schema.ts exports brandReferencePhotosResponseSchema
7. shared/schema.ts exports createBrandReferencePhotoSchema
8. shared/schema.ts exports updateStyleDescriptionSchema
9. server/routes/brand-references.routes.ts exists
10. brand-references.routes.ts declares GET /api/brand/reference-photos
11. brand-references.routes.ts declares POST /api/brand/reference-photos
12. brand-references.routes.ts declares DELETE /api/brand/reference-photos/:id
13. brand-references.routes.ts declares PATCH /api/brand/style-description
14. server/routes/index.ts imports brand-references.routes.js
15. server/routes/index.ts calls router.use(brandReferencesRoutes)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/verify-phase-18.ts with 15 static assertions</name>
  <files>scripts/verify-phase-18.ts</files>
  <read_first>
    - scripts/verify-phase-17.ts (full file — use identical structure: check(), read(), results[], failed, process.exit(1))
    - 18-RESEARCH.md "Verification Script Blueprint" section (lists all 15 assertions verbatim)
  </read_first>
  <action>
Create scripts/verify-phase-18.ts with the following content. Use the same check()/read()/results[]/failed pattern as verify-phase-17.ts — no new patterns.

```typescript
/**
 * Phase 18 Verification Script (v1.5 — Brand Style References: Data Layer + API Endpoints)
 *
 * Statically verifies that the Phase 18 contract is in place:
 *   REF-01: brand_reference_photos table + brands.style_description column + 4 Zod schemas
 *   API-01: GET /api/brand/reference-photos declared in brand-references.routes.ts
 *   API-02: POST /api/brand/reference-photos declared in brand-references.routes.ts
 *   API-03: DELETE /api/brand/reference-photos/:id declared in brand-references.routes.ts
 *   API-04: PATCH /api/brand/style-description declared in brand-references.routes.ts
 *
 * All checks are static (file existence + string search). No live Supabase connection required.
 *
 * Run with: npx tsx scripts/verify-phase-18.ts
 * Exits non-zero if any check fails.
 */

import { readFileSync, existsSync } from "node:fs";
import { resolve } from "node:path";

const ROOT = process.cwd();
let failed = 0;
const results: string[] = [];

function check(label: string, condition: boolean, hint?: string): void {
  if (condition) {
    results.push(`  ok  ${label}`);
  } else {
    failed++;
    results.push(`  FAIL ${label}${hint ? `\n       hint: ${hint}` : ""}`);
  }
}

function read(path: string): string {
  const p = resolve(ROOT, path);
  return existsSync(p) ? readFileSync(p, "utf8") : "";
}

// ── Section 1: Migration (REF-01) ────────────────────────────────────────────

console.log("\nSection 1: Migration — supabase/migrations/20260516000000_brand_style_references.sql");
const migPath = "supabase/migrations/20260516000000_brand_style_references.sql";
const migExists = existsSync(resolve(ROOT, migPath));
const migration = read(migPath);

check(
  "migration file 20260516000000_brand_style_references.sql exists",
  migExists,
  `expected at ${migPath}`,
);

check(
  "migration contains CREATE TABLE IF NOT EXISTS public.brand_reference_photos",
  migration.includes("CREATE TABLE IF NOT EXISTS public.brand_reference_photos"),
);

check(
  "migration contains ALTER TABLE public.brands ADD COLUMN IF NOT EXISTS style_description",
  migration.includes("ADD COLUMN IF NOT EXISTS style_description"),
);

check(
  "migration enables RLS on brand_reference_photos",
  migration.includes("ALTER TABLE public.brand_reference_photos ENABLE ROW LEVEL SECURITY"),
);

// ── Section 2: Zod schemas (REF-01) ─────────────────────────────────────────

console.log("\nSection 2: Zod schemas — shared/schema.ts");
const schema = read("shared/schema.ts");

check(
  "shared/schema.ts exports brandReferencePhotoSchema",
  schema.includes("export const brandReferencePhotoSchema"),
);

check(
  "shared/schema.ts exports brandReferencePhotosResponseSchema",
  schema.includes("export const brandReferencePhotosResponseSchema"),
);

check(
  "shared/schema.ts exports createBrandReferencePhotoSchema",
  schema.includes("export const createBrandReferencePhotoSchema"),
);

check(
  "shared/schema.ts exports updateStyleDescriptionSchema",
  schema.includes("export const updateStyleDescriptionSchema"),
);

// ── Section 3: Route file existence and endpoint declarations (API-01 to API-04) ──

console.log("\nSection 3: Route file — server/routes/brand-references.routes.ts");
const routePath = "server/routes/brand-references.routes.ts";
const routeExists = existsSync(resolve(ROOT, routePath));
const route = read(routePath);

check(
  "server/routes/brand-references.routes.ts exists",
  routeExists,
  `expected at ${routePath}`,
);

check(
  "route file declares GET /api/brand/reference-photos (API-01)",
  route.includes('"/api/brand/reference-photos"') && route.includes("router.get"),
  "expected: router.get(\"/api/brand/reference-photos\", ...)",
);

check(
  "route file declares POST /api/brand/reference-photos (API-02)",
  route.includes('"/api/brand/reference-photos"') && route.includes("router.post"),
  "expected: router.post(\"/api/brand/reference-photos\", ...)",
);

check(
  "route file declares DELETE /api/brand/reference-photos/:id (API-03)",
  route.includes('"/api/brand/reference-photos/:id"') && route.includes("router.delete"),
  "expected: router.delete(\"/api/brand/reference-photos/:id\", ...)",
);

check(
  "route file declares PATCH /api/brand/style-description (API-04)",
  route.includes('"/api/brand/style-description"') && route.includes("router.patch"),
  "expected: router.patch(\"/api/brand/style-description\", ...)",
);

// ── Section 4: Route registration (index.ts) ─────────────────────────────────

console.log("\nSection 4: Route registration — server/routes/index.ts");
const index = read("server/routes/index.ts");

check(
  "server/routes/index.ts imports brand-references.routes.js",
  index.includes("brand-references.routes.js"),
  "expected: import brandReferencesRoutes from \"./brand-references.routes.js\"",
);

check(
  "server/routes/index.ts calls router.use(brandReferencesRoutes)",
  index.includes("router.use(brandReferencesRoutes)"),
  "expected: router.use(brandReferencesRoutes) inside createApiRouter()",
);

// ── Summary ──────────────────────────────────────────────────────────────────

console.log("\n=== Phase 18 Verification ===");
for (const line of results) console.log(line);
if (failed > 0) {
  console.error(`\n${failed} check(s) failed.`);
  process.exit(1);
}
console.log("\nAll Phase 18 checks passed.");
```

Notes on the assertion choices:
- Section 1 check 2 uses `.includes("CREATE TABLE IF NOT EXISTS public.brand_reference_photos")` — matches the exact string from the migration.
- Section 3 endpoint checks combine a path string AND a method string — avoids false positives from comments containing the path.
- The file uses only Node built-ins (fs, path) — no external imports, no Supabase calls. Runs in CI without env vars.
  </action>
  <verify>
    <automated>npx tsx scripts/verify-phase-18.ts</automated>
  </verify>
  <done>scripts/verify-phase-18.ts exits 0 with "All Phase 18 checks passed." output when Plans 18-01 and 18-02 are complete. If either plan is incomplete, the script exits non-zero and prints FAIL lines identifying which assertions failed.</done>
</task>

<task type="auto">
  <name>Task 2: Run npm run check — TypeScript must compile clean</name>
  <files></files>
  <read_first>
    - (none — this task runs a command and inspects output only)
  </read_first>
  <action>
Run npm run check (the project's TypeScript type check command, defined in package.json as "tsx script/build.ts" or "tsc --noEmit" — check package.json to confirm the exact command).

If npm run check reports errors:
1. Read each error location carefully.
2. Fix the error in the appropriate file (most likely shared/schema.ts or server/routes/brand-references.routes.ts from Plans 18-01/18-02).
3. Do NOT fix errors by adding @ts-ignore or loosening types.
4. Common errors to expect and how to fix:
   - "Property 'style_description' does not exist on type 'Brand'" — brandSchema was not updated in Plan 18-01; add the field.
   - "Module not found: brand-references.routes.js" — import path wrong in index.ts; check .js extension.
   - "Argument of type X is not assignable to..." — schema type mismatch; compare Zod schema definition to the route's usage.

Re-run npm run check after each fix until it exits 0.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>npm run check exits 0 with no TypeScript errors. The full project compiles cleanly with all Phase 18 additions in place.</done>
</task>

</tasks>

<verification>
- npx tsx scripts/verify-phase-18.ts exits 0 with "All Phase 18 checks passed." (all 15 assertions green).
- npm run check exits 0 with no errors.
- scripts/verify-phase-18.ts contains exactly 15 check() calls (count by grepping "check(").
</verification>

<success_criteria>
Phase 18 verification is complete when:
1. npx tsx scripts/verify-phase-18.ts exits 0 with all 15 checks passing.
2. npm run check exits 0 (no TypeScript errors across the project).
Both commands run without a live Supabase connection.
</success_criteria>

<output>
After completion, create .planning/phases/18-data-layer-api-endpoints/18-03-SUMMARY.md
</output>
