---
phase: 26-fixes-and-polish
plan: 10
type: execute
wave: 6
depends_on: ["26-01", "26-02", "26-03", "26-04", "26-05", "26-06", "26-07", "26-08", "26-09"]
files_modified:
  - scripts/verify-phase-26.ts
autonomous: false
requirements: [POL-02, POL-03, POL-06, POL-08, POL-09]

must_haves:
  truths:
    - "The full Phase 26 harness is green with zero weakened checks"
    - "Invariants that span files no single plan owns are asserted, not assumed"
    - "Every Phase 26 unit harness still passes, proven by subprocess"
    - "No prior phase harness and no CI-wired gate regressed"
    - "An operator has confirmed real idempotent duplicate requests, a real adaptive logo treatment, real feedback persistence, and the real admin Quality dashboard"
  artifacts:
    - path: "scripts/verify-phase-26.ts"
      provides: "9th [svc-cross-plan] tag + the authoritative MANUAL/LIVE VERIFICATION RUNBOOK"
      contains: "[svc-cross-plan]"
  key_links:
    - from: "scripts/verify-phase-26.ts"
      to: "verify-phase-21/21.1/22/23/24/25 + verify-golden-image + the 4 Phase 26 unit harnesses"
      via: "spawnSync non-regression sweep"
      pattern: "spawnSync"
    - from: "scripts/verify-phase-26.ts"
      to: "server/routes/generate.routes.ts + server/routes/edit.routes.ts"
      via: "indexOf ordering assertions across idempotency, credit gate, and logo overlay"
      pattern: "indexOf"
---

<objective>
Close Phase 26 and the v1.6 milestone: add the cross-plan invariants no individual plan could own, embed the authoritative live runbook, and get operator sign-off against real production traffic.

Purpose: plans 26-01..26-09 each proved their own files. Nobody has yet proved that the idempotency pre-flight still sits before the credit gate AFTER the logo-overlay plan re-edited the same two routes; that the WebP quality bump and the drawBlocks font fix compose without either silently undoing the other's guarantees; that the feedback column the dashboard aggregates is the same column the viewer writes; or that no prior phase regressed. And no static check can prove that two genuinely duplicate live requests produce one post and one charge, or that a JPEG logo on a busy photo actually looks right.
Output: a 9-tag green harness, a 7-step runbook, and a human gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-fixes-and-polish/26-CONTEXT.md
@.planning/phases/26-fixes-and-polish/26-VALIDATION.md

<interfaces>
<!-- The precedent to replicate, and the exact known collision classes to avoid. -->

Closing-plan precedent — `scripts/verify-phase-25.ts`'s `[svc-cross-plan]` tag (added by 25-14) and `scripts/verify-phase-24.ts`'s (added by 24-07). Both follow the same recipe:
1. Ordering/parity assertions spanning two or more plans' files, using `src.indexOf(marker)` comparisons.
2. A "no coverage gap" property: assert that feature A being wired AND feature B being removed are BOTH true, so no intermediate state ships.
3. A `spawnSync` sweep of every unit harness this phase created.
4. A `spawnSync` sweep of every prior-phase gate + the CI-wired `verify-golden-image.ts`.
5. A trailing block comment containing the single authoritative MANUAL/LIVE VERIFICATION RUNBOOK.

```typescript
const run = spawnSync("npx", ["tsx", scriptPath], { encoding: "utf8", shell: process.platform === "win32" });
const lastLine = (run.stdout || "").trim().split("\n").filter(Boolean).pop() || "";
check(`[svc-cross-plan] ${scriptPath} exits 0`, run.status === 0, run.status !== 0 ? lastLine : "");
```

KNOWN COLLISION CLASSES this harness family has hit three times (23-09, 24-07, 25-14) — avoid all three:
- **indexOf substring collision**: an earlier, unrelated occurrence of a marker string shadows the real one. Fix by searching FORWARD from a preceding unique anchor, not from index 0.
- **Self-referential scanner**: the harness's own pattern-literal array trips its own scan. Fix by excluding the harness's own source, or by counting mentions vs. negated mentions.
- **Header-comment collision**: a phrase used in the harness's own header breaks a `grep-count === 1` acceptance criterion. Check counts before committing.

Phase 26 unit harnesses to sweep (all created this phase):
- `scripts/verify-webp-text-edge.ts` (26-02)
- `scripts/test-drawblocks-font-state.ts` (26-03)
- `scripts/test-logo-overlay-contrast.ts` (26-07)
- `scripts/reconcile-openrouter-costs.ts` (26-05 — exits 0 with no credentials; sweeping it proves it is CI-safe)

Prior gates to sweep: `verify-phase-21.ts`, `verify-phase-21.1.ts`, `verify-phase-22.ts`, `verify-phase-23.ts`, `verify-phase-24.ts`, `verify-phase-25.ts`, `verify-golden-image.ts` (the CI-wired one), `verify-phase-12.6.ts`, `test-typography-treatment.ts`.

This plan is the ONLY plan permitted to edit `scripts/verify-phase-26.ts` after 26-01.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [svc-cross-plan] — the invariants no single plan owns</name>
  <files>scripts/verify-phase-26.ts</files>
  <read_first>
    - scripts/verify-phase-25.ts's `[svc-cross-plan]` tag block and `checkZeroRegression()` (the exact recipe)
    - scripts/verify-phase-24.ts's `[svc-cross-plan]` tag block (the second precedent, incl. the indexOf-forward-search fix)
    - server/routes/generate.routes.ts and server/routes/edit.routes.ts (FULL, post-26-06 and post-26-07 — these two files were edited by two different plans in two different waves; this task's job is to prove they still compose)
    - server/services/image-optimization.service.ts (post-26-02 and post-26-07 — same two-plan situation)
    - The SUMMARY.md of every plan 26-02..26-09
  </read_first>
  <action>
Add a 9th tag block, `[svc-cross-plan]`, to `scripts/verify-phase-26.ts` as a PURE ADDITION — do not modify, reorder, or weaken any existing check. If a pre-existing check is now red, that is a real defect in the plan that owned it: report it and stop, do not adjust the check.

Seven invariant groups:

1. **Idempotency survived the logo-overlay re-edit (26-06 x 26-07).** Both routes were edited by two plans in two waves. Assert in BOTH `generate.routes.ts` and `edit.routes.ts` that `indexOf('.eq("idempotency_key"') > -1` AND `< indexOf("checkCredits(")` AND `< indexOf("applyLogoOverlay(")`. Ordering, not mere presence — a duplicate must be rejected before any money is committed and long before any image work happens.

2. **Both image-optimization changes coexist (26-02 x 26-07).** Assert `image-optimization.service.ts` contains `const DEFAULT_IMAGE_QUALITY = 85` AND `applyLogoOverlayDetailed` AND `analyzeRegionContrast` AND that `quality: 70` is still present. 26-07 rewrote a large block of the same file 26-02 touched; this is the guard against one plan's diff eating the other's.

3. **No coverage gap in the logo path.** Assert the conjunction: `applyLogoOverlay` no longer declares a `"bottom-right"` parameter default AND neither route contains `|| "bottom-right"` or `?? "bottom-right"` adjacent to a `logo_position` reference. Half of this fix shipping (a service that can auto-select, behind routes that never let it) would be an invisible no-op.

4. **The feedback write path and the read path address the same column.** Assert `posts.routes.ts`'s feedback handler updates `feedback`, `admin-quality.routes.ts` selects `feedback` from `posts`, `post-viewer-dialog.tsx` PATCHes `/feedback`, and the `_posts_feedback.sql` migration adds that column — a four-file chain owned by two plans.

5. **The compositor contract is unchanged by the font fix (26-03).** Assert `typography-compositor.service.ts` still declares `COMPOSITOR_VERSION = 1` and still contains the zero-text-blocks pass-through early return; and that `shared/schema.ts`'s `typographyMetaSchema` is unchanged in shape (still lists `compositor_version`, `layout_archetype_id`, `text_blocks`, `text_color`, `fonts`, `scrim`, `safe_zone`, `canvas`). The fix changed rasterization, not the persisted contract — if the contract moved, every Phase 23/25 persisted row is suspect.

6. **POL-08 is set up but genuinely not scheduled.** Assert `docs/cost-reconciliation-runbook.md` exists, `scripts/reconcile-openrouter-costs.ts` exists, AND neither `server/services/cleanup-cron.service.ts` nor any file under `.github/workflows/` mentions `reconcile`. Use a mention-vs-negated-mention count, not bare presence, to avoid the self-referential-scanner class of false positive.

7. **Subprocess sweeps.** Inside `if (tagActive("svc-cross-plan"))`, spawnSync each of the four Phase 26 unit harnesses and each of the nine prior gates listed in `<interfaces>`, asserting `status === 0` and surfacing the last stdout line as the failure detail. Skip (with an explicit `check(..., false, "missing")`) any script that does not exist rather than letting `npx` produce an opaque error.

Then run the FULL suite and confirm every tag is green with zero weakened checks. Report the final PASS count in the summary.
  </action>
  <acceptance_criteria>
    - `npx tsx scripts/verify-phase-26.ts` exits 0 with every one of the 9 tags represented in the PASS list
    - `npx tsx scripts/verify-phase-26.ts --only=svc-cross-plan` exits 0
    - `npx tsx scripts/verify-phase-26.ts --only=self-test` still exits 0
    - `grep -c "spawnSync" scripts/verify-phase-26.ts` returns at least 2
    - `git diff scripts/verify-phase-26.ts` shows only ADDED lines in the check region (no existing check text modified or deleted)
    - `npm run check` and `npm run build` exit 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/verify-phase-26.ts &amp;&amp; npm run check</automated>
  </verify>
  <done>The full 9-tag suite is green, cross-plan composition is asserted rather than assumed, and every prior gate is swept by subprocess.</done>
</task>

<task type="auto">
  <name>Task 2: Embed the MANUAL/LIVE VERIFICATION RUNBOOK</name>
  <files>scripts/verify-phase-26.ts</files>
  <read_first>
    - scripts/verify-phase-25.ts's trailing runbook block (the exact format: a `// ── MANUAL/LIVE VERIFICATION RUNBOOK ──` header, numbered steps, real SQL, real expected values, and a closing "If any step fails, record it in NN-SUMMARY.md and do NOT mark Phase NN complete")
    - .planning/phases/26-fixes-and-polish/26-VALIDATION.md's "Manual-Only Verifications" table (the four behaviors that MUST appear as runbook steps)
    - docs/cost-reconciliation-runbook.md (26-05's output — the runbook must POINT to it, not restate it)
  </read_first>
  <action>
Append the authoritative runbook as a trailing block comment at the very bottom of `scripts/verify-phase-26.ts`, below `main().catch(...)`, as a PURE ADDITION. This is the single source for Phase 26 operator sign-off — do not duplicate its content into any other file; reference it.

Seven steps, each with the exact command/SQL and the exact expected result:

1. **LIVE IDEMPOTENCY — GENERATE (POL-06).** Fire two identical `POST /api/generate` requests with the SAME `idempotency_key` (the second after the first completes). Expect the second to return HTTP 200 with `{"idempotent": true, ...}` and NO SSE stream. Then:
   `select count(*) from posts where idempotency_key = '<key>';` -> exactly 1
   `select count(*) from usage_events where post_id = '<post id>';` -> exactly 1
   A second post row or a second usage event means the pre-flight is running after the credit gate.

2. **LIVE IDEMPOTENCY — EDIT (POL-06).** Same with `POST /api/edit-post` on an existing post. Expect `{"idempotent": true, "version": {...}}`. Then:
   `select count(*) from post_versions where idempotency_key = '<key>';` -> exactly 1
   Also confirm `version_number` did not advance. Note explicitly in the runbook that a TRUE concurrent race (two simultaneous in-flight requests) still surfaces as a generic 500 — this matches the shipped carousel/enhance contract and is intentional, not a regression to report.

3. **ADAPTIVE LOGO — JPEG, NO ALPHA (POL-03).** Upload a JPEG logo with a solid non-white background to a brand, then generate a post with `use_logo: true` and NO `logo_position`. Visually confirm: no hard opaque rectangle, a soft feathered plate behind the mark, and the logo placed in the cleanest corner rather than always bottom-right. Then repeat with `logo_position: "bottom-right"` explicitly set over a deliberately busy background and confirm the logo STAYS bottom-right (contrast treatment adapts; position does not).

4. **WEBP QUALITY + TEXT EDGES (POL-02).** Generate a post with a 3-block text layout. Download the resulting `.webp` and confirm at 200% zoom that glyph edges are crisp with no ringing/mosquito artifacts. In the same image confirm the drawBlocks fix visually: the headline must be plainly larger than the CTA line. Then check storage impact is acceptable: `select avg(octet_length(...))`-equivalent by comparing a few pre- and post-deploy file sizes in the Supabase Storage browser — a ~10-20% increase is expected and fine.

5. **FEEDBACK ROUND TRIP (POL-09).** As a normal user, open a post in the viewer, thumbs-up it, close and reopen the dialog (the up button must render active), thumbs-down it, then click thumbs-down again to clear. After each step:
   `select feedback from posts where id = '<post id>';` -> `up`, then `down`, then `null`. One row throughout, never a second.

6. **ADMIN QUALITY DASHBOARD (POL-09).** As an admin, open `/admin/quality`. Confirm the feedback tally matches step 5's votes, the critic card shows real `visual_critic` outcome counts (cross-check with `select outcome, count(*) from generation_logs where event_kind='visual_critic' group by outcome;`), and the fallback card matches `select metadata->>'call_class', count(*) from generation_logs where event_kind='model_fallback' group by 1;`. Switch the window selector to 7 and 90 days and confirm the numbers move. Finally confirm the ACL: as a NON-admin, `GET /api/admin/quality` returns 403.

7. **NO-REGRESSION SWEEP + POL-08 HANDOFF.** Generate one video (the frozen GATE-08 path), one carousel, one product enhancement, and one single-image post; edit one pre-Phase-23 legacy post. Expect all five to succeed unchanged. Then confirm the POL-08 handoff is real but open: `docs/cost-reconciliation-runbook.md` exists, its audit-log table is empty, and its trigger condition has NOT yet been met — record in `26-10-SUMMARY.md` that POL-08 remains scheduled and Pending by design, and that it does not block the v1.6 milestone close.

Close with: "If any step fails, record it in 26-10-SUMMARY.md and do NOT mark Phase 26 complete."

Before committing, run `grep -c "MANUAL/LIVE VERIFICATION RUNBOOK" scripts/verify-phase-26.ts` and confirm it returns exactly 1 — the header-comment collision class that broke 24-07's own acceptance criterion.
  </action>
  <acceptance_criteria>
    - `grep -c "MANUAL/LIVE VERIFICATION RUNBOOK" scripts/verify-phase-26.ts` returns exactly 1
    - The runbook contains at least 6 numbered steps and at least 5 SQL statements
    - `grep -c "does not block\|does not gate" scripts/verify-phase-26.ts` returns at least 1 (the POL-08 handoff language)
    - `npx tsx scripts/verify-phase-26.ts` still exits 0 (the runbook is a comment; it must not affect execution)
    - `npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/verify-phase-26.ts &amp;&amp; npm run check</automated>
  </verify>
  <done>A seven-step authoritative runbook is embedded, appears exactly once, and does not change harness behavior.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator sign-off on the live runbook</name>
  <files>—</files>
  <read_first>
    - scripts/verify-phase-26.ts's trailing MANUAL/LIVE VERIFICATION RUNBOOK (Task 2's output — the authoritative list; do not restate it here)
    - .planning/phases/26-fixes-and-polish/26-06-SUMMARY.md (the exact idempotency response shapes runbook steps 1-2 assert on)
    - .planning/phases/26-fixes-and-polish/26-07-SUMMARY.md (the plate/auto-selection decision rules runbook step 3 checks visually)
    - .planning/phases/26-fixes-and-polish/26-05-SUMMARY.md (the POL-08 handoff state runbook step 7 records)
  </read_first>
  <action>
STOP and hand control to the operator. Do NOT attempt to run the live runbook yourself — every
step requires the live Coolify production host, the live Supabase project, real uploaded brand
logos, and real paid OpenRouter generations.

Before pausing, print to the operator:
  1. The absolute path of the runbook: `scripts/verify-phase-26.ts` (trailing block comment).
  2. Task 1's static-gate result: the full-suite PASS count across all 9 tags, plus the
     pass/fail status of each of the four Phase 26 unit harnesses and each of the nine prior
     gates swept by `[svc-cross-plan]`.
  3. The TWO migrations that must be applied to the live Supabase project BEFORE step 1 —
     `20260730000000_post_versions_idempotency_key.sql` and `20260730000001_posts_feedback.sql`.
     Steps 1, 2, 5 and 6 are meaningless against a database missing
     `post_versions.idempotency_key` or `posts.feedback`, and step 2's edit request will fail
     outright without the first one.
  4. That `idempotency_key` is now a REQUIRED uuid on `/api/generate` and `/api/edit-post`, so
     any external/manual caller (curl, Postman, a third-party integration) that does not send
     one will get a 400 — call this out explicitly so the operator can check for external
     consumers before rollout rather than discovering it in production.
  5. The exact `WEBP_TEXT_EDGE_MIN_RATIO` value and measured ratio table from 26-02-SUMMARY.md,
     so step 4's visual check has a stated numeric expectation rather than an improvised one.
  6. That POL-08 is scheduled-not-run BY DESIGN and must not be treated as a step failure.

After the operator responds, transcribe their per-step results verbatim into 26-10-SUMMARY.md as
a 7-row table (step / expected / observed / PASS-FAIL). If any step failed, record it, leave
Phase 26 open, and do not check its ROADMAP box.
  </action>
  <what-built>
    Phase 26 in full: WebP quality 85 with an automated text-edge gate (POL-02); the drawBlocks per-block font fix; contrast-aware adaptive logo overlay with alpha detection and auto corner selection (POL-03); idempotent `/api/generate` and `/api/edit-post` end to end, client and server (POL-06); the cost-reconciliation runbook and query scaffold (POL-08, scheduled not run); thumbs-up/down feedback plus the admin Quality dashboard (POL-09). Every statically and functionally provable claim is green: `scripts/verify-phase-26.ts` (9 tags), four Phase 26 unit harnesses, and a subprocess sweep of Phases 21/21.1/22/23/24/25 + the CI-wired golden-image gate.
  </what-built>
  <how-to-verify>
    Run the seven-step MANUAL/LIVE VERIFICATION RUNBOOK embedded at the bottom of `scripts/verify-phase-26.ts` against the live Coolify production host and the live Supabase project. Each step names its exact command, its exact SQL, and its exact expected value.

    These steps cannot be automated from a sandbox: they require real paid OpenRouter generations, a real Supabase database to count rows in, real uploaded brand logos, and human visual judgment of compression artifacts and logo plate quality.

    Record the outcome of each step in `26-10-SUMMARY.md`. If any step fails, do NOT mark Phase 26 complete and do NOT check its ROADMAP box.
  </how-to-verify>
  <acceptance_criteria>
    - All seven runbook steps executed against the live environment and their outcomes recorded verbatim in `26-10-SUMMARY.md`
    - Step 1 and Step 2 confirm exactly one row and one usage event per idempotency key
    - Step 5 confirms `up` -> `down` -> `null` on a single `posts` row
    - Step 6 confirms the dashboard totals match the raw SQL and that a non-admin gets 403
    - Step 7 confirms the video, carousel, enhancement, single-image, and legacy-edit paths all still succeed
    - POL-08 explicitly recorded as scheduled-and-Pending, non-blocking for the v1.6 milestone close
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/verify-phase-26.ts</automated>
  </verify>
  <resume-signal>Type "approved" once all seven steps pass, or describe which step failed and what you observed.</resume-signal>
  <done>An operator has executed the full live runbook and recorded every outcome; Phase 26 and the v1.6 milestone are cleared to close (POL-08 excepted by design).</done>
</task>

</tasks>

<verification>
1. `npx tsx scripts/verify-phase-26.ts` — exits 0, all 9 tags green, zero weakened checks.
2. `npx tsx scripts/verify-phase-26.ts --only=svc-cross-plan` — exits 0.
3. The subprocess sweep inside `[svc-cross-plan]` reports 0 for every prior gate and every Phase 26 unit harness.
4. `npm run check` and `npm run build` — exit 0.
5. Task 3's live runbook executed and recorded. Until then, Phase 26 is NOT closed and POL-02/03/06/09 stay Pending in REQUIREMENTS.md.
</verification>

<success_criteria>
- `scripts/verify-phase-26.ts` has 9 tags, is fully green, and its `[svc-cross-plan]` block asserts composition across the three files two different plans each edited.
- The MANUAL/LIVE VERIFICATION RUNBOOK is embedded exactly once and covers all four of 26-VALIDATION.md's manual-only behaviors.
- POL-08 is recorded as scheduled, Pending, and explicitly non-gating.
- Phase 26 is marked complete in the ROADMAP only after operator sign-off.
</success_criteria>

<output>
After completion, create `.planning/phases/26-fixes-and-polish/26-10-SUMMARY.md`
</output>
