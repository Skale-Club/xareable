---
phase: 23-deterministic-typography-and-edit-fidelity
plan: 09
type: execute
wave: 4
depends_on: ["23-06", "23-07"]
files_modified:
  - server/services/text-rendering.service.ts
  - server/services/observability.service.ts
  - server/routes/carousel.routes.ts
  - server/services/gemini.service.ts
  - shared/schema.ts
  - scripts/verify-phase-16.ts
autonomous: true
requirements: [TYPO-06]
must_haves:
  truths:
    - "The AI-rendered-text verify/repair loop no longer exists anywhere in the codebase"
    - "No generation emits a text_verification observability row"
    - "The Phase 16 observability harness still passes, with its OBS-01 block retired rather than broken"
    - "Generation and edit still work end-to-end with zero dangling imports"
  artifacts:
    - path: "server/services/text-rendering.service.ts"
      provides: "DELETED — must not exist after this plan"
    - path: "server/services/observability.service.ts"
      provides: "logTextVerification removed; logCaptionQuality and logSubjectFidelityFailure retained"
  key_links:
    - from: "scripts/verify-phase-16.ts"
      to: "the retired OBS-01 checks"
      via: "replaced with an inverted absence assertion so Phase 16's gate stays green and meaningful"
      pattern: "Phase 23"
---

<objective>
Delete the AI-rendered-text verify/repair loop for good. This runs in Wave 4, strictly AFTER `generate.routes.ts` (23-06) and `edit.routes.ts` (23-07) are both on the compositor — so there is never a moment where post generation has neither the old repair loop nor a working compositor.

Purpose: TYPO-06. Also reconciles the Phase 16 observability harness, which asserts the deleted file's existence and would otherwise throw.
Output: One file deleted, one export removed, one prior-phase harness updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-deterministic-typography-and-edit-fidelity/23-CONTEXT.md
@.planning/phases/23-deterministic-typography-and-edit-fidelity/23-RESEARCH.md

<interfaces>
Complete inventory of every reference to the deleted surface, from a repo-wide grep at planning time. Plans 23-06 and 23-07 clear the two route call sites; this plan clears the rest:

| File | Line(s) | Reference | Disposition |
|------|---------|-----------|-------------|
| `server/services/text-rendering.service.ts` | whole file | `verifyExactImageText`, `enforceExactImageText` | DELETE the file |
| `server/routes/generate.routes.ts` | 25, 269, 669 | import + comment + call | already removed by plan 23-06 — verify only |
| `server/routes/edit.routes.ts` | 28, 192, 506 | import + comment + call | already removed by plan 23-07 — verify only |
| `server/routes/carousel.routes.ts` | 695, 902 | comments asserting it is NOT called | update wording, keep the intent |
| `server/services/observability.service.ts` | 5-6, 36, 67, 70 | `logTextVerification` export + its doc header | remove the export + its `TextVerificationLogParams` type |
| `server/services/gemini.service.ts` | 773 | comment mentioning the verification model | reword |
| `shared/schema.ts` | 188 | comment mentioning `enforceExactImageText`'s verification model | reword |
| `scripts/verify-phase-06.ts` | 219, 290-302, 708-714 | asserts the carousel service does NOT reference it | LEAVE UNCHANGED — an absence assertion that still passes |
| `scripts/verify-phase-16.ts` | 5, 91-92, 148-159, 219-233 | `read("server/services/text-rendering.service.ts")` + OBS-01 checks + a dynamic `logTextVerification(...)` call | MUST be updated — `read()` throws on a missing file, so this harness breaks otherwise |

```typescript
// server/services/observability.service.ts — surrounding exports that MUST survive
export async function logCaptionQuality(params: CaptionQualityLogParams): Promise<void>;
export async function logSubjectFidelityFailure(params: SubjectFidelityLogParams): Promise<void>;
```

`generation_logs.event_kind` is an unconstrained `TEXT` column with no CHECK constraint or enum type (`20260508000000_generation_logs_observability.sql`) — deleting the `text_verification` emitter needs ZERO migration (23-RESEARCH.md Pitfall 8). Historical rows keep their `text_verification` value harmlessly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the routes are clean, then delete text-rendering.service.ts and logTextVerification</name>
  <files>server/services/text-rendering.service.ts (DELETED), server/services/observability.service.ts, server/routes/carousel.routes.ts, server/services/gemini.service.ts, shared/schema.ts</files>
  <read_first>
    - server/services/text-rendering.service.ts (whole file — confirm nothing else lives in it before deleting)
    - server/services/observability.service.ts (whole file — the doc header at :1-20, TextVerificationLogParams at ~:30-45, logTextVerification at ~:60-90)
    - server/routes/generate.routes.ts and server/routes/edit.routes.ts (confirm plans 23-06/23-07 landed — this is the sequencing gate)
    - server/routes/carousel.routes.ts lines 690-700 and 895-910 (the two CRSL-10 comments)
  </read_first>
  <action>
    1. GATE FIRST — do not proceed unless both are true:
       `grep -c 'enforceExactImageText' server/routes/generate.routes.ts` == 0 AND `grep -c 'enforceExactImageText' server/routes/edit.routes.ts` == 0.
       Also confirm `grep -c 'compositeTypography(' server/routes/generate.routes.ts` == 1. If any check fails, STOP and report — plans 23-06/23-07 have not landed and deleting now would leave generation with neither the repair loop nor the compositor.

    2. `git rm server/services/text-rendering.service.ts`.

    3. Edit `server/services/observability.service.ts`:
       - Delete `export async function logTextVerification(...)` and its `TextVerificationLogParams` interface entirely.
       - Update the file's doc header: replace the `logTextVerification — ✅ wired (OBS-01): emitted by enforceExactImageText in server/services/text-rendering.service.ts` bullet with:
         `*   - logTextVerification        — ❌ REMOVED in Phase 23 (TYPO-06). The AI verify/repair`
         `*                                    loop it instrumented is gone; text is now composited`
         `*                                    deterministically by typography-compositor.service.ts,`
         `*                                    so there is no AI-judgment step left to log.`
       - Leave `logCaptionQuality`, `logSubjectFidelityFailure`, and every other export byte-unchanged.
       - Do NOT add a migration. `generation_logs.event_kind` is unconstrained TEXT (Pitfall 8).

    4. Reword the three surviving comments so no dangling name remains:
       - `server/routes/carousel.routes.ts:695` and `:902`: keep the CRSL-10 intent but say "no AI text verify/repair pass is applied to carousel slides (that loop was removed entirely in Phase 23 — TYPO-06)" instead of naming `enforceExactImageText`.
       - `server/services/gemini.service.ts:773`: drop the `enforceExactImageText`'s verification model clause, keeping the rest of the sentence intact.
       - `shared/schema.ts:188`: same — reword the `ai_models.text_generation` tier comment so it no longer names `enforceExactImageText`.

    5. Repo-wide confirmation: `grep -rn "enforceExactImageText\|verifyExactImageText\|logTextVerification\|text-rendering.service" server/ client/src/ shared/ scripts/` must return hits ONLY in `scripts/verify-phase-06.ts` (absence assertions — leave them) and `scripts/verify-phase-16.ts` (fixed in Task 2).
  </action>
  <verify>
    <automated>npm run check &amp;&amp; node -e "const fs=require('fs'); if(fs.existsSync('server/services/text-rendering.service.ts'))process.exit(1); const o=fs.readFileSync('server/services/observability.service.ts','utf8'); if(/export\s+async\s+function\s+logTextVerification/.test(o))process.exit(1); if(!/logCaptionQuality/.test(o)||!/logSubjectFidelityFailure/.test(o))process.exit(1); console.log('deletion OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `server/services/text-rendering.service.ts` does not exist
    - `grep -c 'export async function logTextVerification' server/services/observability.service.ts` == 0
    - `grep -c 'export async function logCaptionQuality' server/services/observability.service.ts` == 1 and `logSubjectFidelityFailure` == 1
    - `grep -rc 'enforceExactImageText\|verifyExactImageText' server/ client/src/ shared/` == 0 across every file
    - `grep -rl 'logTextVerification' server/ client/src/ shared/` returns nothing
    - `grep -c 'TYPO-06' server/services/observability.service.ts` >= 1
    - No new `supabase/migrations/` file was created by this task
    - `npm run check` exits 0
    - `npx tsx scripts/verify-phase-06.ts` exits 0 (its absence assertions still hold)
  </acceptance_criteria>
  <done>The verify/repair service file is gone, `logTextVerification` is removed with its siblings intact, every dangling comment reference is reworded, and no migration was needed.</done>
</task>

<task type="auto">
  <name>Task 2: Retire Phase 16's OBS-01 block without weakening the harness</name>
  <files>scripts/verify-phase-16.ts</files>
  <read_first>
    - scripts/verify-phase-16.ts (whole file — note `read()` throws on a missing file; the OBS-01 block at :148-159, the OBS-03 export check at :88-100, and the dynamic Supabase round-trip at :212-245)
    - server/services/observability.service.ts (post-Task-1)
    - .planning/phases/23-deterministic-typography-and-edit-fidelity/23-RESEARCH.md (Pitfall 8)
  </read_first>
  <action>
    Edit `scripts/verify-phase-16.ts` only. The goal is an honest harness: OBS-01's original claim ("text-rendering.service.ts is instrumented") is no longer a requirement, so replace it with the INVERSE assertion rather than deleting the section or weakening it to a no-op.

    1. Replace the OBS-01 block (`:148-159`) with:
    ```typescript
    // ── OBS-01 (RETIRED in Phase 23 / TYPO-06) ───────────────────────────────
    // text-rendering.service.ts and its verify/repair loop were deleted entirely;
    // on-image text is now composited deterministically by
    // server/services/typography-compositor.service.ts, so there is no AI-judgment
    // step left to instrument. The original OBS-01 checks are replaced by their
    // inverse — this section still fails loudly if the deleted surface returns.
    console.log("\nOBS-01: text-rendering verify/repair loop REMOVED (Phase 23, TYPO-06)");
    check(
      "text-rendering.service.ts no longer exists",
      !fs.existsSync("server/services/text-rendering.service.ts"),
    );
    check(
      "observability.service.ts no longer exports logTextVerification",
      !/export\s+async\s+function\s+logTextVerification\b/.test(obsSvc),
    );
    check(
      "no route imports enforceExactImageText",
      !/enforceExactImageText/.test(readSafe("server/routes/generate.routes.ts"))
        && !/enforceExactImageText/.test(readSafe("server/routes/edit.routes.ts")),
    );
    ```
    If `verify-phase-16.ts` has no `readSafe`/`fs` helper in scope, add the same `readSafe()` helper `verify-phase-22.ts` uses (`fs.existsSync(p) ? fs.readFileSync(p,"utf8") : ""`) and import `fs` from `node:fs`.

    2. Fix the OBS-03 scaffolding check at `:91-92` — it asserts `observability.service.ts` exports `logTextVerification`. Replace that single `check(...)` with one asserting the two SURVIVING exports (`logCaptionQuality`, `logSubjectFidelityFailure`) are both present, and add a trailing comment `// logTextVerification retired in Phase 23 (TYPO-06).`

    3. Fix the dynamic round-trip at `:212-245`: remove the `logTextVerification` import from the destructured dynamic import and delete its `await logTextVerification({...})` call plus any assertion that queries for a `text_verification` row. Keep the `logCaptionQuality` and `logSubjectFidelityFailure` round-trips exactly as they are (they are still env-gated and skip cleanly without Supabase credentials).

    4. Leave the `generationLogSchema error_type widened` check at `:80-84` alone — it asserts the schema still *accepts* `text_verification`, which remains true and correct: historical rows carry that value and `event_kind` is unconstrained TEXT (Pitfall 8). Add a one-line comment saying so.

    5. Add a dated note to the top-of-file comment block: `// Phase 23 (2026-07): OBS-01 retired — see the OBS-01 section below.`
  </action>
  <verify>
    <automated>npx tsx scripts/verify-phase-16.ts &amp;&amp; npx tsx scripts/verify-phase-23.ts --only=svc-verify-repair-removed</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/verify-phase-16.ts` exits 0 (no throw on the missing file, all checks pass)
    - `grep -c 'logTextVerification' scripts/verify-phase-16.ts` >= 1 but every occurrence is inside an INVERSE assertion or a comment — there is no `await logTextVerification(` call left (`grep -c 'await logTextVerification(' scripts/verify-phase-16.ts` == 0)
    - `grep -c 'RETIRED in Phase 23' scripts/verify-phase-16.ts` >= 1
    - `grep -c 'logCaptionQuality' scripts/verify-phase-16.ts` >= 2 (static check + dynamic round-trip retained)
    - `grep -c 'read("server/services/text-rendering.service.ts")' scripts/verify-phase-16.ts` == 0
    - The Phase 16 harness check COUNT did not shrink by more than 2 versus its pre-change output (record both totals in the SUMMARY — retiring must not silently gut the gate)
    - `npx tsx scripts/verify-phase-23.ts --only=svc-verify-repair-removed` exits 0
    - `npx tsx scripts/verify-phase-21.ts && npx tsx scripts/verify-phase-21.1.ts && npx tsx scripts/verify-phase-22.ts && npx tsx scripts/verify-phase-06.ts` all exit 0
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>Phase 16's harness passes with OBS-01 converted from a presence assertion into an absence assertion, its surviving observability round-trips intact, and its total check count essentially unchanged.</done>
</task>

</tasks>

<verification>
- `server/services/text-rendering.service.ts` does not exist
- `npx tsx scripts/verify-phase-23.ts --only=svc-verify-repair-removed` exits 0
- `npx tsx scripts/verify-phase-16.ts` exits 0
- `npx tsx scripts/verify-phase-06.ts` exits 0
- `npm run check` exits 0
- Prior-phase harnesses (21, 21.1, 22) still exit 0
</verification>

<success_criteria>
TYPO-06 is complete: the verify/repair service, its observability emitter, and every dangling reference are gone; no migration was required; the Phase 16 harness is honestly reconciled rather than broken or weakened; and the compositor has been the live text path since Wave 3 so there was never a coverage gap.
</success_criteria>

<output>
After completion, create `.planning/phases/23-deterministic-typography-and-edit-fidelity/23-09-SUMMARY.md`
</output>
