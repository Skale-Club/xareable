---
phase: 25-narrative-carousels-and-aesthetic-dna
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-phase-25.ts
autonomous: true
requirements: [PLAN-05, PLAN-06, PLAN-07, CRSL2-01, CRSL2-02, CRSL2-04]

must_haves:
  truths:
    - "`npx tsx scripts/verify-phase-25.ts --only=self-test` exits 0 and proves the scanner is not vacuous"
    - "`npx tsx scripts/verify-phase-25.ts` runs to completion with zero uncaught exceptions even though plans 25-02..25-13 have not landed"
    - "Every red check names a real, not-yet-written artifact (file path + symbol), never a vague assertion"
  artifacts:
    - path: "scripts/verify-phase-25.ts"
      provides: "7-tag Phase 25 static+functional phase gate"
      min_lines: 400
      contains: "svc-carousel-narrative"
  key_links:
    - from: "scripts/verify-phase-25.ts"
      to: "--only=<tag> filter"
      via: "check(name, cond, detail) with name.includes(only)"
      pattern: "--only="
---

<objective>
Install the Phase 25 phase gate — `scripts/verify-phase-25.ts` — the Wave 0 Nyquist requirement. Nothing in Phase 25 can be verified until this file exists.

Purpose: every subsequent plan's `<verify><automated>` command is `npx tsx scripts/verify-phase-25.ts --only=<tag>`. This plan makes those commands runnable (and honestly red) before any implementation lands.
Output: `scripts/verify-phase-25.ts` with 7 tags — `[self-test]` green, the other 6 honestly red.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-narrative-carousels-and-aesthetic-dna/25-VALIDATION.md
@.planning/phases/25-narrative-carousels-and-aesthetic-dna/25-RESEARCH.md

<interfaces>
<!-- The pattern to copy VERBATIM. scripts/verify-phase-24.ts is the reference implementation. -->

From scripts/verify-phase-24.ts (lines 26-50) — the harness core:
```typescript
const failures: string[] = [];
const ok: string[] = [];
const onlyArg = process.argv.find((a) => /^--only=(.+)$/.test(a));
const only = onlyArg ? onlyArg.match(/^--only=(.+)$/)![1] : null;

function check(name: string, cond: boolean, detail = "") {
  if (only && !name.includes(only)) return;
  if (cond) ok.push(name);
  else failures.push(`${name}${detail ? " — " + detail : ""}`);
}
function read(p: string) { return fs.readFileSync(p, "utf8"); }
function exists(p: string) { return fs.existsSync(p); }
function readSafe(p: string): string { return fs.existsSync(p) ? fs.readFileSync(p, "utf8") : ""; }
function tagActive(tag: string): boolean { return !only || tag.includes(only) || only.includes(tag); }
```

From scripts/verify-phase-24.ts (lines 59-118) — balanced-delimiter extractors already written and reusable:
`extractZodObjectBody(src, constName)`, `extractConstObjectLiteral(src, markerPrefix)`, `extractInterfaceBody(src, name)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harness scaffold + [self-test] tag</name>
  <read_first>
    - scripts/verify-phase-24.ts (read lines 1-235 in full — copy the header comment shape, the `check`/`read`/`exists`/`readSafe`/`tagActive` helpers, the balanced-delimiter extractors, and the `[self-test]` block verbatim in structure)
    - scripts/verify-phase-24.ts (read the last 40 lines before the runbook block — the summary/`process.exit(1)` epilogue)
    - .planning/phases/25-narrative-carousels-and-aesthetic-dna/25-VALIDATION.md (the tag list)
  </read_first>
  <files>scripts/verify-phase-25.ts</files>
  <action>
Create `scripts/verify-phase-25.ts` following `scripts/verify-phase-24.ts`'s exact structure.

Header block comment must state: run command, `--only=<substring>` usage, that this harness is created by plan 25-01 and extended ONLY by plan 25-14 (which adds `[svc-cross-plan]` + the live runbook), and that plans 25-02..25-13 must NOT edit this file.

Copy these helpers verbatim from verify-phase-24.ts: `check`, `read`, `exists`, `readSafe`, `tagActive`, `extractZodObjectBody`, `extractConstObjectLiteral`, `extractInterfaceBody`.

Declare the 7 tag literals in a `const PHASE_25_TAGS` array so the self-test can count them:
`["self-test", "svc-carousel-narrative", "svc-carousel-compositor", "svc-carousel-textstyle-logo", "svc-aesthetic-dna-catalog", "svc-color-proportion", "svc-style-reference-boards"]`

Load sources ONCE at the top with `readSafe()` (NOT `read()`) so a not-yet-created file yields `""` instead of throwing:
`shared/schema.ts`, `server/services/carousel-generation.service.ts`, `server/services/carousel-plan-schema.service.ts`, `server/services/style-reference.service.ts`, `server/services/style-art-direction.service.ts`, `server/services/prompt-builder.service.ts`, `server/services/typography-compositor.service.ts`, `server/services/gemini.service.ts`, `server/routes/generate.routes.ts`, `server/routes/carousel.routes.ts`, `server/routes/style-references.routes.ts`, `server/routes/index.ts`, `client/src/components/admin/post-creation/brand-styles-card.tsx`, `client/src/components/admin/post-creation/post-moods-card.tsx`, `client/src/components/admin/post-creation/style-reference-boards-card.tsx`.

Write the `[self-test]` block — exactly 5 checks:
1. `check("[self-test] harness executes", true)`
2. harness source contains all 7 tag literals from `PHASE_25_TAGS` (read own file via `read(__filename)` equivalent — use `read("scripts/verify-phase-25.ts")`)
3. harness is wired to invoke the Wave 0 unit harness `scripts/test-carousel-narrative-plan.ts` (assert the harness source MENTIONS that path — NOT that the file exists on disk; 25-03 creates it)
4. harness is wired to invoke the Wave 0 unit harness `scripts/test-style-reference-merge.ts` (same MENTIONS-not-exists rule; 25-04 creates it)
5. scanner-is-not-vacuous proof: assert a deliberately absent sentinel string (e.g. `"__PHASE_25_ABSENT_SENTINEL__"`) is correctly reported ABSENT from `shared/schema.ts`

Epilogue (copy verify-phase-24.ts's): print `PASS n` / `FAIL n` counts, print each failure line, `process.exit(1)` when `failures.length > 0`, else exit 0.
  </action>
  <verify>
    <automated>npx tsx scripts/verify-phase-25.ts --only=self-test</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/verify-phase-25.ts --only=self-test` exits 0 with exactly 5 PASS and 0 FAIL
    - `grep -c "PHASE_25_TAGS" scripts/verify-phase-25.ts` returns >= 1
    - `grep -c "readSafe(" scripts/verify-phase-25.ts` returns >= 15 (every source loaded tolerantly)
    - `grep -c "fs.readFileSync" scripts/verify-phase-25.ts` shows no bare top-level `read(` of a not-yet-created path (no uncaught ENOENT)
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>The harness runs, its self-test is green, and it does not throw on any missing Phase 25 artifact.</done>
</task>

<task type="auto">
  <name>Task 2: The 6 requirement tags (honestly red)</name>
  <read_first>
    - scripts/verify-phase-25.ts (Task 1's output)
    - .planning/phases/25-narrative-carousels-and-aesthetic-dna/25-RESEARCH.md (the "Phase Requirements → Test Map" table — the authoritative behavior list per tag)
    - scripts/verify-phase-24.ts (lines 280-500 — how a functional `import()`-based check is written alongside static greps)
  </read_first>
  <files>scripts/verify-phase-25.ts</files>
  <action>
Append the 6 requirement tag blocks. Every check name MUST start with its `[tag]` literal so `--only=` filtering works. Every red check's `detail` argument must name the exact file path + symbol that will make it green.

`[svc-carousel-narrative]` (CRSL2-01) — ~9 checks:
- `server/services/carousel-plan-schema.service.ts` exists
- it exports `SLIDE_ROLES` containing `"hook"`, `"content"`, `"cta"`
- it exports `CAROUSEL_PLAN_JSON_SCHEMA` (OpenRouter dialect) AND `CAROUSEL_PLAN_GEMINI_RESPONSE_SCHEMA` (direct dialect) as two DISTINCT const declarations
- dialect non-cross-wiring: the OpenRouter schema body contains `strict: true` and `additionalProperties: false`; the Gemini schema body contains `"OBJECT"`/`"STRING"` uppercase Type literals and contains NEITHER `additionalProperties` NOR `strict:`
- it exports `assignSlideRoles`, `findDuplicateCompositionNotes`, `validateCarouselWirePlan`
- carousel-generation.service.ts's `CarouselTextPlan` interface body contains `layout_archetype_id`, `composition_note`, `text_blocks`, `role`
- carousel-generation.service.ts imports from `./carousel-plan-schema.js`
- carousel-generation.service.ts calls `assignSlideRoles(` (server-side deterministic role assignment, model output overwritten)
- FUNCTIONAL (guarded by `tagActive("svc-carousel-narrative")`): `spawnSync("npx", ["tsx", "scripts/test-carousel-narrative-plan.ts"])` exits 0

`[svc-carousel-compositor]` (CRSL2-02) — ~8 checks:
- carousel-generation.service.ts imports `cropToExactAspectRatio` from `./image-crop.service.js`
- it imports `resolveTextBlocks` and `compositeTypography` from `./typography-compositor.service.js`
- PIPELINE ORDER inside the per-slide loop: `indexOf("cropToExactAspectRatio(")` < `indexOf("compositeTypography(")` < `indexOf("applyLogoOverlay(")` < `indexOf("uploadSlideBuffer(")` (mirrors verify-phase-23.ts's pipeline-order check)
- SLIDE-1 ANCHOR DISCIPLINE: the assignment `slide1Base64 = result.rawBase64` appears BEFORE the first `cropToExactAspectRatio(` occurrence in the file (the anchor is captured from the raw AI buffer)
- carousel-generation.service.ts's `slideRows` insert object contains `base_image_url` and `typography_meta`
- `layout_archetype_id` is read from the CAROUSEL-level plan field, not from a per-slide field, at the `compositeTypography(` call site (assert the call site's surrounding 400 chars contain `plan.layout_archetype_id` and do NOT contain `slides[i].layout_archetype_id`)
- shared/schema.ts's `postSlideSchema` body contains `base_image_url` and `typography_meta`
- `server/routes/carousel.routes.ts` slide-edit path exports `resolveSlideEditTarget` (typography-aware edit, CRSL2-02 no-double-render)

`[svc-carousel-textstyle-logo]` (CRSL2-04) — ~7 checks:
- typography-compositor.service.ts exports `resolveTypographyTreatment`
- `compositeTypography`'s param object type contains `treatment?:`
- typography-compositor.service.ts still registers ONLY the bundled Inter aliases (assert `FONT_ALIASES` body has exactly 3 entries and contains no `.ttf`/`.otf` path literal outside `server/assets/fonts`) — scope guard: no new font family bundled this phase
- carousel-generation.service.ts calls `resolveTypographyTreatment(`
- carousel-generation.service.ts resolves `params.textStyleIds` against `styleCatalog.text_styles` (assert both `params.textStyleIds` and `text_styles` appear in the same 800-char window)
- carousel-generation.service.ts still calls `applyLogoOverlay(` exactly once in the slide loop (logo regression guard)
- carousel-generation.service.ts's `posts` insert object contains `generation_params` (so slide edit/remake can reuse aspect_ratio/use_logo/logo_position/text_style_ids)

`[svc-aesthetic-dna-catalog]` (PLAN-05) — ~7 checks:
- shared/schema.ts declares `artDirectionSchema` with `photography_type`, `lighting`, `composition`, `texture`, `negative_prompts`
- `brandStyleSchema` body contains `art_direction`
- `postMoodSchema` body contains `art_direction`
- `server/services/style-art-direction.service.ts` exists and exports `buildStyleArtDirectionBlock` and `GLOBAL_ANTI_AI_NEGATIVE_PROMPT`
- FUNCTIONAL (`tagActive` guarded, `await import("../shared/schema.js")`): every entry in `DEFAULT_STYLE_CATALOG.styles` has non-empty `art_direction.photography_type`, `.lighting`, `.composition`, `.texture` and `negative_prompts.length >= 2`
- FUNCTIONAL: same assertion for every entry in `DEFAULT_STYLE_CATALOG.post_moods`
- FUNCTIONAL non-duplication: across all styles, the set of `art_direction.photography_type` values has the same size as the styles array (no two styles share a photography_type verbatim), and no entry's `photography_type` is byte-equal to its own `description`

`[svc-color-proportion]` (PLAN-06) — ~6 checks:
- prompt-builder.service.ts exports `formatBrandColorsProportional`
- FUNCTIONAL: calling it with `{color_1:"#0A2540",color_2:"#F5F7FA",color_3:"#8892A0",color_4:"#FF6B35"}` returns a string containing `60`, `30`, `10`, `#0A2540`, `#F5F7FA` and `#FF6B35`
- FUNCTIONAL: the returned string does NOT contain `#8892A0` in the 10% accent clause (color_3 is NOT the accent — color_4 is)
- FUNCTIONAL graceful degradation: with `color_3: null, color_4: null` it still returns a non-empty string mentioning `60` and `30` and does NOT contain the literal `null` or `undefined`
- gemini.service.ts's `buildContextPrompt` calls `formatBrandColorsProportional(`
- carousel-generation.service.ts's `buildCarouselMasterPrompt` calls `formatBrandColorsProportional(`

`[svc-style-reference-boards]` (PLAN-07) — ~10 checks:
- a migration file matching `supabase/migrations/*_style_reference_photos.sql` exists
- that migration contains `CREATE TABLE IF NOT EXISTS public.style_reference_photos`
- RLS INVERTED-ACL: it contains `FOR SELECT USING (true)` AND `profiles.is_admin = true` AND does NOT contain `user_id = auth.uid()` (must NOT copy brand_reference_photos' owner-only pattern)
- it contains `ENABLE ROW LEVEL SECURITY`
- shared/schema.ts declares `styleReferencePhotoSchema`
- `server/services/style-reference.service.ts` exists and exports `planReferenceImageSlots` and `REFERENCE_IMAGE_SLOT_LIMIT`
- `server/routes/style-references.routes.ts` exists and uses `requireAdminGuard`
- `server/routes/index.ts` imports and `router.use(...)` the style-references router
- generate.routes.ts calls `planReferenceImageSlots(` (single-image path uses the shared merge, not the old inline arithmetic)
- carousel-generation.service.ts calls `planReferenceImageSlots(` (carousel path too — PLAN-07 covers both per 25-CONTEXT.md's resolved question)
- FUNCTIONAL (`tagActive` guarded): `spawnSync("npx", ["tsx", "scripts/test-style-reference-merge.ts"])` exits 0

Run the full suite once and confirm it is HONESTLY RED (many FAIL, zero uncaught exceptions).
  </action>
  <verify>
    <automated>npx tsx scripts/verify-phase-25.ts --only=self-test && npx tsx scripts/verify-phase-25.ts; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/verify-phase-25.ts --only=self-test` still exits 0 with 5/5
    - `npx tsx scripts/verify-phase-25.ts` exits 1 (honestly red) and prints NO stack trace / no "ENOENT" / no "TypeError"
    - Each of `--only=svc-carousel-narrative`, `--only=svc-carousel-compositor`, `--only=svc-carousel-textstyle-logo`, `--only=svc-aesthetic-dna-catalog`, `--only=svc-color-proportion`, `--only=svc-style-reference-boards` runs and reports >= 6 checks each
    - `grep -c "^  check(\"\[" scripts/verify-phase-25.ts` combined with the tag blocks totals >= 45 checks
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>All 7 tags exist; self-test green; the other 6 red with actionable detail strings naming the exact artifacts plans 25-02..25-13 will create.</done>
</task>

</tasks>

<verification>
- `npx tsx scripts/verify-phase-25.ts --only=self-test` → 5/5 PASS, exit 0
- `npx tsx scripts/verify-phase-25.ts` → exit 1, zero uncaught exceptions
- `npm run check` → exit 0
- Zero regression: `npx tsx scripts/verify-phase-24.ts` still green
</verification>

<success_criteria>
The Phase 25 gate exists, is self-proving, and every downstream plan's `--only=<tag>` command is runnable today.
</success_criteria>

<output>
After completion, create `.planning/phases/25-narrative-carousels-and-aesthetic-dna/25-01-SUMMARY.md`
</output>
