---
phase: 25-narrative-carousels-and-aesthetic-dna
plan: 14
type: execute
wave: 6
depends_on: ["25-01", "25-02", "25-03", "25-04", "25-05", "25-06", "25-07", "25-08", "25-09", "25-10", "25-11", "25-12", "25-13"]
files_modified:
  - scripts/verify-phase-25.ts
autonomous: false
requirements: [PLAN-05, PLAN-06, PLAN-07, CRSL2-01, CRSL2-02, CRSL2-04]

must_haves:
  truths:
    - "The full Phase 25 harness is green with zero weakened checks"
    - "Invariants that span files no single plan owns are asserted, not assumed"
    - "Every Phase 25 no-network unit harness still passes, proven by subprocess"
    - "No prior phase harness and no golden-image gate regressed"
    - "An operator has confirmed a real narrative carousel, real composited slide text, real framing variation, a real slide edit with no double-render, real aesthetic-DNA output, and a real style reference board attachment"
  artifacts:
    - path: "scripts/verify-phase-25.ts"
      provides: "8th [svc-cross-plan] tag + the authoritative MANUAL/LIVE VERIFICATION RUNBOOK"
      contains: "[svc-cross-plan]"
  key_links:
    - from: "scripts/verify-phase-25.ts"
      to: "verify-phase-21/21.1/22/23/24 + verify-golden-image"
      via: "spawnSync non-regression sweep"
      pattern: "spawnSync"
    - from: "scripts/verify-phase-25.ts"
      to: "server/routes/generate.routes.ts + server/services/carousel-generation.service.ts"
      via: "indexOf pipeline-parity assertions across both generation paths"
      pattern: "indexOf"
---

<objective>
Close Phase 25: add the cross-plan invariant checks no individual plan could own, embed the authoritative live runbook, and get operator sign-off against real traffic.

Purpose: plans 25-01..25-13 each proved their own file. Nobody has yet proved that the carousel path and the single-image path run the SAME pipeline in the same order, that the layout archetype is structurally impossible to vary per slide, that "slides now carry text" and "slide edits are base-aware" have no coverage gap between them, that the 4-slot reference cap is declared exactly once, or that no prior phase regressed. And no static check can prove a real carousel actually reads as a narrative with visibly varied framing.
Output: an 8-tag green harness, an 8-step runbook, and a human gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-narrative-carousels-and-aesthetic-dna/25-CONTEXT.md
@.planning/phases/25-narrative-carousels-and-aesthetic-dna/25-RESEARCH.md
@.planning/phases/25-narrative-carousels-and-aesthetic-dna/25-VALIDATION.md

<interfaces>
<!-- The exact precedent to mirror. Read it, do not invent a new shape. -->

scripts/verify-phase-24.ts — the `[svc-cross-plan]` block added by plan 24-07 (7 checks:
pipeline order via `indexOf`, a no-bypass structural assertion, a frozen-branch fence, a
backward-compatibility scan across 6 files, a two-sided billing invariant, a `spawnSync`
prior-phase regression sweep, and a CI-safety spawn of the live harness) followed by the
trailing `MANUAL/LIVE VERIFICATION RUNBOOK` block comment. `scripts/verify-phase-23.ts`'s
`[svc-cross-plan]` (plan 23-11) is the same shape one phase earlier.

Harness helpers already present in scripts/verify-phase-25.ts (created by plan 25-01):
```typescript
function check(name: string, cond: boolean, detail = "")   // skips when --only= does not match
function readSafe(p: string): string                        // "" for a missing file
function tagActive(tag: string): boolean                    // guard for real subprocess spawns
const PHASE_25_TAGS = ["self-test", "svc-carousel-narrative", "svc-carousel-compositor",
  "svc-carousel-textstyle-logo", "svc-aesthetic-dna-catalog", "svc-color-proportion",
  "svc-style-reference-boards"];                            // this plan appends the 8th
```
`spawnSync` is NOT yet imported in verify-phase-25.ts — add `import { spawnSync } from "node:child_process";`
if plan 25-01 did not already include it (25-01 Task 2 uses it inside two tag blocks, so it is
most likely already there — check before adding a duplicate import).

The four Phase 25 no-network unit harnesses this plan sweeps:
```
scripts/test-carousel-narrative-plan.ts   (25-03)
scripts/test-style-reference-merge.ts     (25-04)
scripts/test-typography-treatment.ts      (25-07)
scripts/test-slide-edit-resolution.ts     (25-13)
```

The prior-phase harnesses this plan sweeps (25-VALIDATION.md "Phase gate" line + the
golden-image gate 25-07 touches a Phase 23 file to preserve):
```
scripts/verify-phase-21.ts   scripts/verify-phase-21.1.ts   scripts/verify-phase-22.ts
scripts/verify-phase-23.ts   scripts/verify-phase-24.ts     scripts/verify-golden-image.ts
```

The 4 manual-only items from 25-VALIDATION.md that the runbook must cover:
1. Real visual composition variation across slides (CRSL2-01, SC2)
2. Real on-slide text + narrative structure end-to-end (CRSL2-01, CRSL2-02)
3. Aesthetic DNA prompt quality (PLAN-05, PLAN-06)
4. Style reference board attachment (PLAN-07)

Log lines the runbook greps for (written by 25-09 / 25-10 / 25-12):
```
[Reference Images] Post <id>: N user + N brand + N style-board = N/4 slots
[carousel] reference images: N brand + N style-board = N/4
[carousel] composition variation warning: slides a/b (0.xx)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: the 8th [svc-cross-plan] tag — invariants spanning multiple plans' files</name>
  <read_first>
    - scripts/verify-phase-25.ts (all of it, as built by 25-01 — the helper names, the tag array, the epilogue)
    - scripts/verify-phase-24.ts lines 639-800 (the `[svc-cross-plan]` block: `indexOf` ordering, slice-and-assert fences, the `tagActive`-guarded `spawnSync` sweep, and the deviation note about `indexOf` substring collisions)
    - server/routes/generate.routes.ts and server/services/carousel-generation.service.ts (final state after 25-09 / 25-12 — the two files whose pipeline parity check 1 asserts)
    - server/routes/carousel.routes.ts (final state after 25-13)
    - .planning/phases/25-narrative-carousels-and-aesthetic-dna/25-RESEARCH.md ("Don't Hand-Roll" — the single-4-slot-cap rationale behind check 5)
  </read_first>
  <files>scripts/verify-phase-25.ts</files>
  <action>
Append a `[svc-cross-plan]` block to `scripts/verify-phase-25.ts`, add the literal
`"svc-cross-plan"` to `PHASE_25_TAGS` (making it 8), update the `[self-test]` tag-count check
from 7 to 8, and update the file header comment to record that plan 25-14 added this tag.

This plan is the ONLY plan permitted to edit this file after 25-01 (25-01's own header states
so). Touch NO other file. Do not weaken, delete, or soften any check that plans 25-01..25-13
turned green — if a check is red, fix the CODE. The one known-legitimate exception is a
self-referential scanner false positive (a harness pattern literal tripping its own scan; see
23-09 and 24-07's Rule-3 deviations) — fix the scanner and record it as a deviation in the SUMMARY.

Checks (all real assertions, none stubbed):

  1. **PIPELINE PARITY ACROSS BOTH GENERATION PATHS** (spans 25-09's `generate.routes.ts` and
     25-12's `carousel-generation.service.ts` — no single plan owns both). For EACH file,
     compute `indexOf` for `cropToExactAspectRatio(`, `compositeTypography(`, `applyLogoOverlay(`
     and the file's own optimize marker (`processImageWithThumbnail(` for generate.routes.ts,
     `uploadSlideBuffer(` for carousel-generation.service.ts) and assert they are strictly
     increasing. Emit ONE check per file, both named `[svc-cross-plan] PIPELINE PARITY ...`.
     BEWARE the 23-11/24-07 substring-collision class: `generate.routes.ts` contains an earlier,
     unrelated `processImageWithThumbnail(` inside the video-post branch — search for the optimize
     marker starting FROM the `applyLogoOverlay(` index, not from 0.

  2. **ARCHETYPE SINGULARITY (SC1)** — spans 25-03, 25-10, 25-12 and 25-13. Assert ALL THREE:
     (a) `extractInterfaceBody(carouselPlanSchemaSrc, "CarouselWireSlide")` does NOT contain
     `layout_archetype_id` (it is a carousel-level field only, never per-slide);
     (b) `carousel-generation.service.ts` contains `plan.layout_archetype_id` and contains
     NEITHER `slides[i].layout_archetype_id` NOR `slide.layout_archetype_id`;
     (c) `carousel.routes.ts` passes `layoutArchetypeId: carouselArchetype` to its
     `compositeTypography(` call — i.e. an edit re-uses the CAROUSEL's archetype, not a
     per-slide guess. If any one fails, SC1's "layout archetype held consistent across all
     slides" is structurally violated somewhere no single plan's check can see.

  3. **NO-DOUBLE-RENDER CLOSURE (no coverage gap)** — spans 25-12 and 25-13. Assert the
     conjunction: `carousel-generation.service.ts` contains `compositeTypography(` (generation
     now composites real text onto slides) AND `carousel.routes.ts` contains BOTH
     `resolveSlideEditTarget(` and `isBaseImage` (the edit path resolves a pre-typography
     target) AND `carousel.routes.ts` contains ZERO occurrences of `CRSL-10` and zero
     occurrences of the case-insensitive phrase `do not use on-image text`. Write the check
     name to state the property: *"slides carry composited text AND the slide-edit path is
     base-image-aware — the Phase-23-class double-render regression 25-CONTEXT.md forbids is
     structurally closed"*. Add a comment noting that generation-composites-WITHOUT-base-aware-edit
     is exactly the failure mode this single conjunction catches and neither plan alone can.

  4. **LEGACY REACHABILITY** — spans 25-02's migration and 25-13's route. Read
     `supabase/migrations/20260729000000_post_slides_base_image_typography.sql` and assert it
     contains `ADD COLUMN IF NOT EXISTS base_image_url` and contains NONE of `NOT NULL`,
     `DEFAULT `, `UPDATE public.post_slides`, `BACKFILL` (case-insensitive) — a backfilled or
     non-null column would make the LEGACY branch dead code and silently rewrite pre-Phase-25
     slides. Then assert `carousel.routes.ts` contains a literal `isBaseImage: false` return
     (the LEGACY branch is genuinely reachable, not just documented).

  5. **REFERENCE-SLOT CAP DECLARED EXACTLY ONCE** — spans 25-04, 25-09 and 25-12
     (25-RESEARCH.md "Don't Hand-Roll": *"a second, slightly different cap invites drift"*).
     Assert `style-reference.service.ts` contains `export const REFERENCE_IMAGE_SLOT_LIMIT = 4`
     AND that neither `generate.routes.ts` nor `carousel-generation.service.ts` contains
     `slotsRemaining` or the old arithmetic literal `4 - userRefImages.length`.

  6. **AESTHETIC DNA REACHES BOTH PATHS (SC4)** — spans 25-06, 25-09 and 25-10. Assert that
     BOTH `gemini.service.ts` AND `carousel-generation.service.ts` contain
     `buildStyleArtDirectionBlock(` and `formatBrandColorsProportional(`. SC4 says "selecting
     any style/mood in the creator" — carousels are a creator surface, so a single-image-only
     injection would satisfy every per-plan check while still failing SC4.

  7. **GATE-08 VIDEO FENCE STILL HOLDS** — spans 25-06 and 25-09. Assert (a)
     `gemini.service.ts` still contains `formatBrandColorsLabeled(` (the frozen video branch's
     helper survived the image-branch rewrite) and (b) the 400 characters surrounding
     `generate.routes.ts`'s `resolveGenerationReferenceImages({` call contain `isVideo` — the
     two new reference tiers are excluded from the frozen video path.

  8. **EVERY PHASE 25 UNIT HARNESS STILL PASSES** — guard with `tagActive("svc-cross-plan")`,
     then `spawnSync("npx", ["tsx", script], { encoding: "utf8", shell: true })` for
     `scripts/test-carousel-narrative-plan.ts`, `scripts/test-style-reference-merge.ts`,
     `scripts/test-typography-treatment.ts`, `scripts/test-slide-edit-resolution.ts`, asserting
     each `status === 0`. Include the child's last stderr/stdout line in the failure detail.
     This is what pulls 25-13's own no-network harness into the phase gate (the
     `[svc-carousel-compositor]` tag only greps for the export).

  9. **ZERO REGRESSION** — same guard, same spawn shape, for
     `scripts/verify-phase-21.ts`, `scripts/verify-phase-21.1.ts`, `scripts/verify-phase-22.ts`,
     `scripts/verify-phase-23.ts`, `scripts/verify-phase-24.ts`, `scripts/verify-golden-image.ts`.
     The golden-image gate is in this list specifically because 25-07 edited a Phase 23 file
     (`typography-compositor.service.ts`) that the gate renders through.

Run the FULL suite and confirm it is green before finishing.
  </action>
  <verify>
    <automated>npx tsx scripts/verify-phase-25.ts --only=svc-cross-plan && npx tsx scripts/verify-phase-25.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/verify-phase-25.ts --only=svc-cross-plan` exits 0 with >= 17 PASS lines (9 check groups, 8 of which expand to one check per swept script/file)
    - `npx tsx scripts/verify-phase-25.ts --only=self-test` exits 0 and its tag-literal check now requires 8 tags
    - `npx tsx scripts/verify-phase-25.ts` exits 0 — FULL suite green, >= 60 checks
    - `grep -c '"svc-cross-plan"' scripts/verify-phase-25.ts` returns >= 1 and `grep -c "\[svc-cross-plan\]" scripts/verify-phase-25.ts` returns >= 10
    - `grep -c "spawnSync" scripts/verify-phase-25.ts` returns >= 3 and `grep -c "verify-golden-image" scripts/verify-phase-25.ts` returns >= 1
    - `git diff --stat` shows `scripts/verify-phase-25.ts` as the ONLY changed file
    - `git diff scripts/verify-phase-25.ts` shows additions only, apart from the tag array, the self-test tag count, and the header comment
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>Pipeline parity across both generation paths, archetype singularity, the closed double-render gap, LEGACY reachability, the single slot cap, both-path aesthetic DNA, and the frozen video fence are all machine-asserted — plus every Phase 25 and prior-phase harness is proven green by subprocess.</done>
</task>

<task type="auto">
  <name>Task 2: embed the MANUAL/LIVE VERIFICATION RUNBOOK</name>
  <read_first>
    - scripts/verify-phase-24.ts (the trailing `MANUAL/LIVE VERIFICATION RUNBOOK` block comment added by 24-07 — mirror its numbered, copy-pasteable style; each step states the exact command/SQL and the exact expected result)
    - scripts/verify-phase-23.ts (the same block one phase earlier, for the shorter form)
    - .planning/phases/25-narrative-carousels-and-aesthetic-dna/25-VALIDATION.md ("Manual-Only Verifications" — the 4 items every step set must cover)
    - .planning/phases/25-narrative-carousels-and-aesthetic-dna/25-05-SUMMARY.md and 25-12-SUMMARY.md (the actual curated `photography_type` strings and the actual reference-image log-line format the runbook greps for)
  </read_first>
  <files>scripts/verify-phase-25.ts</files>
  <action>
Append a trailing block comment titled
`── MANUAL/LIVE VERIFICATION RUNBOOK (not automated — requires the live Coolify host, the live Supabase project, and real paid generations) ──`
with exactly these 8 numbered steps. Every step must state a copy-pasteable command or SQL query
AND its exact expected result. The literal string `MANUAL/LIVE VERIFICATION RUNBOOK` must appear
exactly ONCE in the file (24-07 hit a header-comment wording collision here — check before
committing).

  1. **NARRATIVE + ON-SLIDE TEXT (CRSL2-01, CRSL2-02, SC1).** Generate one real 5-slide carousel
     with a text-style selected and `use_logo` on. Then:
     `select slide_number, base_image_url is not null as has_base, typography_meta->>'layout_archetype_id' as archetype, jsonb_array_length(typography_meta->'text_blocks') as blocks from post_slides where post_id='<id>' order by slide_number;`
     Expect 5 rows; every `has_base` = true; every `archetype` IDENTICAL across all 5 rows
     (SC1's "layout archetype held consistent"); every `blocks` >= 1. Then
     `select generation_params->>'text_style_ids', generation_params->>'use_logo', generation_params->>'logo_position' from posts where id='<id>';`
     Expect the creator's actual selections, not defaults. Visually confirm each slide shows
     crisp, correctly-placed text and the logo in the chosen corner on EVERY slide.

  2. **VISUAL COMPOSITION VARIATION (CRSL2-01, SC2).** For the same carousel, visually confirm
     the 5 slides show genuinely different framing — a wide establishing hook, varied
     detail/overhead/over-the-shoulder content slides, a clean CTA framing — not five crops of
     the same scene with different text. Then grep the server log for
     `[carousel] composition variation warning:`. Expect it ABSENT. If present, note which slide
     pair and its similarity score: the automated `composition_note` check fired, meaning the
     model reused a framing. This step is the human half of SC2; the automated half is
     `[svc-carousel-narrative]`'s `findDuplicateCompositionNotes` coverage.

  3. **TEXT-STYLE TREATMENT + LOGO (CRSL2-04, SC3).** Generate two carousels from the SAME prompt
     and slide count, one with `bold-promo` selected and one with `elegant-serif`. Then for each:
     `select slide_number, typography_meta->'fonts' from post_slides where post_id='<id>' order by slide_number limit 1;`
     Expect the two carousels' `fonts[].alias` and/or `size_px` to DIFFER, and the highlight
     block's rendered text to be visibly heavier/uppercased in the `bold-promo` run. Visually
     confirm the logo appears in the configured corner on every slide of both.
     `textStyleIds` reaching the compositor is the whole point of CRSL2-04 — before Phase 25 it
     was read by nothing.

  4. **SLIDE EDIT — NO DOUBLE RENDER (CRSL2-02, plan 25-13).** Open the carousel from step 1 and
     edit slide 2 with a purely visual instruction (e.g. "make the background warmer"). Expect
     the result to show ONE crisp set of text, never ghosted or doubled. Then:
     `select version_number, base_image_url is not null as has_base, typography_meta->>'layout_archetype_id' as archetype from post_slide_versions where post_slide_id='<slide 2 id>' order by version_number desc limit 1;`
     Expect `has_base` = true and `archetype` EQUAL to step 1's carousel-level value. Then repeat
     with a text-only edit (change only the Text step): expect the SSE stream to show
     "Recomposing slide text..." and the server log to show NO image-provider edit call for that
     request — the compositor-only fast path.

  5. **LEGACY SLIDE EDIT (plan 25-13's LEGACY branch).** Pick a carousel generated BEFORE this
     deploy: `select p.id, s.id from posts p join post_slides s on s.post_id=p.id where p.content_type='carousel' and s.base_image_url is null limit 1;`
     Edit one of its slides. Expect it to behave exactly as it did pre-Phase-25 — the edit
     succeeds, no crop, no compositor re-render, no lockout — and the new row keeps
     `base_image_url IS NULL`:
     `select version_number, base_image_url from post_slide_versions where post_slide_id='<legacy slide id>' order by version_number desc limit 1;`
     A non-null value here means the LEGACY branch was bypassed and pre-migration slides are
     being silently re-composited.

  6. **AESTHETIC DNA IN THE PAYLOAD (PLAN-05, PLAN-06, SC4).** Generate single-image posts across
     3 different style/mood combinations. For each, confirm in the server log's planning-prompt
     payload that (a) the selected style's exact `art_direction.photography_type` string appears
     VERBATIM, (b) the sentence `Apply a 60-30-10 color balance:` appears and its 10% clause names
     the brand's `color_4` hex (not `color_3`), and (c) the `Avoid: ` negative block is present.
     Then visually confirm the three outputs show recognizably different photography type,
     lighting and color proportion — SC4's "recognizable, specific" bar, which no static check
     can judge.

  7. **STYLE REFERENCE BOARD ATTACHMENT (PLAN-07, SC5).** As an admin, open the Post Creation tab
     → Style Reference Boards card, upload 2 images to one style's board, and confirm they
     persist across a page reload WITHOUT pressing "Save Post Settings" (the card owns its own
     immediate-persist mutations). Then generate one single-image post AND one carousel with
     that style selected. Expect the log lines
     `[Reference Images] Post <id>: ... + N style-board = N/4 slots` and
     `[carousel] reference images: ... + N style-board = N/4` with N > 0 in both. Confirm the
     outputs visibly reflect the board's aesthetic. Finally confirm the ACL: as a NON-admin user,
     `POST /api/admin/style-reference-photos` and `DELETE /api/admin/style-reference-photos/:id`
     both return 403.

  8. **NO-REGRESSION SWEEP.** Generate one video (the frozen GATE-08 path), one product
     enhancement, and one single-image post; edit one pre-Phase-25 single-image post. Expect all
     four to succeed unchanged, with no `style-board` slots attached to the video
     (`[Reference Images] ... style-board = 0/4`). Then confirm the admin catalog round-trip:
     edit a style's Photography type in the admin tab, press "Save Post Settings", reload, and
     confirm the value persisted — then confirm a style whose `art_direction` is entirely empty
     still serves the curated default on the next read (the `withDefaultArtDirection` backfill).

  If any step fails, record it in 25-14-SUMMARY.md and do NOT mark Phase 25 complete.

Also run the full regression sweep now and record the numbers in the SUMMARY:
`npx tsx scripts/verify-phase-25.ts`, the four Phase 25 unit harnesses,
`verify-phase-21.ts`, `verify-phase-21.1.ts`, `verify-phase-22.ts`, `verify-phase-23.ts`,
`verify-phase-24.ts`, `verify-golden-image.ts`, `npm run check`, `npm run build`.
  </action>
  <verify>
    <automated>npx tsx scripts/verify-phase-25.ts && npx tsx scripts/test-carousel-narrative-plan.ts && npx tsx scripts/test-style-reference-merge.ts && npx tsx scripts/test-typography-treatment.ts && npx tsx scripts/test-slide-edit-resolution.ts && npm run check && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'MANUAL/LIVE VERIFICATION RUNBOOK' scripts/verify-phase-25.ts` returns exactly 1
    - the runbook contains 8 numbered steps, each with at least one copy-pasteable command or SQL query
    - every one of 25-VALIDATION.md's 4 "Manual-Only Verifications" rows maps to at least one runbook step (1+2 → composition variation; 1 → on-slide text/narrative; 6 → aesthetic DNA; 7 → reference boards)
    - `npx tsx scripts/verify-phase-25.ts` exits 0 (full suite, >= 60 checks, zero weakened)
    - all four Phase 25 unit harnesses exit 0
    - `verify-phase-21.ts`, `verify-phase-21.1.ts`, `verify-phase-22.ts`, `verify-phase-23.ts`, `verify-phase-24.ts`, `verify-golden-image.ts` all exit 0
    - `npm run check` exits 0 and `npm run build` exits 0
    - `git diff --stat` still shows `scripts/verify-phase-25.ts` as the ONLY changed file
  </acceptance_criteria>
  <done>The authoritative live runbook lives next to the harness it complements, and the whole static gate plus every prior-phase gate is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator sign-off on the live runbook</name>
  <files>.planning/phases/25-narrative-carousels-and-aesthetic-dna/25-14-SUMMARY.md</files>
  <read_first>
    - scripts/verify-phase-25.ts (the trailing MANUAL/LIVE VERIFICATION RUNBOOK block from Task 2)
    - .planning/phases/25-narrative-carousels-and-aesthetic-dna/25-05-SUMMARY.md (the exact curated `photography_type` strings the operator will grep for in runbook step 6)
    - .planning/phases/25-narrative-carousels-and-aesthetic-dna/25-13-SUMMARY.md (the LEGACY-branch behavior runbook step 5 checks)
  </read_first>
  <action>
STOP and hand control to the operator. Do NOT attempt to run the live runbook yourself — every
step requires the live Coolify host, the live Supabase project, and real paid generations.

Before pausing, print to the operator:
  1. The absolute path of the runbook: `scripts/verify-phase-25.ts` (trailing block comment).
  2. Task 2's static-gate result: the full-suite check count plus the six prior-phase harness
     results and the four Phase 25 unit-harness results.
  3. The two migrations that must be applied to the live Supabase project BEFORE step 1
     (`20260729000000_post_slides_base_image_typography.sql`,
     `20260729000001_style_reference_photos.sql`) — the runbook is meaningless against a
     database missing `post_slides.base_image_url` or the `style_reference_photos` table.
  4. The exact `photography_type` string for the style the operator should use in step 6, taken
     from 25-05-SUMMARY.md, so the grep has a stated expectation rather than an improvised one.
  5. That the parent post's `generation_params` is the source of truth step 1 and step 3 query,
     and that a carousel generated before this deploy will NOT have it.

After the operator responds, transcribe their per-step results verbatim into 25-14-SUMMARY.md as
an 8-row table (step / expected / observed / PASS-FAIL). If any step failed, record it and leave
Phase 25 open.
  </action>
  <what-built>
    The complete Phase 25 carousel + aesthetic-DNA upgrade: a narrative master plan with
    server-assigned hook/content/CTA roles, per-slide `composition_note` variation with an
    automated similarity check, per-slide `text_blocks` and ONE carousel-level layout archetype,
    strict dual-dialect structured output on the planning-tier model, the full Phase 23
    crop → typography → logo pipeline running per slide with the creator's text-style treatment
    and logo choices, a typography-aware slide-edit path with a LEGACY branch, dense
    admin-curated art direction on all 9 styles and 12 post moods with a read-time backfill, a
    60-30-10 color rule that finally uses `color_4`, a global anti-AI-look negative block, and
    platform-curated style reference boards with a three-tier priority merge feeding both
    generation paths. Static gate: `npx tsx scripts/verify-phase-25.ts` green across 8 tags,
    plus zero regression on Phases 21/21.1/22/23/24 and the golden-image CI gate.
  </what-built>
  <how-to-verify>
    Execute all 8 steps of the MANUAL/LIVE VERIFICATION RUNBOOK embedded at the bottom of
    `scripts/verify-phase-25.ts`, in order, against the live Coolify host. Apply both Phase 25
    migrations first. Steps 1, 3, 4, 5 and 7 each need a real generation plus the SQL quoted
    inline in the runbook.

    The three that matter most, if time is short:
      - Step 4 (slide edit): the edited slide must show ONE set of crisp text. Ghosted or doubled
        text means the edit path is still hitting the flattened image — the exact regression
        25-CONTEXT.md forbids.
      - Step 5 (legacy slide edit): a pre-Phase-25 slide must still edit, and its new version row
        must keep `base_image_url IS NULL`.
      - Step 2 (composition variation): five slides that differ only in their text are a failed
        narrative, even with every automated check green.
  </how-to-verify>
  <acceptance_criteria>
    - Operator has run all 8 runbook steps and recorded the observed result for each in 25-14-SUMMARY.md
    - Steps 2, 4 and 5 explicitly PASS (or a deviation is recorded with the raw evidence)
    - The operator states, for step 1, whether all 5 slides reported the SAME `layout_archetype_id`
    - The operator states, for step 7, the observed style-board slot counts for both the
      single-image and the carousel generation
  </acceptance_criteria>
  <resume-signal>Type "approved" to close Phase 25, or paste the failing step number plus its observed output.</resume-signal>
  <done>A human has confirmed against real traffic what no static check can prove: the carousel reads as a narrative with visibly varied framing, its slides carry correct composited text, editing a slide does not double-render, legacy slides still edit, the aesthetic DNA is recognizable, and style reference boards actually reach the model.</done>
</task>

</tasks>

<verification>
- `npx tsx scripts/verify-phase-25.ts` exits 0 across all 8 tags with zero weakened checks
- `scripts/test-carousel-narrative-plan.ts`, `scripts/test-style-reference-merge.ts`, `scripts/test-typography-treatment.ts`, `scripts/test-slide-edit-resolution.ts` all exit 0
- `verify-phase-21.ts`, `verify-phase-21.1.ts`, `verify-phase-22.ts`, `verify-phase-23.ts`, `verify-phase-24.ts`, `verify-golden-image.ts` all exit 0
- `npm run check` and `npm run build` both exit 0
- Operator sign-off recorded for all 8 runbook steps
</verification>

<success_criteria>
Phase 25's static gate is fully green including cross-file invariants no single plan could assert, no prior phase or CI gate regressed, the live runbook is embedded and authoritative, and an operator has verified the narrative carousel, its composited slide text, its framing variation, the no-double-render slide edit, the legacy edit path, the aesthetic DNA payload, and the style reference board attachment.
</success_criteria>

<output>
After completion, create `.planning/phases/25-narrative-carousels-and-aesthetic-dna/25-14-SUMMARY.md` with the per-step runbook results table and the final check counts. Phase 25 is NOT complete until Task 3 is signed off.
</output>
</content>
</invoke>
