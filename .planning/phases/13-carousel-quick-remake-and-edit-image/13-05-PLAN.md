---
phase: 13-carousel-quick-remake-and-edit-image
plan: 05
type: execute
wave: 4
depends_on: ["13-02", "13-03", "13-04"]
files_modified:
  - client/src/lib/translations.ts
  - scripts/verify-phase-13.ts
  - .planning/phases/13-carousel-quick-remake-and-edit-image/13-UAT.md
autonomous: false
requirements: [CRSL-EDIT-06]
must_haves:
  truths:
    - "All new EN strings introduced in Plans 13-03 and 13-04 have PT and ES translations"
    - "scripts/verify-phase-13.ts covers provider parity (Gemini and OpenAI both reach provider.edit with additionalRefs)"
    - "A UAT script (13-UAT.md) documents the exact steps to verify CRSL-EDIT-01..06 against a live deployment with both providers"
    - "Operator runs the UAT once per provider and records PASS/FAIL"
  artifacts:
    - path: "client/src/lib/translations.ts"
      provides: "PT and ES translations for Phase 13 strings"
      contains: "Editing slide 1 may affect"
    - path: "scripts/verify-phase-13.ts"
      provides: "Provider-parity static checks + summary report"
      contains: "GeminiImageProvider"
    - path: ".planning/phases/13-carousel-quick-remake-and-edit-image/13-UAT.md"
      provides: "Human-executable UAT script for live carousel slide edit + quick remake on both providers"
      min_lines: 40
  key_links:
    - from: "client/src/lib/translations.ts"
      to: "client/src/components/post-edit-dialog.tsx (Plan 13-03) + post-viewer-dialog.tsx (Plan 13-04)"
      via: "i18n keys: 'Editing slide 1...', 'Edit slide {n}', 'Slide remade', 'Slide updated', 'Slide {n} updated.', 'Slide not ready', etc."
      pattern: "Edit slide \\{n\\}"
---

<objective>
Close out Phase 13 with internationalization, provider-parity verification, and a structured UAT script. Adds PT + ES translations for every EN string introduced in 13-03 and 13-04. Extends `scripts/verify-phase-13.ts` with a provider-parity static check (asserts the route uses `getActiveImageProvider` and works for both `GeminiImageProvider` and `OpenAIImageProvider`). Produces `13-UAT.md` — a human-executable UAT checklist that exercises CRSL-EDIT-01..06 against a real Supabase + AI keys deployment, run once per provider.

Purpose: Verifies provider abstraction parity (CRSL-EDIT-06), gives the user a single document to validate the whole phase, and ensures no English-only strings ship to production.
Output: Modified translations file, expanded verify script, new UAT document.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-carousel-quick-remake-and-edit-image/13-RESEARCH.md
@.planning/phases/13-carousel-quick-remake-and-edit-image/13-02-SUMMARY.md
@.planning/phases/13-carousel-quick-remake-and-edit-image/13-03-SUMMARY.md
@.planning/phases/13-carousel-quick-remake-and-edit-image/13-04-SUMMARY.md

@client/src/lib/translations.ts
@scripts/verify-phase-13.ts
@server/routes/carousel.routes.ts
@server/services/image-provider.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PT + ES translations for Phase 13 strings</name>
  <files>client/src/lib/translations.ts</files>
  <action>
Open `client/src/lib/translations.ts` and confirm its dictionary structure (the project uses a flat EN-keyed dict where the EN string IS the key, with PT and ES sub-dictionaries — established in Phase 09-01).

Enumerate ALL English strings introduced by Plans 13-03 and 13-04. Confirm the list against the 13-03 and 13-04 SUMMARYs; the canonical set (add any others discovered during implementation):

| EN key | PT | ES |
|---|---|---|
| `Editing slide 1 may affect the visual style of the rest of the carousel.` | `Editar o slide 1 pode afetar o estilo visual do restante do carrossel.` | `Editar la diapositiva 1 puede afectar el estilo visual del resto del carrusel.` |
| `Edit slide {n}` | `Editar slide {n}` | `Editar diapositiva {n}` |
| `Editing slide 1` | `Editando o slide 1` | `Editando la diapositiva 1` |
| `Slide 1 sets the visual style for the rest of the carousel. Edits may cause visual drift in other slides.` | `O slide 1 define o estilo visual do restante do carrossel. Edições podem causar variações nos outros slides.` | `La diapositiva 1 define el estilo visual del resto del carrusel. Las ediciones pueden causar variaciones en las otras diapositivas.` |
| `Slide not ready` | `Slide não está pronto` | `La diapositiva no está lista` |
| `Please wait for slides to load.` | `Aguarde os slides carregarem.` | `Espera a que se carguen las diapositivas.` |
| `Cannot quick remake` | `Não é possível refazer rapidamente` | `No se puede rehacer rápidamente` |
| `Original generation prompt not available.` | `O prompt original de geração não está disponível.` | `El prompt original de generación no está disponible.` |
| `Remaking slide...` | `Refazendo slide...` | `Rehaciendo diapositiva...` |
| `Slide remade` | `Slide refeito` | `Diapositiva rehecha` |
| `Slide {n} updated.` | `Slide {n} atualizado.` | `Diapositiva {n} actualizada.` |
| `Quick remake failed` | `Falha ao refazer rapidamente` | `Falló la rehecho rápida` |
| `Slide updated` | `Slide atualizado` | `Diapositiva actualizada` |
| `Slide {n} edited.` | `Slide {n} editado.` | `Diapositiva {n} editada.` |

Add each entry to the PT and ES dictionaries following the file's existing pattern. If any of these EN strings already exists in the dictionary from an earlier phase, do not duplicate — just confirm.

If the executor implementing Plan 13-03/13-04 introduced additional EN strings not in this table, add them too with translations. The rule: zero EN strings ship without PT+ES counterparts.

Note on `{n}` placeholders: the project uses `t(key).replace('{n}', String(value))` per STATE.md decision (Phase 10-04). Preserve the literal `{n}` token in PT and ES — do NOT translate the placeholder itself.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('client/src/lib/translations.ts','utf8');const keys=['Editing slide 1 may affect','Edit slide {n}','Slide remade','Slide updated','Slide {n} updated.','Slide {n} edited.','Remaking slide...','Slide not ready'];const missing=keys.filter(k=>!s.includes(k));if(missing.length)throw new Error('Missing: '+missing.join(' | '));console.log('OK — all keys present');"</automated>
  </verify>
  <done>All Phase 13 EN strings exist in PT and ES dictionaries. Verify script confirms presence of every key.</done>
</task>

<task type="auto">
  <name>Task 2: Add provider-parity check to scripts/verify-phase-13.ts</name>
  <files>scripts/verify-phase-13.ts</files>
  <action>
Extend `scripts/verify-phase-13.ts` (originally scaffolded in Plan 13-01, populated by Plan 13-02) with one final static check:

**CRSL-EDIT-06 — Provider abstraction parity**:
- Read `server/routes/carousel.routes.ts` as text.
- Inside the `/api/carousel/slide/edit` route window (from `'/api/carousel/slide/edit'` literal to the next `router.post(` or end-of-file):
  - Assert it calls `getActiveImageProvider(`.
  - Assert it does NOT directly import or instantiate `GoogleGenerativeAI`, `OpenAI`, `@google/genai`, or call any `gemini-` / `gpt-image-` model identifier as a string literal outside the style-catalog lookup.
  - Assert it reads `imageApiKey` (or equivalent) conditionally based on `provider.name === "openai"`.
- Additionally read `server/services/image-provider.ts` and assert both `GeminiImageProvider` and `OpenAIImageProvider` export an `edit` method whose signature accepts `additionalRefs?: ReferenceImage[]` (regex match on the file is sufficient — the implementations already exist from Phase 12).
- PASS if all conditions hold; FAIL otherwise.

Final summary line: print the active count (now 7 checks total: 3 for CRSL-EDIT-01, 1 each for -03, -04, -05, -06). Exit 0 if all PASS, 1 otherwise.

Update the script header comment to reflect the full requirement coverage: `// Phase 13 verify — CRSL-EDIT-01..06`. CRSL-EDIT-02 (Edit Image button visible/functional) and CRSL-EDIT-07 (1 credit per slide edit at runtime) are intentionally not covered here — they require live UI / live billing observation and are covered by the UAT script in Task 3.
  </action>
  <verify>
    <automated>npx tsx scripts/verify-phase-13.ts</automated>
  </verify>
  <done>scripts/verify-phase-13.ts reports 7 active checks; on a tree with Plans 13-01 through 13-04 merged and the migration applied, all 7 PASS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Author and execute 13-UAT.md against a live deployment</name>
  <what-built>
End-to-end Phase 13 functionality: per-slide carousel edit + quick remake, with provider abstraction supporting both Gemini and OpenAI.
  </what-built>
  <how-to-verify>
**Pre-step (Claude must do first — autonomous):** Write `.planning/phases/13-carousel-quick-remake-and-edit-image/13-UAT.md` with the following structure:

```markdown
# Phase 13 — UAT Script

**Goal:** Verify CRSL-EDIT-01..07 end-to-end on a live deployment with both image providers.

## Pre-flight
- [ ] Migration `20260518000000_post_slide_versions.sql` applied via Supabase dashboard SQL editor.
- [ ] `npm run dev` running locally OR deployment is reachable.
- [ ] At least one carousel post (≥3 slides) exists in your account; if not, generate one via the creator dialog.
- [ ] `npx tsx scripts/verify-phase-13.ts` returns 7/7 PASS.

## Provider A: Gemini (default)
1. Confirm admin Image Provider setting = Gemini in `/admin`.
2. Open a carousel post in the gallery → Post Viewer opens, slides load.
3. Navigate to slide 2 (or 3) using the existing slide chevrons.
4. Click **Edit Image** → PostEditDialog opens with title "Edit slide 2/3".
   - [ ] Only ONE step is shown ("Edit Goal").
   - [ ] No "Text on Image" step appears.
   - [ ] No slide-1 drift warning banner.
5. Type an edit goal (e.g. "Change the background to a marble countertop"), submit.
   - [ ] SSE progress events render.
   - [ ] On complete, the slide in the viewer updates without a full reload.
   - [ ] Toast "Slide updated — Slide N edited." appears.
6. In Supabase, query `select * from post_slide_versions where post_slide_id = '<the slide_id>'`.
   - [ ] One row exists with `version_number = 1`.
7. Navigate to slide 1. Click **Edit Image**.
   - [ ] Toast warning fires: "Slide 1 sets the visual style for the rest of the carousel..."
   - [ ] Dialog opens with persistent in-dialog yellow warning banner.
8. Submit an edit. Verify slide 1's image updates locally.
9. Click **Quick Remake** on slide 2 (or any slide).
   - [ ] QuickRemakeGeneratingState overlay appears (or progress text shows "Remaking slide...").
   - [ ] On complete, slide image refreshes; toast "Slide remade — Slide N updated." appears.
10. Check credit balance: should have decreased by EXACTLY 3× single-edit cost (steps 5, 8, 9).

## Provider B: OpenAI
1. Toggle Image Provider to OpenAI in `/admin`. Confirm your OpenAI API key is set.
2. Repeat steps 3–9 from Provider A.
3. CRITICAL: confirm the OpenAI provider receives the slide-1 anchor for slide 2 edits — inspect server logs for "additionalRefs" presence in the Responses API request.

## Regression: non-carousel posts
1. Open an image post. Click Edit Image → still shows TWO steps (Edit Goal + Text on Image). Submit → /api/edit-post call (not /api/carousel/slide/edit).
2. Click Quick Remake on an image post → /api/edit-post with source="quick_remake".

## Sign-off
- [ ] Provider A (Gemini): PASS / FAIL
- [ ] Provider B (OpenAI): PASS / FAIL
- [ ] Regression non-carousel: PASS / FAIL
- [ ] Date/operator:
```

**Then prompt the user (human-verify gate):**
"Phase 13 UAT script is ready at `.planning/phases/13-carousel-quick-remake-and-edit-image/13-UAT.md`. Please:
1. Apply the migration (`supabase/migrations/20260518000000_post_slide_versions.sql`) via Supabase dashboard if not already done.
2. Run `npx tsx scripts/verify-phase-13.ts` and confirm 7/7 PASS.
3. Execute the UAT script for Provider A (Gemini) and Provider B (OpenAI).
4. Report PASS/FAIL per provider in this thread."
  </how-to-verify>
  <resume-signal>Type "approved" with PASS for both providers, or describe specific failures (e.g., "OpenAI slide 2 edit drifted from carousel style — additionalRefs may not be reaching the provider").</resume-signal>
</task>

</tasks>

<verification>
- Task 1 verified by the EN/PT/ES key presence check.
- Task 2 verified by `npx tsx scripts/verify-phase-13.ts` returning 7/7 PASS.
- Task 3 verified by human running the UAT script against both providers and reporting back.
</verification>

<success_criteria>
- All Phase 13 EN strings have PT + ES translations.
- scripts/verify-phase-13.ts covers CRSL-EDIT-01, -03, -04, -05, -06 via static checks (5 active requirement IDs across 7 numbered checks).
- 13-UAT.md exists with concrete steps for both providers + regression checks.
- Human UAT records PASS for both providers (or a logged failure narrative driving a `--gaps` follow-up plan).
</success_criteria>

<output>
After completion, create `.planning/phases/13-carousel-quick-remake-and-edit-image/13-05-SUMMARY.md` documenting: i18n keys added, final verify script check count, UAT outcomes for both providers, any defects discovered (file as gaps for `/gsd:plan-phase 13 --gaps` if needed).
</output>
