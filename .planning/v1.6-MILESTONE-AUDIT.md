---
milestone: v1.6
audited: 2026-07-28T18:29:11Z
status: tech_debt
scores:
  requirements: 40/40
  phases: 7/7
  integration: 7/7
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 21-openrouter-gateway-foundation
    items:
      - "GATE-04/GATE-07: admin gateway-routing/fallback-chain endpoints (/api/admin/ai-gateway-routing, /api/admin/ai-model-fallbacks) are fully functional server-side but have NO consuming client UI anywhere in client/src — the emergency no-deploy rollback GATE-07 exists for currently requires curl/Postman/direct-Supabase-edit instead of an admin panel toggle. Explicitly deferred to 'Phase 26 polish territory' in 21-06-SUMMARY.md but never actually built in Phase 26."
      - "Dead code: OpenAIImageProvider (~70 lines in image-provider.ts) is unreachable through the normal factory since resolveImageProviderName() only returns 'gemini'|'openrouter'. Corresponding provider.name === 'openai' branches in generate.routes.ts/edit.routes.ts are also unreachable. Explicitly flagged as an open question in 21-CONTEXT.md ('delete now or in Phase 26 cleanup') and never resolved."
  - phase: 24-visual-critic-and-re-roll
    items:
      - "Critic coverage (CRIT-01..05) only applies to /api/generate's image branch — edit.routes.ts (including 'quick remake', which calls provider.edit() to produce a functionally-new AI image) and carousel-generation.service.ts have NO critic/re-roll gate at all. Neither 24-CONTEXT.md nor 25-CONTEXT.md explicitly documents this as a deliberate scope narrowing (unlike the logo-contrast-treatment exclusion, which IS explicitly flagged as deferred) — CONTEXT.md's own 'Phase Boundary' text says 'Every generated base image is scored', broader than what shipped. Not a broken requirement (REQUIREMENTS.md scopes CRIT-01..05 to Phase 24 only, and neither CRSL2-01/02/04 nor edit's requirements ever asked for critic coverage) — but a real, unflagged quality-gate gap for two AI-image-generating surfaces."
  - phase: 25-narrative-carousels-and-aesthetic-dna
    items:
      - "Text-style visual treatment (resolveTypographyTreatment, weight/size/case/tracking) is wired into carousels only — single-image generate/edit always default to IDENTITY_TYPOGRAPHY_TREATMENT regardless of text_style_ids. Legitimate scope boundary (CRSL2-04 is carousel-only per REQUIREMENTS.md) but produces a real UX inconsistency: the same text-style selection looks visually different on a carousel vs. a single image."
  - phase: 26-fixes-and-polish
    items:
      - "Two applyLogoOverlay call sites (carousel-generation.service.ts:677, carousel.routes.ts:1209) pre-collapse logo_position to 'bottom-right' before calling the overlay, defeating POL-03's auto-corner-selection gate for carousels. Explicitly documented and accepted as out-of-scope in 26-fixes-and-polish/deferred-items.md per 26-07-PLAN.md's own instruction — not a silent gap."
      - "All 7 phases (21, 21.1, 22, 23, 24, 25, 26) carry an outstanding operator live-verification checkpoint requiring a real Coolify production host, live Supabase project, a funded OPENROUTER_API_KEY, and real paid AI generations — none reachable from this execution environment. Each is fully tracked in its own {phase}-HUMAN-UAT.md with the exact runbook steps. This is the single largest category of outstanding work before the milestone can be considered fully closed-out in production, though it does not represent a code-level gap (100% of automatable/code-level must-haves passed in every phase's VERIFICATION.md)."
nyquist:
  compliant_phases: [21, "21.1", 22, 23, 24, 25, 26]
  partial_phases: []
  missing_phases: []
  overall: "COMPLIANT — all 7 phases have VALIDATION.md with nyquist_compliant: true"
---

# v1.6 Milestone Audit — Professional Design Quality Overhaul + OpenRouter Gateway

**Audited:** 2026-07-28
**Status:** tech_debt (no blockers; real accumulated debt worth reviewing)

## Summary

All 7 phases of the v1.6 milestone are code-complete: 40/40 requirements mapped and satisfied at the code level, all cross-phase integration points wired correctly, the one traced E2E flow (single-image generation with references + reference board + text) works end-to-end with no dropped fields, and GATE-08 (video pipeline freeze) holds across the entire span — independently confirmed via a SHA-256 hash match against the pre-Phase-21 baseline.

No requirement is unsatisfied. No cross-phase wiring is broken. The milestone's own "definition of done" (as stated in ROADMAP.md's phase goals and REQUIREMENTS.md's traceability table) is met in code.

Two categories of outstanding work remain, neither of which blocks calling the milestone code-complete:

1. **Operator live-verification checkpoints** (all 7 phases) — requires real production infrastructure (Coolify host, live Supabase, funded OpenRouter key) not available in this execution environment. Fully tracked per-phase in `*-HUMAN-UAT.md` files with exact runbook steps.
2. **Accumulated tech debt** (4 items, listed above) — an unbuilt admin UI for an existing emergency-rollback API, one dead class, an unflagged critic-coverage scope narrowing, and a documented text-style-treatment scope boundary.

## Requirements Coverage (3-Source Cross-Reference)

All 40 requirements cross-referenced against REQUIREMENTS.md's traceability table, each phase's VERIFICATION.md requirements table, and each plan's SUMMARY.md `requirements-completed` frontmatter.

| Phase | Requirements | REQUIREMENTS.md Status | VERIFICATION.md Status | Final Status |
|-------|-------------|------------------------|------------------------|---------------|
| 21 | GATE-01..05,07,08, POL-01, POL-07, CRSL2-03 | Complete (10/10) | human_needed (6/6 code-verified) | **satisfied** |
| 21.1 | GATE-06 | Complete | human_needed (3/3 code-verified) | **satisfied** |
| 22 | PLAN-01..04 | Complete (4/4) | human_needed (5/5 code-verified) | **satisfied** |
| 23 | TYPO-01..07, POL-04, POL-05 | Complete (9/9) | human_needed (5/5 code-verified) | **satisfied** |
| 24 | CRIT-01..05 | Complete (5/5) | human_needed (31/32 code-verified; the 1 non-code item is the SAME live sign-off) | **satisfied** |
| 25 | PLAN-05..07, CRSL2-01,02,04 | Complete (6/6) | human_needed (6/6 code-verified) | **satisfied** |
| 26 | POL-02,03,06,08,09 | Complete (1) / Pending-code-complete (3) / Scheduled (1) | human_needed (5/5 code-verified) | **satisfied** — "Pending"/"Scheduled" labels reflect live-sign-off status, not code gaps |

**Orphan check:** 0 requirements present in REQUIREMENTS.md's traceability table are absent from a phase VERIFICATION.md. No orphans.

**FAIL gate check:** 0 requirements are `unsatisfied` at the code level. No requirement forces `gaps_found`.

## Cross-Phase Integration

Integration checker (gsd-integration-checker) independently traced 7 specific cross-phase wiring points plus one full E2E flow by reading live source, not trusting SUMMARY prose. Full report incorporated above (see `tech_debt` section for findings). No hard breaks found.

**Confirmed wired end-to-end:**
- `generate.routes.ts`'s full 9-stage pipeline (reference merge → planning → image-gen+critic/re-roll → crop → base-image persist → typography → adaptive logo → optimize@85 → idempotent persist), in the exact intended order.
- `edit.routes.ts`'s base-image-aware equivalent (no critic, by design).
- `carousel-generation.service.ts`'s parallel per-slide pipeline (crop → typography with shared archetype → logo, no critic).
- `ai-gateway.service.ts`'s `chatCompletion` used consistently by all 6 callers across Phases 21-25 with compatible `callClass`/`signal` optional params.
- GATE-08: video-generation.service.ts untouched since before Phase 21 started (git history) — SHA-256 hash matches the frozen baseline hardcoded in `verify-phase-21.ts`.

## Nyquist Compliance

All 7 phases have a `*-VALIDATION.md` with `nyquist_compliant: true` in frontmatter, produced during their respective planning passes and confirmed by the plan-checker before execution began. No phase requires a `/gsd:validate-phase` follow-up pass.

## Recommendation

Proceed to `/gsd:complete-milestone v1.6` — the milestone is code-complete with no blocking gaps. The accumulated tech debt items above should be captured (e.g., as backlog/seed items or a follow-up milestone) rather than gating this milestone's close, consistent with how POL-08's cost-reconciliation audit was already scoped as a scheduled, non-gating post-ship item.

The 7 outstanding operator-verification checkpoints remain the responsibility of whoever deploys this work to production — each is self-contained with an exact runbook in its phase's `*-HUMAN-UAT.md`.
