---
phase: 21.1
slug: affiliate-byok-migration
status: planned
nyquist_compliant: true
wave_0_complete: false
created: 2026-07-27
---

# Phase 21.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None (no jest/vitest) — custom `tsx`-executed static/functional verification scripts, same pattern as every prior phase (`scripts/verify-phase-{N}.ts`) |
| **Config file** | none — Wave 0 installs `scripts/verify-phase-21.1.ts` |
| **Quick run command** | `npx tsx scripts/verify-phase-21.1.ts` |
| **Full suite command** | `npx tsx scripts/verify-phase-21.1.ts && npm run check` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npx tsx scripts/verify-phase-21.1.ts` + `npm run check`
- **After every plan wave:** Same, plus `scripts/test-affiliate-key-resolution.ts` if created
- **Before `/gsd:verify-work`:** Full suite must be green; SC2's manual/live simulated-failure step AND the affiliate-video regression step executed at least once and documented (mirrors Phase 21's GATE-04/GATE-07 manual-step precedent)
- **Max feedback latency:** ~10 seconds

---

## Per-Task Verification Map

The harness supports `--only=<substring>` so every task gets a targeted, fast, PASSING signal while the full gate stays red until Wave 4.

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | Expected PASS | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|---------------|-------------|--------|
| 01-T1 harness + RED fixture | 01 | 1 | GATE-06 | static harness (Wave 0) | `npx tsx scripts/verify-phase-21.1.ts --only=self-test` | 1 | ❌ W0 | ⬜ pending |
| 01-T2 migration + profileSchema | 01 | 1 | GATE-06 (provisioning) | static | `npx tsx scripts/verify-phase-21.1.ts --only=foundation-schema && npm run check` | 5 | ❌ W0 | ⬜ pending |
| 01-T3 getOpenRouterApiKey + selectImageApiKey | 01 | 1 | GATE-06 (SC2/SC3 contract) | functional (tdd RED→GREEN) | `npx tsx scripts/test-affiliate-key-resolution.ts && npx tsx scripts/verify-phase-21.1.ts --only=foundation-resolver` | 9 + 5 | ❌ W0 | ⬜ pending |
| 02-T1 OpenRouterImageProvider | 02 | 2 | GATE-06 (billing attribution, image leg) | static | `npx tsx scripts/verify-phase-21.1.ts --only=svc-image-provider && npm run check` | 2 | ✅ (after 01) | ⬜ pending |
| 02-T2 GeminiService key param | 02 | 2 | GATE-06 (billing attribution, planning leg) | static | `npx tsx scripts/verify-phase-21.1.ts --only=svc-gemini && npm run check` | 5 | ✅ | ⬜ pending |
| 03-T1 caption-quality | 03 | 2 | GATE-06 | static | `npx tsx scripts/verify-phase-21.1.ts --only=svc-caption && npm run check` | 3 | ✅ | ⬜ pending |
| 03-T2 carousel-generation | 03 | 2 | GATE-06 | static | `npx tsx scripts/verify-phase-21.1.ts --only=svc-carousel && npm run check` | 3 | ✅ | ⬜ pending |
| 03-T3 enhancement | 03 | 2 | GATE-06 | static | `npx tsx scripts/verify-phase-21.1.ts --only=svc-enhancement && npm run check` | 3 | ✅ | ⬜ pending |
| 04-T1 generate.routes (gateway gate + GATE-08 video carve-out) | 04 | 3 | GATE-06 (route lockout fix, SC1/SC3) | static (`gateIsConditional` + video guard) | `npx tsx scripts/verify-phase-21.1.ts --only=route-generate && npm run check` | 4 | ✅ | ⬜ pending |
| 04-T2 edit.routes (gateway gate + GATE-08 video carve-out) | 04 | 3 | GATE-06 (route lockout fix) | static (`gateIsConditional` + video guard) | `npx tsx scripts/verify-phase-21.1.ts --only=route-edit && npm run check` | 4 | ✅ | ⬜ pending |
| 05-T1 carousel.routes (x2 handlers) | 05 | 3 | GATE-06 (route lockout fix) | static | `npx tsx scripts/verify-phase-21.1.ts --only=route-carousel && npm run check` | 4 | ✅ | ⬜ pending |
| 05-T2 enhance.routes | 05 | 3 | GATE-06 (route lockout fix) | static | `npx tsx scripts/verify-phase-21.1.ts --only=route-enhance && npm run check` | 3 | ✅ | ⬜ pending |
| 06-T1 transcribe.routes | 06 | 3 | GATE-06 (route lockout fix) | static | `npx tsx scripts/verify-phase-21.1.ts --only=route-transcribe && npm run check` | 3 | ✅ | ⬜ pending |
| 06-T2 posts.routes remake-caption | 06 | 3 | GATE-06 (7th call site) | static | `npx tsx scripts/verify-phase-21.1.ts --only=route-remake-caption && npm run check` | 2 | ✅ | ⬜ pending |
| 07-T1 Settings UI (add OpenRouter card, relabel Gemini video-only, drop OpenAI + provider radio) | 07 | 4 | GATE-06 (provisioning/rotation, SC1) | static | `npx tsx scripts/verify-phase-21.1.ts --only=ui-settings && npm run check` | 4 | ✅ | ⬜ pending |
| 07-T2 phase gate + runbook + scoped call-site grep | 07 | 4 | GATE-06 (all) | static + functional + regression | `npx tsx scripts/verify-phase-21.1.ts && npx tsx scripts/verify-phase-21.ts && npm run check` | all | ✅ | ⬜ pending |
| 07-T3 operator verification (incl. affiliate video regression) | 07 | 4 | GATE-06 (SC2 live + GATE-08 carve-out) | manual (checkpoint) | MISSING — operator-only, see Manual-Only Verifications | n/a | n/a | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

Route-tag PASS totals (20 route checks in all): generate 4, edit 4, carousel 4, enhance 3, transcribe 3, remake-caption 2.

---

## Wave 0 Requirements

All three are owned by **plan 21.1-01 (Wave 1)** — nothing else can start until they land.

- [ ] `scripts/verify-phase-21.1.ts` — 21.1-01 Task 1. Mirrors `verify-phase-21.ts` (`check(name, cond, detail)`, exit 1 on failure) plus three additions: an `--only=<substring>` filter for per-task feedback, a `readSafe()` that tolerates not-yet-created files, and a `gateIsConditional(src)` structural helper that proves the legacy `getGeminiApiKey` gate is inside an `else` branch (the Pitfall-1 lockout check a naive "does the new function exist" regex would miss). `gateIsConditional` is applied UNIFORMLY to all six route files — `transcribe.routes.ts` and `posts.routes.ts` never call `getGeminiApiKey(` at all, which the helper's `gemIdx < 0 → return true` branch already handles; no per-file special-casing.
- [ ] `scripts/test-affiliate-key-resolution.ts` — 21.1-01 Task 1 (written RED) → Task 3 (GREEN). No-network, no-Supabase fixture test of `getOpenRouterApiKey` (affiliate-with-key / affiliate-without-key exact SC3 string / no platform-key leak / non-affiliate / admin) and `selectImageApiKey` (Pitfall 3: `openrouter` must win over a non-empty Gemini key). Mirrors `scripts/test-openrouter-image-adapter.ts`.
- [ ] `supabase/migrations/20260727000000_profiles_openrouter_api_key.sql` — 21.1-01 Task 2. `ALTER TABLE profiles ADD COLUMN IF NOT EXISTS openrouter_api_key TEXT;` — additive, drops nothing. Application to the live DB is an operator step (21.1-07 Task 3 checkpoint), per the Phase 11 Supabase-native-migration convention.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live billing attribution round-trip | GATE-06 (SC2) | Requires a real, funded second OpenRouter account (the affiliate's) + a deliberately-invalid model slug to observe a clean failure with no platform-balance deduction — not CI-blocking, mirrors Phase 21's GATE-04/GATE-07 manual-live precedent | 1) Provision a real affiliate account with its own OpenRouter key in Settings. 2) Generate a post as that affiliate. 3) Confirm the `usage_events` row attributes cost with no platform-balance deduction. 4) Temporarily misconfigure the affiliate's model slug to force a failure; confirm the failure does not fall through to platform billing. |
| Affiliate video generation + editing still work (GATE-08 carve-out) | GATE-06 (SC1 no-lockout) | Video is a paid direct-Google Veo call on the affiliate's own Gemini key; no CI credentials and no gateway involvement, so only a live run proves the migration did not break a live billed feature | 1) With BOTH keys set, generate one video and edit one video post as the affiliate — both must succeed. 2) Clear ONLY the Gemini key; a video request must return a pre-SSE HTTP 400 JSON with exactly `Video generation requires a Gemini API key. Add yours in Settings → Gemini API key (used for video generation only).` 3) With the Gemini key still cleared, an IMAGE generation must still succeed (no-lockout proof). 4) Restore the Gemini key. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending — will be set by gsd-plan-checker during the verification loop

*Updated 2026-07-27 during `/gsd:plan-phase 21.1`: per-task map bound to the 7 real plans; `--only=<tag>` filter added to the harness contract so no task waits on a red full-gate for feedback.*

*Amended 2026-07-27 (revision pass, post-checker): CONTEXT.md's "Settings UI" decision was amended to KEEP the affiliate Gemini key field (relabeled video-only) because video is a GATE-08-frozen direct-Google path. Consequences reflected above — 04-T1/04-T2 gain a video-guard check each (3 → 4 expected PASS lines; commands unchanged), 07-T1's scope changed from "one card replaces three" to "add OpenRouter, relabel Gemini, drop OpenAI + provider radio" (command and expected PASS count unchanged at 4), 07-T2 gained a precisely-scoped un-migrated-call-site grep (command unchanged), 07-T3 gained the affiliate-video regression step, and a second Manual-Only Verification row was added. No task IDs were added, removed, or renumbered, and no `<automated>` command string changed.*
</content>
