---
phase: 21.1-affiliate-byok-migration
plan: 05
subsystem: api
tags: [openrouter, affiliate, byok, express, carousel, enhancement, gemini]

# Dependency graph
requires:
  - phase: 21.1-01
    provides: "getOpenRouterApiKey/selectImageApiKey in auth.middleware.ts, profiles.openrouter_api_key column, verify-phase-21.1.ts harness"
  - phase: 21.1-02
    provides: "OpenRouterImageProvider caller-key preference, GeminiService openRouterApiKey threading"
  - phase: 21.1-03
    provides: "openRouterApiKey?: string on CarouselGenerationParams and EnhancementParams, threaded into every internal text/caption call"
provides:
  - "carousel.routes.ts: both handlers (generate + slide-edit) gate affiliates on getOpenRouterApiKey instead of a bare getGeminiApiKey hard gate"
  - "enhance.routes.ts: single handler gates affiliates on getOpenRouterApiKey"
  - "openRouterApiKey threaded into generateCarousel(), enhanceProductPhoto(), and both image-provider.edit() call sites via selectImageApiKey"
affects: [21.1-06, 21.1-07, phase-22]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Affiliate-aware key gate: if (ownApiKey) resolve getOpenRouterApiKey else getGeminiApiKey — replaces the unconditional getGeminiApiKey hard gate in every remaining SSE route"
    - "selectImageApiKey({ providerName, geminiApiKey, openRouterApiKey, openaiApiKey }) replaces ad-hoc `let imageApiKey = geminiApiKey` / `let imageApiKey: string | undefined` patterns at every image call site"

key-files:
  created: []
  modified:
    - server/routes/carousel.routes.ts
    - server/routes/enhance.routes.ts

key-decisions:
  - "No video carve-out needed in either file — confirmed via grep that neither carousel nor enhancement calls generateVideo/enforceExactImageText, so geminiApiKey legitimately stays \"\" for affiliates in all three handlers"

patterns-established:
  - "Canonical affiliate-aware key gate (21.1-CONTEXT.md) applied verbatim across generate/edit/carousel/enhance routes for GATE-06"

requirements-completed: [GATE-06]

# Metrics
duration: 4min
completed: 2026-07-27
---

# Phase 21.1 Plan 05: Carousel + Enhancement Affiliate BYOK Gating Summary

**Both carousel handlers (generate + slide-edit) and the enhancement handler now gate affiliates on `getOpenRouterApiKey` and thread the resolved key into `generateCarousel`/`enhanceProductPhoto` and every `provider.edit`/image call site via `selectImageApiKey`.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-07-27T16:34:09Z
- **Completed:** 2026-07-27T16:37:43Z
- **Tasks:** 2 completed
- **Files modified:** 2

## Accomplishments
- `carousel.routes.ts` generate handler: profile SELECT extended with `openrouter_api_key`; unconditional Gemini gate replaced with the affiliate-aware branch; image key resolution now goes through `selectImageApiKey`; `openRouterApiKey` passed into `generateCarousel({...})`
- `carousel.routes.ts` slide-edit handler: same affiliate-aware gate pattern applied (dropping the old "Admin and affiliate accounts must configure their own Gemini API key..." 400/500 message); `selectImageApiKey` now selects the key for `provider.edit(...)`
- `enhance.routes.ts`: profile SELECT extended, affiliate-aware gate applied, `selectImageApiKey` wired into the image call site, `openRouterApiKey` passed into `enhanceProductPhoto({...})`
- Zero regressions: `verify-phase-21.ts` (43/43) and `verify-phase-21.1.ts --only=svc-*` (16/16 across image-provider/gemini/caption/carousel/enhancement) both still pass

## Task Commits

Each task was committed atomically:

1. **Task 1: carousel.routes.ts — both handlers (generate + slide-edit)** - `02b5ee4` (feat)
2. **Task 2: enhance.routes.ts — affiliate gate + key threading** - `f9c4eb3` (feat)

**Plan metadata:** (this commit) `docs: complete plan`

## Files Created/Modified
- `server/routes/carousel.routes.ts` - Affiliate-aware OpenRouter key gate + `selectImageApiKey`-based image key resolution in both the generate and slide-edit handlers; `openRouterApiKey` threaded into `generateCarousel()`
- `server/routes/enhance.routes.ts` - Affiliate-aware OpenRouter key gate + `selectImageApiKey`-based image key resolution; `openRouterApiKey` threaded into `enhanceProductPhoto()`

## Decisions Made
None beyond what 21.1-CONTEXT.md already locked — the canonical gate snippet from the plan's `<interfaces>` block was applied verbatim to both files, adapting only the profile variable name (`profile` vs `editProfile`) and each handler's existing error-response shape (400 for the generate handler and pre-SSE JSON errors in slide-edit; 400/500 split preserved for slide-edit's ownApiKey branch).

## Deviations from Plan

None - plan executed exactly as written. All acceptance-criteria greps and both harness runs (`--only=route-carousel` 4/4, `--only=route-enhance` 3/3) passed on the first attempt for both tasks.

## Issues Encountered

Parallel-execution git staging: `git status` showed other concurrently-running executor agents' files (`generate.routes.ts`, `edit.routes.ts`, `posts.routes.ts`) transiently staged in the shared index before each of my commits. Per the parallel-execution protocol, I ran `git add` scoped to only my own file (`carousel.routes.ts`, then `enhance.routes.ts`) and verified via `git diff --cached --name-only` that exactly one file was staged before each commit — no foreign files were ever included in either of my commits, and no `git reset` was needed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All three remaining SSE-streaming AI surfaces (carousel generate, carousel slide-edit, enhancement) now bill an affiliate's own OpenRouter key end-to-end for every planning/image/caption call, completing the route-level GATE-06 rollout alongside plan 21.1-04's generate/edit routes.
- No blockers. `npm run check` clean; `verify-phase-21.ts` and `verify-phase-21.1.ts` (route-carousel, route-enhance, svc-*) all green.
- Remaining GATE-06 surface for the phase: transcribe.routes.ts/posts.routes.ts (parallel plan 21.1-06) and any UI-facing settings work tracked separately.

---
*Phase: 21.1-affiliate-byok-migration*
*Completed: 2026-07-27*

## Self-Check: PASSED

- FOUND: server/routes/carousel.routes.ts
- FOUND: server/routes/enhance.routes.ts
- FOUND: .planning/phases/21.1-affiliate-byok-migration/21.1-05-SUMMARY.md
- FOUND: 02b5ee4 (Task 1 commit)
- FOUND: f9c4eb3 (Task 2 commit)
