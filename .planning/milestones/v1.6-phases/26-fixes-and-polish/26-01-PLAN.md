---
phase: 26-fixes-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/logo/make-logo-fixtures.ts
  - tests/fixtures/logo/logo-alpha-256.png
  - tests/fixtures/logo/logo-opaque-256.jpg
  - tests/fixtures/logo/quad-corners-1024.png
  - scripts/verify-phase-26.ts
autonomous: true
requirements: [POL-02, POL-03, POL-06, POL-08, POL-09]

must_haves:
  truths:
    - "Every Phase 26 requirement has a named, runnable check before any implementation lands"
    - "The harness runs to completion with zero uncaught exceptions even though none of plans 26-02..26-09 have landed"
    - "--only=self-test exits 0; every other tag is honestly RED and names the exact missing artifact"
    - "POL-03's functional tests have a real no-alpha JPEG logo, a real alpha PNG logo, and a base image whose four corners have deterministically different contrast"
  artifacts:
    - path: "scripts/verify-phase-26.ts"
      provides: "The 9-tag Phase 26 phase gate (8 tags now, [svc-cross-plan] added by 26-10)"
      contains: "[svc-webp-quality]"
      min_lines: 350
    - path: "tests/fixtures/logo/make-logo-fixtures.ts"
      provides: "Deterministic regenerator for the three logo fixtures"
      contains: "logo-opaque-256.jpg"
    - path: "tests/fixtures/logo/logo-opaque-256.jpg"
      provides: "No-alpha JPEG logo fixture (hasAlpha === false)"
    - path: "tests/fixtures/logo/logo-alpha-256.png"
      provides: "RGBA PNG logo fixture (hasAlpha === true)"
    - path: "tests/fixtures/logo/quad-corners-1024.png"
      provides: "Four-quadrant base image with deterministically distinct per-corner contrast"
  key_links:
    - from: "scripts/verify-phase-26.ts"
      to: "scripts/verify-webp-text-edge.ts + scripts/test-logo-overlay-contrast.ts + scripts/test-drawblocks-font-state.ts"
      via: "spawnSync invocation of the three unit harnesses later plans create"
      pattern: "spawnSync"
    - from: "tests/fixtures/logo/make-logo-fixtures.ts"
      to: "tests/fixtures/logo/*.png|jpg"
      via: "sharp create + toFile, idempotent"
      pattern: "sharp\\("
---

<objective>
Install the Phase 26 Wave 0 validation floor: the 9-tag phase gate `scripts/verify-phase-26.ts` and the three image fixtures POL-03's functional tests need.

Purpose: nothing in Phase 26 can be verified until this file exists. Every subsequent plan's `<verify>` block is a `--only=<tag>` invocation of this harness. Per the Phase 23/24/25 precedent, this harness must be honestly RED at creation time — every red check must name the exact not-yet-written file or symbol the owning plan is responsible for producing.
Output: a harness that exits 1 with a readable failure list today, and exits 0 once plans 26-02..26-09 land.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-fixes-and-polish/26-CONTEXT.md
@.planning/phases/26-fixes-and-polish/26-RESEARCH.md
@.planning/phases/26-fixes-and-polish/26-VALIDATION.md

<interfaces>
<!-- Structural precedent this harness MUST mirror. Executor should copy these shapes, not invent new ones. -->

From scripts/verify-phase-25.ts (the template — read it in full before writing anything):
```typescript
const failures: string[] = [];
const ok: string[] = [];
const onlyArg = process.argv.find((a) => /^--only=(.+)$/.test(a));
const only = onlyArg ? onlyArg.match(/^--only=(.+)$/)![1] : null;
function check(name: string, cond: boolean, detail = ""): void
function read(p: string): string
function exists(p: string): boolean
function readSafe(p: string): string   // "" instead of ENOENT for not-yet-written files
function tagActive(tag: string): boolean
function extractZodObjectBody(src: string, constName: string): string
function extractConstObjectLiteral(src: string, markerPrefix: string): string
function extractInterfaceBody(src: string, name: string): string
function windowAround(src: string, marker: string, radius: number): string
function findMigrationFile(suffix: string): string | null
// ...then: async function main() { ...checks... report... } ; main().catch(...)
```

From tests/fixtures/typography/make-fixtures.ts (the fixture-generator template):
```typescript
await sharp({ create: { width: SIZE, height: SIZE, channels: 3, background: { r, g, b } } })
  .png({ compressionLevel: 9 })
  .toFile(outPath);
```

From server/services/image-optimization.service.ts (targets of [svc-webp-quality] / [svc-logo-contrast]):
```typescript
const DEFAULT_IMAGE_QUALITY = 80;              // 26-02 makes this 85
export type LogoPosition = "top-left" | "top-center" | "top-right" | "middle-left" | "middle-center" | "middle-right" | "bottom-left" | "bottom-center" | "bottom-right";
export async function applyLogoOverlay(baseImageBuffer: Buffer, logoBuffer: Buffer, position?: LogoPosition): Promise<Buffer>;
```

From server/services/typography-compositor.service.ts (target of [svc-drawblocks-font-fix]):
```typescript
function drawBlocks(ctx: any, archetypeId: LayoutArchetypeId, region: {...}, layouts: BlockLayout[], treatment?: TypographyTreatment): void
// BUG: never assigns ctx.font — inherits layoutBlocks()'s last measured font.
export async function analyzeRegionContrast(baseBuffer: Buffer, region: { left: number; top: number; width: number; height: number }): Promise<RegionContrast>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Logo + corner-contrast fixtures</name>
  <files>tests/fixtures/logo/make-logo-fixtures.ts, tests/fixtures/logo/logo-alpha-256.png, tests/fixtures/logo/logo-opaque-256.jpg, tests/fixtures/logo/quad-corners-1024.png</files>
  <read_first>
    - tests/fixtures/typography/make-fixtures.ts (the generator convention to mirror exactly: flat sharp `create`, `compressionLevel: 9`, idempotent, `npx tsx` runnable, prints `wrote <name>`)
    - server/services/typography-compositor.service.ts lines 289-370 (`LUMINANCE_TEXT_SWITCH = 140`, `MIN_LUMINANCE_DELTA = 90`, `BUSY_STDEV_THRESHOLD = 55`, `MID_BAND_LOW = 90`, `MID_BAND_HIGH = 170`, `analyzeRegionContrast`) — the fixture luminance values MUST be chosen so `scrimNeeded` is deterministic per corner
  </read_first>
  <action>
Create `tests/fixtures/logo/make-logo-fixtures.ts`, a `npx tsx`-runnable, idempotent generator mirroring `tests/fixtures/typography/make-fixtures.ts` exactly (same header-comment style, same `OUT_DIR = path.resolve(process.cwd(), "tests/fixtures/logo")`, same per-file `console.log(\`wrote ${filename}\`)`). It writes exactly three files, then run it once and commit the three outputs alongside it.

1. `logo-alpha-256.png` — 256x256 RGBA. Fully transparent background with an opaque white filled circle of radius 96 centred at (128,128). Build it by rendering an SVG buffer through sharp (this repo's verified idiom):
   `Buffer.from('<svg width="256" height="256"><circle cx="128" cy="128" r="96" fill="#FFFFFF"/></svg>')` -> `sharp(svg).png({ compressionLevel: 9 }).toFile(...)`.
   This fixture MUST satisfy `(await sharp(buf).metadata()).hasAlpha === true`.

2. `logo-opaque-256.jpg` — 256x256 JPEG, no alpha channel. Same white circle but on a solid `#FFFFFF`-different background so the box artifact is detectable: flat `{ r: 236, g: 236, b: 236 }` field with an opaque `#111111` filled circle of radius 96 centred at (128,128), encoded `.jpeg({ quality: 92 })`. Produce it by compositing the SVG circle over a `sharp({ create: { width: 256, height: 256, channels: 3, background: { r: 236, g: 236, b: 236 } } })` base.
   This fixture MUST satisfy `(await sharp(buf).metadata()).hasAlpha === false` and `.channels === 3`.

3. `quad-corners-1024.png` — 1024x1024, four flat quadrants with deterministically distinct `analyzeRegionContrast` outcomes so automatic corner selection has one unambiguous winner. Build it as a raw RGB pixel buffer (`sharp(raw, { raw: { width: 1024, height: 1024, channels: 3 } })`) filled quadrant-by-quadrant, then `.png({ compressionLevel: 9 })`:
   - top-left  (x<512, y<512): flat `rgb(12,12,12)`   -> luminance ~12, stdev 0  -> scrimNeeded FALSE (the winner)
   - top-right (x>=512, y<512): flat `rgb(128,128,128)` -> mid band            -> scrimNeeded TRUE
   - bottom-left (x<512, y>=512): flat `rgb(250,250,250)` -> luminance ~250, textColor dark, |17-250|=233 >= 90, stdev 0, not mid band -> scrimNeeded FALSE, but stdev ties with top-left
   - bottom-right (x>=512, y>=512): deterministic checkerboard of `rgb(0,0,0)` / `rgb(255,255,255)` in 8x8 blocks -> stdev >> 55 -> scrimNeeded TRUE ("busy")
   Do NOT use randomness anywhere — the checkerboard must be a pure function of (x, y) so re-running the generator is byte-idempotent.

Add a trailing comment block in the generator naming which Phase 26 assertions depend on each fixture (`[svc-logo-contrast]`), so a future editor cannot change a luminance value without noticing what it breaks.
  </action>
  <acceptance_criteria>
    - `npx tsx tests/fixtures/logo/make-logo-fixtures.ts` exits 0 and prints exactly 3 `wrote ` lines
    - `node -e "require('sharp')('tests/fixtures/logo/logo-opaque-256.jpg').metadata().then(m=>{if(m.hasAlpha!==false||m.channels!==3)process.exit(1)})"` exits 0
    - `node -e "require('sharp')('tests/fixtures/logo/logo-alpha-256.png').metadata().then(m=>{if(m.hasAlpha!==true)process.exit(1)})"` exits 0
    - Re-running the generator leaves `git status --porcelain tests/fixtures/logo` empty (byte-idempotent)
    - `grep -c "Math.random" tests/fixtures/logo/make-logo-fixtures.ts` returns 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsx tests/fixtures/logo/make-logo-fixtures.ts &amp;&amp; git status --porcelain tests/fixtures/logo</automated>
  </verify>
  <done>Three committed fixtures exist with the asserted alpha/channel properties; the generator is deterministic and re-runnable.</done>
</task>

<task type="auto">
  <name>Task 2: verify-phase-26.ts — harness skeleton, self-test, and the three image-quality tags</name>
  <files>scripts/verify-phase-26.ts</files>
  <read_first>
    - scripts/verify-phase-25.ts (FULL file — copy its helper block, `--only` filter, `tagActive`, report block, and `main().catch()` tail verbatim in style)
    - scripts/verify-phase-24.ts lines 1-140 (the balanced-delimiter extraction helpers this repo re-copies per harness rather than sharing)
    - server/services/image-optimization.service.ts (FULL — 218 lines; the [svc-webp-quality] and [svc-logo-contrast] target)
    - server/services/typography-compositor.service.ts lines 425-570 (`layoutBlocks`, `drawBlocks` — the [svc-drawblocks-font-fix] target)
  </read_first>
  <action>
Create `scripts/verify-phase-26.ts` with the exact structural skeleton of `scripts/verify-phase-25.ts`: header comment block declaring ownership ("created by plan 26-01 — the Wave 0 Nyquist requirement; extended ONLY by plan 26-10, which adds the 9th tag `[svc-cross-plan]` and the trailing MANUAL/LIVE VERIFICATION RUNBOOK. Plans 26-02..26-09 are NOT permitted to edit this file — their job is to turn its red checks green."), the `failures`/`ok` arrays, `check`/`read`/`exists`/`readSafe`/`tagActive`, the four extraction helpers, `windowAround`, `findMigrationFile`, `async function main()`, the `=== Phase 26 verify ===` report block, and `main().catch(...)`.

Declare the tag list:
```typescript
const PHASE_26_TAGS = [
  "self-test",
  "svc-webp-quality",
  "svc-webp-edge-check",
  "svc-drawblocks-font-fix",
  "svc-logo-contrast",
  "svc-idempotency",
  "svc-quality-dashboard",
  "svc-cost-reconciliation-runbook",
  "svc-cross-plan",
];
```
(`svc-cross-plan` is declared here but has NO checks yet — plan 26-10 adds them. The self-test asserts the literal is present, exactly as 25-01 did.)

Load all target sources ONCE via `readSafe` at module scope: `server/services/image-optimization.service.ts`, `server/services/typography-compositor.service.ts`, `server/routes/generate.routes.ts`, `server/routes/edit.routes.ts`, `shared/schema.ts`, `server/routes/posts.routes.ts`, `server/routes/admin-quality.routes.ts`, `server/routes/index.ts`, `client/src/components/post-creator-dialog.tsx`, `client/src/components/post-edit-dialog.tsx`, `client/src/components/post-viewer-dialog.tsx`, `client/src/pages/posts.tsx`, `client/src/pages/admin.tsx`, `client/src/components/app-sidebar.tsx`, `client/src/lib/quick-remake.ts`, `client/src/components/admin/quality-tab.tsx`, `docs/cost-reconciliation-runbook.md`.

Then write these four tag groups:

**[self-test] (5 checks, ALL must be green today):**
1. `check("[self-test] harness executes", true)`
2. harness source contains all 9 tag literals (`PHASE_26_TAGS.every(t => selfSrc.includes(\`[${t}]\`))`)
3. harness is wired to invoke `scripts/verify-webp-text-edge.ts` (created by 26-02) — assert the string literal is present in its own source, NOT `exists()`
4. harness is wired to invoke `scripts/test-drawblocks-font-state.ts` (created by 26-03) and `scripts/test-logo-overlay-contrast.ts` (created by 26-07) — same string-literal assertion
5. scanner is not vacuous — `!imageOptimizationSrc.includes("__PHASE_26_ABSENT_SENTINEL__")`
6. the three Wave 0 logo fixtures exist on disk (`exists()` for all three) — this one IS a real disk check because Task 1 already delivered them

**[svc-webp-quality] (POL-02, 3 checks — RED today):**
- `image-optimization.service.ts` declares `const DEFAULT_IMAGE_QUALITY = 85;` (exact literal match, and assert the `= 80` literal is GONE)
- `DEFAULT_THUMBNAIL_OPTIONS` still declares `quality: 70` (the separate lower thumbnail setting must NOT be swept along) — this one is GREEN today and must stay green
- no call site passes an explicit main-image quality override: `processImageWithThumbnail(` occurrences across `generate.routes.ts`/`edit.routes.ts`/`carousel.routes.ts`/`carousel-generation.service.ts` are all single-argument (regex `processImageWithThumbnail\([A-Za-z0-9_.]+\)`), so the constant genuinely propagates

**[svc-webp-edge-check] (POL-02, 2 checks — RED today):**
- `exists("scripts/verify-webp-text-edge.ts")`
- guarded by `if (tagActive("svc-webp-edge-check"))`: `spawnSync("npx", ["tsx", "scripts/verify-webp-text-edge.ts"], { encoding: "utf8", shell: process.platform === "win32" })` exits 0; on failure report the last non-empty stdout line as the detail (mirror `verify-phase-25.ts`'s spawnSync detail extraction exactly). Skip the spawn entirely when the file does not exist so the harness reports "missing" instead of a confusing npx error.

**[svc-drawblocks-font-fix] (3 checks — RED today):**
- `drawBlocks`'s balanced-brace body (extract from `function drawBlocks(` to its matching close) contains a `ctx.font =` assignment
- that assignment appears INSIDE the `layouts.forEach(` loop: take `windowAround(drawBlocksBody, "layouts.forEach(", 1200)` and assert it contains `ctx.font =`
- `exists("scripts/test-drawblocks-font-state.ts")` + spawnSync it exits 0 (same guarded pattern as above)

Every red check's `detail` string must name the owning plan and the exact artifact, e.g. `"26-02 must change DEFAULT_IMAGE_QUALITY from 80 to 85 in server/services/image-optimization.service.ts"`.
  </action>
  <acceptance_criteria>
    - `npx tsx scripts/verify-phase-26.ts --only=self-test` exits 0 and prints at least 6 PASS lines
    - `npx tsx scripts/verify-phase-26.ts --only=svc-webp-quality` exits 1 and its FAIL lines name `DEFAULT_IMAGE_QUALITY`
    - `npx tsx scripts/verify-phase-26.ts --only=svc-drawblocks-font-fix` exits 1 and names `scripts/test-drawblocks-font-state.ts`
    - `grep -c "spawnSync" scripts/verify-phase-26.ts` returns at least 1
    - `grep -c "svc-cross-plan" scripts/verify-phase-26.ts` returns at least 1
    - The harness never throws: `npx tsx scripts/verify-phase-26.ts 2>&1 | grep -c "harness crashed"` returns 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/verify-phase-26.ts --only=self-test</automated>
  </verify>
  <done>self-test is 100% green; the three image-quality tags are honestly red, each naming the plan and artifact that will close it; zero uncaught exceptions.</done>
</task>

<task type="auto">
  <name>Task 3: verify-phase-26.ts — the idempotency, quality-dashboard, and cost-runbook tags</name>
  <files>scripts/verify-phase-26.ts</files>
  <read_first>
    - server/routes/carousel.routes.ts lines 335-360 (the existing idempotency pre-flight SELECT this phase replicates)
    - shared/schema.ts lines 1400-1470 (`carouselRequestSchema`/`enhanceRequestSchema`'s `idempotency_key: z.string().uuid()`, `generateRequestSchema`, `editPostRequestSchema`) and lines 857-875 (`postSchema`)
    - server/services/observability.service.ts lines 148-206 (`logVisualCritic` — the `visual_critic` `outcome` value set the dashboard aggregates)
    - server/services/ai-gateway.service.ts lines 51-75 (`logModelFallback` — `event_kind: "model_fallback"`, `metadata.call_class`)
    - server/routes/admin-generations.routes.ts lines 1-70 (the admin route structural precedent: `requireAdminGuard`, `createAdminSupabase`, missing-table/missing-column tolerance)
    - docs/production-cron.md (the runbook convention POL-08 replicates)
  </read_first>
  <action>
Append four more tag groups to `scripts/verify-phase-26.ts`. All are RED today.

**[svc-idempotency] (POL-06, 9 checks):**
Server side:
1. `extractZodObjectBody(sharedSchemaSrc, "generateRequestSchema")` contains `idempotency_key: z.string().uuid()` and does NOT contain `idempotency_key: z.string().uuid().optional()`
2. `extractZodObjectBody(sharedSchemaSrc, "editPostRequestSchema")` contains `idempotency_key: z.string().uuid()` — and the match is TOP-LEVEL, not inside `edit_context`: assert the `idempotency_key` index within that body is LESS than the index of `edit_context: z.object({`
3. `extractZodObjectBody(sharedSchemaSrc, "editSlideRequestSchema")` does NOT contain `idempotency_key` (explicit out-of-scope guard — `POST /api/carousel/slide/edit` must NOT gain a key)
4. `postVersionSchema` gained `idempotency_key: z.string().uuid().nullable()`
5. `generate.routes.ts` has an idempotency pre-flight that runs BEFORE the credit gate: `generateRoutesSrc.indexOf('.eq("idempotency_key"') < generateRoutesSrc.indexOf("checkCredits(")` and both indexes are `> -1`; and its window contains `.eq("user_id"` and `idempotent: true`
6. `edit.routes.ts` has an idempotency pre-flight that runs BEFORE `checkCredits(` (same indexOf ordering), and its `windowAround('.eq("idempotency_key"', 600)` contains `from("post_versions")` and `.eq("post_id"` and does NOT contain `.eq("user_id"` (Pitfall 3 — `post_versions` has no `user_id` column)
7. `generate.routes.ts`'s `posts` insert object contains `idempotency_key` and `edit.routes.ts`'s `post_versions` insert object contains `idempotency_key`
8. A migration file ending `_post_versions_idempotency_key.sql` exists (`findMigrationFile`), and contains BOTH `add column if not exists idempotency_key` (case-insensitive) and `create unique index if not exists` with `where idempotency_key is not null` (case-insensitive)
Client side:
9. All four client call sites send a generated key: `postCreatorSrc` contains `idempotency_key` within `windowAround('fetchSSE("/api/generate"', 1500)`; `quickRemakeSrc` contains `crypto.randomUUID()`; `postEditDialogSrc` contains `idempotency_key`; `postsPageSrc` contains `idempotency_key` within `windowAround('apiRequest("POST", "/api/edit-post"', 800)`

**[svc-quality-dashboard] (POL-09, 8 checks):**
1. `extractZodObjectBody(sharedSchemaSrc, "postSchema")` contains `feedback:` with `z.enum(["up", "down"])` (tolerate whitespace variants via regex `feedback:\s*z\.enum\(\[\s*"up"\s*,\s*"down"\s*\]\)`)
2. A migration ending `_posts_feedback.sql` exists and contains `add column if not exists feedback` (case-insensitive)
3. `posts.routes.ts` registers a feedback endpoint: matches `router\.(patch|post)\("\/api\/posts\/:id\/feedback"`
4. that endpoint's window contains BOTH `.eq("user_id"` (ownership) and `.update({` with `feedback` (overwritable single column, not an insert into a new table)
5. `server/routes/admin-quality.routes.ts` exists and contains `requireAdminGuard`, `createAdminSupabase`, and `router.get("/api/admin/quality"`
6. that file queries all three sources: `from("posts")` with `feedback`, `event_kind` with `"visual_critic"`, and `event_kind` with `"model_fallback"`
7. `server/routes/index.ts` both imports and `router.use(...)`s the new route module
8. UI is wired: `client/src/components/admin/quality-tab.tsx` exists and exports `QualityTab`; `admin.tsx` contains `case "quality":`; `app-sidebar.tsx`'s `adminNavItems` contains `page: "quality"`; `post-viewer-dialog.tsx` contains `/api/posts/${post.id}/feedback` (or an equivalent template-literal form — assert on `"/feedback"` plus `ThumbsUp`)

**[svc-cost-reconciliation-runbook] (POL-08, 4 checks):**
1. `docs/cost-reconciliation-runbook.md` exists
2. it names `usage_events` as the PRIMARY source of truth and `cost_usd_micros` (per 26-CONTEXT.md's resolved question), and mentions `generation_logs` only as the investigation source (assert both strings present AND the doc contains the phrase `source of truth`)
3. it states a numeric material-discrepancy threshold (regex `\d+\s*%`) and a scheduled trigger naming "billing period"
4. it explicitly records that this audit does NOT gate milestone close (assert the doc contains `does not gate` or `cannot gate`, case-insensitive), AND `scripts/reconcile-openrouter-costs.ts` exists and is NOT registered in any cron: assert `readSafe("server/services/cleanup-cron.service.ts")` does NOT contain `reconcile`

Give every check a `detail` naming the owning plan (26-04/26-06 for idempotency, 26-08/26-09 for the dashboard, 26-05 for the runbook).
  </action>
  <acceptance_criteria>
    - `npx tsx scripts/verify-phase-26.ts --only=self-test` still exits 0 (unchanged by this task)
    - `npx tsx scripts/verify-phase-26.ts --only=svc-idempotency` exits 1 with at least 8 FAIL lines
    - `npx tsx scripts/verify-phase-26.ts --only=svc-quality-dashboard` exits 1 and names `admin-quality.routes.ts`
    - `npx tsx scripts/verify-phase-26.ts --only=svc-cost-reconciliation-runbook` exits 1 and names `docs/cost-reconciliation-runbook.md`
    - Full run `npx tsx scripts/verify-phase-26.ts` exits 1 with zero occurrences of `harness crashed` in its output
    - `npm run check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/verify-phase-26.ts --only=self-test &amp;&amp; npm run check</automated>
  </verify>
  <done>All 8 implemented tags exist; self-test green; the 7 requirement tags honestly red with plan-attributed failure details; `npm run check` clean.</done>
</task>

</tasks>

<verification>
1. `npx tsx scripts/verify-phase-26.ts --only=self-test` — exits 0.
2. `npx tsx scripts/verify-phase-26.ts` — exits 1, prints a PASS block and a FAIL block, no stack trace, no `harness crashed`.
3. Every FAIL line names a concrete file path or symbol that a later plan owns.
4. Zero regression: `npx tsx scripts/verify-phase-25.ts` and `npx tsx scripts/verify-golden-image.ts` still exit 0.
5. `npm run check` exits 0.
</verification>

<success_criteria>
- `scripts/verify-phase-26.ts` declares 9 tags and implements checks for 8 of them.
- `--only=self-test` is 100% green; no other tag is falsely green.
- Three deterministic, idempotent logo/corner fixtures are committed under `tests/fixtures/logo/`.
- No production source file was modified by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/26-fixes-and-polish/26-01-SUMMARY.md`
</output>

