---
phase: 24-visual-critic-and-re-roll
plan: 05
type: execute
wave: 3
depends_on: ["24-01", "24-02"]
files_modified:
  - server/services/visual-critic.service.ts
  - scripts/test-critic-reroll-logic.ts
  - scripts/verify-critic-live.ts
autonomous: true
requirements: [CRIT-01, CRIT-02]

must_haves:
  truths:
    - "An image whose critic reports unwanted rendered text is never selected as the delivered image, at any attempt count"
    - "When all 3 attempts soft-fail but none has unwanted text, the highest-scoring attempt is selected deterministically"
    - "When all 3 attempts report unwanted text, no attempt is selected and the caller must fail the generation"
    - "A critic outage does not fail a generation and does not consume re-roll attempts"
    - "A fired abort signal propagates out of the critic instead of being swallowed as an outage"
    - "Discarded-attempt cost is computable without touching the accepted attempt's cost"
  artifacts:
    - path: "server/services/visual-critic.service.ts"
      provides: "CRITIC_JSON_SCHEMA, thresholds, pure selection logic, runVisualCritic"
      min_lines: 220
      exports: ["CRITIC_JSON_SCHEMA", "MAX_REROLL_ATTEMPTS", "selectFinalAttempt", "computeRerollMetadata", "shouldRerollAfter", "runVisualCritic", "isAbortLikeError"]
    - path: "scripts/test-critic-reroll-logic.ts"
      provides: "no-network unit harness for the re-roll decision matrix"
      min_lines: 120
    - path: "scripts/verify-critic-live.ts"
      provides: "OPENROUTER_API_KEY-gated live critic smoke test"
      min_lines: 60
  key_links:
    - from: "runVisualCritic"
      to: "chatCompletion({ callClass: 'critic', responseFormat: json_schema, signal })"
      via: "ai-gateway.service.ts"
      pattern: "callClass: \"critic\""
    - from: "runVisualCritic"
      to: "toOpenRouterInputReference"
      via: "multimodal image_url content part"
      pattern: "toOpenRouterInputReference"
    - from: "scripts/test-critic-reroll-logic.ts"
      to: "selectFinalAttempt / computeRerollMetadata"
      via: "direct import, synthetic outcomes, no network"
      pattern: "selectFinalAttempt"
---

<objective>
Build the visual critic as a self-contained service: the strict-schema multimodal scoring call
plus the PURE decision functions that encode CRIT-02's asymmetric re-roll rules — proven by a
no-network unit harness before any route wiring exists.

Purpose: the hard/soft fail asymmetry (unwanted text is an unconditional hard fail even after the
cap; low scores get a best-of-3 fallback) is the single easiest thing in this phase to implement
subtly wrong. Isolating it in pure functions with a 20-assertion harness makes it provable rather
than reviewable.
Output: `server/services/visual-critic.service.ts` + two Wave 0 harness scripts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-visual-critic-and-re-roll/24-CONTEXT.md
@.planning/phases/24-visual-critic-and-re-roll/24-RESEARCH.md
@.planning/phases/24-visual-critic-and-re-roll/24-VALIDATION.md

<interfaces>
<!-- Everything this service consumes. Verbatim — do not go exploring. -->

server/services/ai-gateway.service.ts (as widened by plan 24-02):
```typescript
export interface ChatCompletionParams {
  apiKey: string; model: string; fallbackModels?: string[];
  messages: Array<{ role: "user" | "system"; content: ChatMessageContent }>;
  temperature?: number; maxTokens?: number;
  responseFormat?: { type: "json_object" } | { type: "json_schema"; json_schema: Record<string, unknown> };
  callClass?: FallbackCallClass;   // Phase 24 — pass "critic"
  signal?: AbortSignal;            // Phase 24 — real cancellation
}
export interface ChatCompletionResult { text: string; usage?: {...}; costUsdMicros?: number; modelUsed: string; }
export function toOpenRouterInputReference(ref: { mimeType: string; data: string }): { type: "image_url"; image_url: { url: string } };
export async function chatCompletion(params: ChatCompletionParams): Promise<ChatCompletionResult>;
```

server/services/planning-schema.service.ts — the strict-mode dialect to mirror
(`PLANNING_JSON_SCHEMA`, lines 102-200): `{ name, strict: true, schema: { type: "object",
properties: {...}, required: [EVERY property], additionalProperties: false } }`. Its
`layout_archetype_id` uses `enum: [...]` — that is the proven bounded-value pattern.
Also exports `LAYOUT_ARCHETYPE_IDS = ["bottom_band", "top_stack", "centered_hero"]` and
`DEFAULT_LAYOUT_ARCHETYPE_ID = "bottom_band"`.

server/services/planning-schema.service.ts lines 22-26 — the in-repo evidence for Pitfall 2:
`minimum`/`maximum`/length JSON-Schema keywords are NOT reliable under this gateway's strict
mode; bound values with `enum` and re-validate at runtime instead.

scripts/test-planning-schema-classification.ts — the exact no-network unit-harness style to
mirror (`assertEqual` helper, `process.exit(1)` on first failure, final "All ... passed." line).

scripts/verify-planning-ablation.ts — the exact live-gated smoke-test style to mirror
(SKIP + `process.exit(0)` when `OPENROUTER_API_KEY` is unset, `--image=` arg parsing,
`mimeTypeFor`, PASS/FAIL list, `process.exit(1)` on failure).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure re-roll decision logic + its no-network unit harness</name>
  <files>server/services/visual-critic.service.ts, scripts/test-critic-reroll-logic.ts</files>
  <read_first>
    - scripts/test-planning-schema-classification.ts (all 149 lines — the harness style to mirror)
    - .planning/phases/24-visual-critic-and-re-roll/24-CONTEXT.md "Re-roll trigger logic" paragraph (the locked asymmetry)
    - .planning/phases/24-visual-critic-and-re-roll/24-RESEARCH.md "Pattern 1" and Open Question 4
  </read_first>
  <behavior>
    Write `scripts/test-critic-reroll-logic.ts` FIRST (it must fail on a missing module), then
    implement until every assertion passes. Required assertions (>= 20):
    - summarizeCriticScores({5,5,5}) -> totalScore 15, passesThresholds true
    - summarizeCriticScores({3,3,3}) -> passesThresholds true (3 is the inclusive floor)
    - summarizeCriticScores({3,3,2}) -> passesThresholds false (ANY dimension < 3 fails)
    - shouldRerollAfter(scored, passes, no unwanted text) -> false
    - shouldRerollAfter(scored, soft-fail) -> true
    - shouldRerollAfter(scored, unwanted text, perfect scores) -> true
    - shouldRerollAfter(unavailable) -> false (an outage must not burn re-rolls)
    - selectFinalAttempt([pass]) -> { acceptedIndex: 1, outcome: "pass" }
    - selectFinalAttempt([hardFail, pass]) -> accepted 2, outcome "pass"
    - selectFinalAttempt([softFail(total 9), softFail(total 12), softFail(total 10)]) -> accepted 2, outcome "soft_fail_accepted_best"
    - selectFinalAttempt([softFail(total 11), softFail(total 11)]) -> accepted 1 (tie -> lowest index, deterministic)
    - selectFinalAttempt([hardFail(total 15), softFail(total 6)]) -> accepted 2 — a hard-fail attempt is NEVER selected even with a strictly higher total
    - selectFinalAttempt([hardFail, hardFail, hardFail]) -> { acceptedIndex: null, outcome: "hard_fail_all_attempts" }
    - selectFinalAttempt([hardFail, softFail, hardFail]) -> accepted 2, outcome "soft_fail_accepted_best"
    - selectFinalAttempt([unavailable]) -> accepted 1, outcome "critic_unavailable"
    - selectFinalAttempt([softFail, unavailable]) -> accepted 1, outcome "soft_fail_accepted_best" (an observed non-hard-fail image beats an unobserved one)
    - selectFinalAttempt([hardFail, unavailable]) -> accepted 2, outcome "critic_unavailable"
    - selectFinalAttempt([]) -> { acceptedIndex: null, outcome: "hard_fail_all_attempts" } (total function, no throw)
    - computeRerollMetadata(3 attempts costing 100/200/400 image + 10/20/40 critic, acceptedIndex 2) -> { reroll_attempt_count: 2, reroll_cost_usd_micros: 550 } — the accepted attempt's 220 is EXCLUDED
    - computeRerollMetadata(single accepted attempt) -> { reroll_attempt_count: 0, reroll_cost_usd_micros: 0 }
    - computeRerollMetadata(attempts, acceptedIndex: null) -> counts every attempt as discarded
    - MAX_REROLL_ATTEMPTS === 2 and CRITIC_MIN_DIMENSION_SCORE === 3
  </behavior>
  <action>
Create `server/services/visual-critic.service.ts` with the types and PURE functions below (the
network call comes in Task 2 — this task adds no imports from ai-gateway.service.ts).

```typescript
/** 3 total generations: 1 initial + MAX_REROLL_ATTEMPTS re-rolls (24-CONTEXT.md locked). */
export const MAX_REROLL_ATTEMPTS = 2;
/** Inclusive floor. ANY dimension below this is a SOFT fail. Claude's-discretion constant — change here, nowhere else. */
export const CRITIC_MIN_DIMENSION_SCORE = 3;

export interface CriticScores { composition: number; color_harmony: number; text_legibility_zone: number; }

export interface CriticOutcome {
  status: "scored" | "unavailable";
  scores: CriticScores | null;      // null iff status === "unavailable"
  unwantedTextDetected: boolean;    // ALWAYS false when unavailable — never assert a hard fail we did not observe
  unwantedTextDetail: string;
  reasoning: string;
  passesThresholds: boolean;        // false when unavailable
  totalScore: number;               // 0 when unavailable
  costUsdMicros?: number;
  modelUsed?: string;
}

export interface CriticAttempt {
  index: number;                    // 1-based attempt number
  outcome: CriticOutcome;
  imageCostUsdMicros: number;       // that attempt's image-generation cost (0 when the provider reported none)
}

export type FinalSelectionOutcome = "pass" | "soft_fail_accepted_best" | "critic_unavailable" | "hard_fail_all_attempts";
export interface FinalSelection { acceptedIndex: number | null; outcome: FinalSelectionOutcome; }

export function summarizeCriticScores(scores: CriticScores): { totalScore: number; passesThresholds: boolean };
export function shouldRerollAfter(outcome: CriticOutcome): boolean;
export function selectFinalAttempt(attempts: CriticAttempt[]): FinalSelection;
export function computeRerollMetadata(attempts: CriticAttempt[], acceptedIndex: number | null):
  { reroll_attempt_count: number; reroll_cost_usd_micros: number };
```

Locked semantics — implement EXACTLY these, and put this list in a block comment above
`selectFinalAttempt` so it is reviewable in code:

  shouldRerollAfter(o) = o.status === "scored" && (o.unwantedTextDetected || !o.passesThresholds)
    An "unavailable" critic returns false: a critic outage is fail-OPEN (accept the image) and
    must never consume paid re-roll attempts.

  selectFinalAttempt applies these rules IN ORDER and returns on the first match:
    1. First attempt with status "scored", !unwantedTextDetected, passesThresholds
         -> outcome "pass"
    2. Among attempts with status "scored" and !unwantedTextDetected, the one with the highest
       totalScore; ties resolve to the LOWEST index (deterministic, no Math.random, no sort
       instability) -> outcome "soft_fail_accepted_best"
    3. First attempt with status "unavailable" -> outcome "critic_unavailable"
       (rule 2 outranks rule 3 on purpose: an image we scored and found text-free is safer than
       one we never looked at)
    4. Otherwise -> { acceptedIndex: null, outcome: "hard_fail_all_attempts" }
       This is the ONLY branch where the caller must fail the generation. An attempt with
       unwantedTextDetected === true is unreachable by rules 1-3 at ANY attempt count — there is
       no "best available" escape hatch for rendered text (24-CONTEXT.md, non-negotiable).

  computeRerollMetadata sums, over every attempt whose index !== acceptedIndex,
  `imageCostUsdMicros + (outcome.costUsdMicros ?? 0)`, and counts them. The ACCEPTED attempt's
  image + critic cost is deliberately excluded — it is what the user is charged (CRIT-03).

Then create `scripts/test-critic-reroll-logic.ts` in the exact style of
`scripts/test-planning-schema-classification.ts`: an `assertEqual(label, actual, expected)` helper
that `process.exit(1)`s on the first mismatch, small fixture builders
(`scoredOutcome({composition, color_harmony, text_legibility_zone, unwantedText})` and
`unavailableOutcome()`), one numbered block per assertion in `<behavior>`, and a final
`console.log("\nAll CRIT-02 re-roll decision tests passed.")`. Use `JSON.stringify` comparison
for the object-returning functions. No network, no Supabase, no `chatCompletion` import.
  </action>
  <verify>
    <automated>npx tsx scripts/test-critic-reroll-logic.ts && npx tsx scripts/verify-phase-24.ts --only=svc-reroll-logic</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/test-critic-reroll-logic.ts` exits 0 and prints >= 20 PASS lines
    - `grep -c 'assertEqual(' scripts/test-critic-reroll-logic.ts` >= 20
    - `grep -rn 'chatCompletion\|createAdminSupabase\|fetch(' scripts/test-critic-reroll-logic.ts` returns nothing (no network)
    - `npx tsx scripts/verify-phase-24.ts --only=svc-reroll-logic` shows checks 1, 4, 5 and 6 as PASS (check 2 and 3 stay red until plan 24-06)
    - `npx tsx scripts/verify-phase-24.ts --only=self-test` shows check 3 as PASS
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>The hard/soft fail asymmetry, the best-of-3 tie-break, the fail-open outage rule, and the accepted-attempt cost exclusion are all proven by direct assertion on returned values.</done>
</task>

<task type="auto">
  <name>Task 2: CRITIC_JSON_SCHEMA + prompt + runVisualCritic gateway call</name>
  <files>server/services/visual-critic.service.ts</files>
  <read_first>
    - server/services/planning-schema.service.ts lines 1-30 and 102-200 (PLANNING_JSON_SCHEMA's strict-mode dialect + the MIN_IMAGE_PROMPT_LENGTH comment grounding Pitfall 2) and lines 400-465 (validatePlanningWireResult's defense-in-depth runtime re-validation)
    - server/services/gemini.service.ts lines 730-850 (buildPlanningContentParts + the live image+json_schema chatCompletion call — the strongest in-repo precedent)
    - server/services/enhancement.service.ts lines 240-275 (the other image+chatCompletion call site, which uses the looser json_object)
    - .planning/phases/24-visual-critic-and-re-roll/24-RESEARCH.md "Pattern 2", "Pattern 3", Pitfall 2, Pitfall 6
  </read_first>
  <action>
Extend `server/services/visual-critic.service.ts` (same file, additive below Task 1's pure block).

1. `export const CRITIC_SCHEMA_NAME = "visual_critic_score";` and
   `export const CRITIC_MAX_OUTPUT_TOKENS = 512;` and
   `export const DEFAULT_CRITIC_MODEL = "<the exact slug plan 24-01 committed to aiModelsSchema.critic>";`
   (read it from shared/schema.ts — do not assume).

2. `export const CRITIC_JSON_SCHEMA` mirroring PLANNING_JSON_SCHEMA's dialect exactly:
   `{ name: CRITIC_SCHEMA_NAME, strict: true, schema: { type: "object", properties: {...},
   required: [ALL SIX property names], additionalProperties: false } }` with properties:
   - `composition_score`: `{ type: "integer", enum: [1,2,3,4,5], description: "..." }`
   - `color_harmony_score`: same shape
   - `text_legibility_zone_score`: same shape
   - `unwanted_text_detected`: `{ type: "boolean", description: "..." }`
   - `unwanted_text_detail`: `{ type: "string", description: "What text was detected and where. Empty string when unwanted_text_detected is false." }`
   - `reasoning`: `{ type: "string", description: "One or two sentences justifying the scores." }`
   HARD RULE (Pitfall 2): use `enum` for the bounded scores. Do NOT use `minimum`/`maximum`
   anywhere in this file — this repo already documents those keywords as unreliable under the
   gateway's strict mode. Add that rationale as a comment above the schema.

3. `export function buildCriticPrompt(layoutArchetypeId?: string): string` — the rubric. It must
   state:
   - The image under review is a PRE-TYPOGRAPHY base image. All headline/support/CTA text is
     composited deterministically by the server AFTERWARD (Phase 23), so the base image must be
     free of rendered text and must reserve clean negative space for it.
   - Score each of composition, color harmony, and text-legibility zone on 1-5 with explicit
     anchors (1 = unusable, 3 = acceptable for publication, 5 = professional art direction).
   - `text_legibility_zone_score` judges specifically whether the region the layout archetype
     will occupy is clean and low-detail enough for legible composited text. Name the region for
     the archetype: `bottom_band` = lower third, `top_stack` = upper third,
     `centered_hero` = central band. Default to `bottom_band` when unknown.
   - `unwanted_text_detected` is TRUE for any AI-rendered overlay/graphic-design text the model
     invented: headlines, captions, watermarks, signage, labels, gibberish or malformed
     letterforms, or any lettering that reads as a design element rather than as part of a
     photographed object. It is FALSE for incidental, naturally-occurring real-world text that
     legitimately belongs to a depicted subject (e.g. a product label on a bottle that came from
     a reference image). RATIONALE (record this in the plan SUMMARY too): an over-strict reading
     would make legitimate product photography hard-fail all 3 attempts and fail the generation
     outright, which is worse than the artifact it guards against.
   - Return ONLY the JSON object matching the schema.

4. `export function parseCriticWireResult(raw: unknown): CriticScores & { unwantedTextDetected: boolean; unwantedTextDetail: string; reasoning: string } | null`
   — defense-in-depth runtime re-validation mirroring `validatePlanningWireResult`'s spirit
   (strict mode reduces, but does not eliminate, malformed output). Return `null` (NOT a throw)
   when any of the three scores is missing, non-integer, or outside 1-5, or when
   `unwanted_text_detected` is not a boolean. A `null` here becomes an `"unavailable"` outcome.

5. `export function isAbortLikeError(err: unknown, signal?: AbortSignal): boolean` —
   true when `signal?.aborted === true`, or `(err as any)?.name === "AbortError"`, or
   `/abort/i.test(String((err as any)?.message ?? ""))`.

6. `export async function runVisualCritic(params: {
        apiKey: string; model?: string; imageBuffer: Buffer; imageMimeType: string;
        layoutArchetypeId?: string; signal?: AbortSignal;
      }): Promise<CriticOutcome>`
   - If `!params.apiKey`, return an `"unavailable"` outcome immediately (no call, no throw).
     An affiliate or a rollback window without an OpenRouter key must not break generation.
   - Build `content` as `[{ type: "text", text: buildCriticPrompt(layoutArchetypeId) },
     toOpenRouterInputReference({ mimeType: params.imageMimeType, data: params.imageBuffer.toString("base64") })]`.
     Use `toOpenRouterInputReference` — do NOT hand-roll the data-URI (24-RESEARCH "Don't Hand-Roll").
   - Call `chatCompletion({ apiKey, model: params.model || DEFAULT_CRITIC_MODEL,
     callClass: "critic", messages: [{ role: "user", content }], temperature: 0.1,
     maxTokens: CRITIC_MAX_OUTPUT_TOKENS,
     responseFormat: { type: "json_schema", json_schema: CRITIC_JSON_SCHEMA },
     signal: params.signal })`.
     `callClass: "critic"` is MANDATORY — without it the call silently inherits the shared text
     fallback chain (24-RESEARCH Pitfall 1) and its `model_fallback` rows are mislabeled.
   - `JSON.parse(result.text)` then `parseCriticWireResult`. On `null`, return `"unavailable"`
     (log a one-line `console.warn` with a 300-char raw preview).
   - On a `"scored"` result, fill `passesThresholds`/`totalScore` from `summarizeCriticScores`,
     and carry `costUsdMicros: result.costUsdMicros` and `modelUsed: result.modelUsed`.
   - CATCH BLOCK — the critical rule: `if (isAbortLikeError(err, params.signal)) throw err;`
     BEFORE returning the fail-open `"unavailable"` outcome. Swallowing an abort would silently
     defeat CRIT-04's cancellation and let the loop start another paid attempt after the safety
     timer fired. Comment it as such.
   - Never throw for any other reason.

Add a file-header comment documenting the two discretion decisions this file locks: the 1-5
enum scale with an inclusive floor of 3, and fail-open-on-critic-outage.
  </action>
  <verify>
    <automated>npx tsx scripts/verify-phase-24.ts --only=svc-critic-call && npx tsx scripts/test-critic-reroll-logic.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/verify-phase-24.ts --only=svc-critic-call` shows checks 1-8 as PASS and the `aiModelsSchema` half of check 9 as PASS (the `generate.routes.ts` half stays red until 24-06)
    - `grep -c 'minimum:\|maximum:' server/services/visual-critic.service.ts` returns 0
    - `grep -c 'enum: \[1, 2, 3, 4, 5\]' server/services/visual-critic.service.ts` returns 3
    - `grep -n 'callClass: "critic"' server/services/visual-critic.service.ts` returns exactly 1 line
    - `grep -n 'isAbortLikeError(err, params.signal)) throw err' server/services/visual-critic.service.ts` returns 1 line
    - `grep -n 'toOpenRouterInputReference' server/services/visual-critic.service.ts` returns >= 1 line and `grep -c 'data:\${' server/services/visual-critic.service.ts` returns 0 (no hand-rolled data URI)
    - `npx tsx scripts/test-critic-reroll-logic.ts` still exits 0 (Task 1's assertions unaffected)
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>A single strict-schema multimodal critic call exists on its own call class, fails open on outage, and re-throws aborts instead of masking them.</done>
</task>

<task type="auto">
  <name>Task 3: OPENROUTER_API_KEY-gated live critic smoke test</name>
  <files>scripts/verify-critic-live.ts</files>
  <read_first>
    - scripts/verify-planning-ablation.ts (all 158 lines — the SKIP-when-no-key gate, `--image=` parsing, `mimeTypeFor`, PASS/FAIL accumulation, exit codes)
    - server/services/visual-critic.service.ts (as written by Tasks 1-2)
    - .planning/phases/24-visual-critic-and-re-roll/24-VALIDATION.md "Wave 0 Requirements" third bullet
  </read_first>
  <action>
Create `scripts/verify-critic-live.ts` mirroring `scripts/verify-planning-ablation.ts`'s shape:

- Header comment: this COSTS REAL MONEY (one vision completion per run) and is the only proof
  that the image + strict-`json_schema` combination works against the live catalog
  (24-RESEARCH Pitfall 6 — capability data is volatile, a research snapshot is not evidence).
  Usage line: `OPENROUTER_API_KEY=sk-or-... npx tsx scripts/verify-critic-live.ts --image=./sample.png [--model=<slug>]`
- If `process.env.OPENROUTER_API_KEY` is unset: `console.log("SKIP verify-critic-live — OPENROUTER_API_KEY not set (live-only check, see verify-phase-24.ts runbook)")` and `process.exit(0)`.
  CI-safe by construction.
- Require `--image=` pointing at an existing file; otherwise print usage and `process.exit(1)`.
- Read the file, call `runVisualCritic({ apiKey, model: modelArg || undefined, imageBuffer, imageMimeType: mimeTypeFor(path), layoutArchetypeId: "bottom_band" })`.
- Print the full returned `CriticOutcome` as pretty JSON plus `costUsdMicros` and `modelUsed`.
- Assert and report PASS/FAIL for each of:
  1. `outcome.status === "scored"` — a `"unavailable"` result here means the live model rejected
     the vision+strict-schema combination; print the console.warn preview guidance in the failure
     message and name `ai_models.critic` as the thing to change.
  2. all three scores are integers within 1-5
  3. `typeof outcome.unwantedTextDetected === "boolean"`
  4. `outcome.reasoning.trim().length > 0`
  5. `outcome.passesThresholds` equals `summarizeCriticScores(outcome.scores).passesThresholds`
     (proves the pure logic and the live path agree)
- Add a SECOND, optional `--abort-probe` flag: when present, create an `AbortController`,
  `setTimeout(() => controller.abort(), 50)`, pass `signal: controller.signal`, and assert
  `runVisualCritic` REJECTS (does not resolve to an `"unavailable"` outcome). This is the only
  automatable proof of CRIT-04's real-cancellation behavior; it is the live counterpart to the
  static `[svc-abort-signal]` checks. Report it as PASS/FAIL and skip it when the flag is absent.
- `process.exit(1)` on any failure; otherwise print a final confirmation line and `process.exit(0)`.
  </action>
  <verify>
    <automated>npx tsx scripts/verify-critic-live.ts && npx tsx scripts/verify-phase-24.ts --only=self-test</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/verify-critic-live.ts` with no `OPENROUTER_API_KEY` prints a SKIP line and exits 0 (CI-safe)
    - `OPENROUTER_API_KEY=dummy npx tsx scripts/verify-critic-live.ts` (no `--image`) prints usage and exits 1
    - `npx tsx scripts/verify-phase-24.ts --only=self-test` shows check 4 as PASS (all 5 self-test checks green)
    - `grep -n 'process.exit(0)' scripts/verify-critic-live.ts` returns >= 2 lines (skip path + success path)
    - `grep -n 'abort-probe' scripts/verify-critic-live.ts` returns >= 1 line
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>Both Wave 0 harness scripts exist; the live critic call and its real-abort behavior are exercisable with one command when a funded key is present.</done>
</task>

</tasks>

<verification>
- `npx tsx scripts/test-critic-reroll-logic.ts` exits 0 with >= 20 PASS lines
- `npx tsx scripts/verify-phase-24.ts --only=self-test` — all 5 PASS
- `npx tsx scripts/verify-phase-24.ts --only=svc-critic-call` — checks 1-8 PASS
- `npx tsx scripts/verify-phase-24.ts --only=svc-reroll-logic` — checks 1, 4, 5, 6 PASS
- `npx tsx scripts/verify-critic-live.ts` exits 0 (SKIP) without a key
- `npm run check` exits 0; zero regression on `verify-phase-21.ts`, `verify-phase-22.ts`, `verify-phase-23.ts`
</verification>

<success_criteria>
`server/services/visual-critic.service.ts` provides a strict-schema, `callClass: "critic"`,
abort-propagating, fail-open multimodal scorer plus pure decision functions whose hard-fail
asymmetry, best-of-3 tie-break and cost-exclusion rules are proven by 20+ direct assertions with
no network. Both Wave 0 harness scripts exist and are CI-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/24-visual-critic-and-re-roll/24-05-SUMMARY.md`.
Record verbatim: the chosen `DEFAULT_CRITIC_MODEL` slug, the locked threshold constants, the
fail-open-on-outage decision, and the "incidental real-world text is not unwanted text" prompt
rationale — plan 24-06 and the 24-07 live runbook both depend on these being explicit.
</output>
