---
phase: 23-deterministic-typography-and-edit-fidelity
plan: 11
type: execute
wave: 5
depends_on: ["23-05", "23-06", "23-07", "23-08", "23-09", "23-10"]
files_modified:
  - scripts/verify-phase-23.ts
autonomous: false
requirements: [TYPO-01, TYPO-02, TYPO-03, TYPO-04, TYPO-05, TYPO-06, TYPO-07, POL-04, POL-05]
must_haves:
  truths:
    - "The full Phase 23 gate is green with zero weakened or deleted checks"
    - "Cross-plan invariants nobody plan could assert alone are enforced (pipeline order, no double-render, legacy branch present)"
    - "No prior-phase harness regressed"
    - "An operator has a single authoritative runbook for the checks that cannot run in a dev sandbox"
  artifacts:
    - path: "scripts/verify-phase-23.ts"
      provides: "12 green tags + cross-plan invariants + the MANUAL/LIVE VERIFICATION RUNBOOK"
      contains: "MANUAL/LIVE VERIFICATION RUNBOOK"
  key_links:
    - from: "scripts/verify-phase-23.ts"
      to: "every Phase 23 requirement"
      via: "12 tags asserted against real implemented code"
      pattern: "\\[svc-"
---

<objective>
Close Phase 23: prove the whole gate green without weakening a single check, add the three cross-plan invariants no individual plan could assert alone, embed the authoritative operator runbook for the Alpine/AVX/live-round-trip checks that cannot run in this environment, and take operator sign-off.

Purpose: All nine Phase 23 requirements, verified together.
Output: A green `scripts/verify-phase-23.ts` with a runbook, plus a blocking operator checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-deterministic-typography-and-edit-fidelity/23-CONTEXT.md
@.planning/phases/23-deterministic-typography-and-edit-fidelity/23-RESEARCH.md
@.planning/phases/23-deterministic-typography-and-edit-fidelity/23-VALIDATION.md

<interfaces>
The 12 tags written in plan 23-01, and which plan closes each:

| Tag | Requirement | Closed by |
|-----|-------------|-----------|
| `[self-test]` | infra | 23-01 |
| `[svc-text-free-prompt]` | TYPO-01 | 23-05 |
| `[svc-compositor-archetypes]` | TYPO-02 | 23-04 |
| `[svc-contrast-scrim]` | TYPO-03 | 23-04 |
| `[svc-golden-image-glyphs]` | TYPO-04 | 23-04 + 23-08 |
| `[svc-aspect-crop]` | POL-04 | 23-03 |
| `[svc-schema-migration]` | TYPO-05 | 23-02 |
| `[svc-generation-params]` | POL-05 | 23-02 + 23-06 + 23-07 |
| `[svc-edit-base-image]` | TYPO-07 | 23-07 |
| `[svc-verify-repair-removed]` | TYPO-06 | 23-06 + 23-07 + 23-09 |
| `[svc-docker-fonts]` | TYPO-04 | 23-08 |
| `[svc-remake-ui]` | POL-05 | 23-10 |

Runbook precedent to follow verbatim in style: the `MANUAL/LIVE VERIFICATION RUNBOOK` comment blocks at the bottom of `scripts/verify-phase-21.1.ts` and `scripts/verify-phase-22.ts`.

Standing environment blockers this plan inherits (STATE.md Blockers): Phase 21.1 plan 07 Task 3 and Phase 22 plan 06 Task 3 are both unresolved operator checkpoints. Phase 23's checkpoint is independent of both — do not conflate them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full gate green + three cross-plan invariant checks</name>
  <files>scripts/verify-phase-23.ts</files>
  <read_first>
    - scripts/verify-phase-23.ts (whole file — the 12 tags written in plan 23-01; this is the FIRST plan since 23-01 permitted to edit it)
    - server/routes/generate.routes.ts and server/routes/edit.routes.ts (post-23-06/23-07)
    - server/services/gemini.service.ts (post-23-05)
    - scripts/verify-phase-22.ts (the "cross-plan invariant checks" convention added by plan 22-06)
  </read_first>
  <action>
    1. Run `npx tsx scripts/verify-phase-23.ts` and record the exact pass/fail counts BEFORE any change. For every failure, determine whether the IMPLEMENTATION is wrong or the CHECK's regex is wrong:
       - Implementation wrong → fix the implementation file, not the check.
       - Check regex genuinely inaccurate (e.g. a substring collision, as documented in 22-03/22-04/22-06 summaries) → fix the regex to be MORE precise, never less. Record every such adjustment in the SUMMARY with a before/after regex and a one-line justification.
       - NEVER delete a check, downgrade an assertion to `true`, or narrow a `--only` tag to sidestep a failure. The SUMMARY must state "zero weakened checks".

    2. Append a `[svc-cross-plan]` section with these three invariants — each spans files that no single plan owns:

    a. PIPELINE ORDER (POL-04 before TYPO-02 before logo before optimize), asserted in BOTH routes by byte-offset comparison:
    ```typescript
    const genOrderOk = (() => {
      const c = generateRouteSrc.indexOf("cropToExactAspectRatio(");
      const t = generateRouteSrc.indexOf("compositeTypography(");
      const l = generateRouteSrc.indexOf("applyLogoOverlay(");
      const o = generateRouteSrc.indexOf("processImageWithThumbnail(");
      return c > -1 && t > c && l > t && o > l;
    })();
    check("[svc-cross-plan] generate.routes.ts pipeline order: crop → typography → logo → optimize", genOrderOk);
    ```
    plus the `edit.routes.ts` equivalent (its crop is inside the non-fast-path branch, so assert `crop < composite < optimize` and that `applyLogoOverlay(` appears after `compositeTypography(`).

    b. NO DOUBLE-RENDER SURFACE: the compositor exists AND the repair loop does not, simultaneously — the exact condition the TYPO-06 sequencing rule protects:
    ```typescript
    check(
      "[svc-cross-plan] compositor is wired AND the repair loop is gone (never a coverage gap)",
      /compositeTypography\(/.test(generateRouteSrc)
        && !fs.existsSync("server/services/text-rendering.service.ts")
        && !/enforceExactImageText/.test(generateRouteSrc)
        && !/enforceExactImageText/.test(editRouteSrc),
    );
    ```

    c. LEGACY SAFETY: the `base_image_url IS NULL` branch exists and is genuinely reachable — assert `edit.routes.ts` contains `isBaseImage: false`, the `LEGACY (base_image_url IS NULL)` marker, AND that both `cropToExactAspectRatio(` and `compositeTypography(` appear inside an `editTarget.isBaseImage` guard (assert `editTarget.isBaseImage` appears at least 3 times and that the file contains no unguarded call — verify by reading, and encode as: the character index of the first `editTarget.isBaseImage` is LESS than the index of `compositeTypography(`).

    Also add a fourth: `[svc-cross-plan] no prior-phase harness regressed` — spawn `npx tsx scripts/verify-phase-16.ts`, `verify-phase-21.ts`, `verify-phase-21.1.ts`, and `verify-phase-22.ts` via `spawnSync` (the pattern `verify-phase-22.ts` already imports `spawnSync` for) and assert all four exit 0. Guard it behind the `--only` filter so a targeted run stays fast.

    3. Update the `[self-test]` tag-literal list to include `[svc-cross-plan]` (13 tags total).

    4. Run the full suite plus `npm run check` and `npm run build`. All must pass.
  </action>
  <verify>
    <automated>npx tsx scripts/verify-phase-23.ts &amp;&amp; npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/verify-phase-23.ts` exits 0 with zero failures
    - `grep -c '\[svc-cross-plan\]' scripts/verify-phase-23.ts` >= 5 (4 checks + the self-test literal)
    - `grep -c 'check(' scripts/verify-phase-23.ts` is GREATER than the count recorded before this task (checks were added, never removed) — record both numbers in the SUMMARY
    - `grep -cE 'check\([^,]+,\s*true\s*[,)]' scripts/verify-phase-23.ts` <= 1 (only `[self-test] harness executes` may be an unconditional true)
    - `npx tsx scripts/verify-golden-image.ts` exits 0
    - `npx tsx scripts/test-aspect-crop.ts` exits 0
    - `npx tsx scripts/test-edit-base-image-resolution.ts` exits 0
    - `npx tsx scripts/verify-phase-16.ts && npx tsx scripts/verify-phase-21.ts && npx tsx scripts/verify-phase-21.1.ts && npx tsx scripts/verify-phase-22.ts && npx tsx scripts/verify-phase-06.ts && npx tsx scripts/verify-phase-12.6.ts` all exit 0
    - `npm run check` exits 0 and `npm run build` exits 0
    - The SUMMARY states "zero weakened checks" and lists every regex adjustment with a justification
  </acceptance_criteria>
  <done>The Phase 23 gate is fully green across 13 tags including four cross-plan invariants that pin the pipeline order, the no-coverage-gap property, the legacy branch, and prior-phase non-regression — with no check weakened or removed.</done>
</task>

<task type="auto">
  <name>Task 2: Embed the MANUAL/LIVE VERIFICATION RUNBOOK</name>
  <files>scripts/verify-phase-23.ts</files>
  <read_first>
    - scripts/verify-phase-22.ts (the runbook comment block at the bottom — copy its structure and tone exactly)
    - scripts/verify-phase-21.1.ts (the same, for the migration-application step's wording)
    - .planning/phases/23-deterministic-typography-and-edit-fidelity/23-VALIDATION.md (Manual-Only Verifications table — the three rows this runbook must cover)
    - .planning/phases/23-deterministic-typography-and-edit-fidelity/23-RESEARCH.md (Pitfalls 1, 3, 7 — the risks the operator is confirming)
  </read_first>
  <action>
    Append a `// ── MANUAL/LIVE VERIFICATION RUNBOOK (Phase 23) ──` comment block to the BOTTOM of `scripts/verify-phase-23.ts` (pure addition — `git diff --stat` must show only insertions). It is the single authoritative source; the SUMMARY references it rather than duplicating it.

    Seven numbered steps, each with the exact command or click-path and the exact expected result:

    1. APPLY THE MIGRATION. Supabase Dashboard → SQL Editor → paste `supabase/migrations/20260728000000_posts_base_image_typography_generation_params.sql` → Run. Expected: success; `select column_name from information_schema.columns where table_name='posts' and column_name in ('base_image_url','typography_meta','generation_params');` returns 3 rows, and the same query for `post_versions` returns 2.

    2. ALPINE/AVX SMOKE (23-RESEARCH.md Pitfall 1, Brooooooklyn/canvas#1117). `docker build -t xareable:phase23 .` — expected to SUCCEED; both the builder-stage `verify-golden-image.ts` RUN and the runner-stage `require('@napi-rs/canvas')` RUN must pass. Then on the real Coolify/Hetzner host, in the deployed container: `docker exec <container> node -e "require('@napi-rs/canvas');console.log('AVX OK')"`. Expected: prints `AVX OK`. A crash with `Illegal instruction` means the production CPU lacks AVX — the documented remedy is a non-Alpine base image for this dependency; do NOT ship until this passes.

    3. GOLDEN IMAGE INSIDE THE REAL ALPINE CONTAINER (Pitfall 3 — font rendering differs from a non-Alpine dev machine). `docker run --rm -v "$PWD/out:/out" xareable:phase23 npx tsx scripts/verify-golden-image.ts --out=/out` (note: `tsx` and the scripts are not in the runner stage — run against the `builder` target: `docker build --target builder -t xareable:phase23-builder . && docker run --rm -v "$PWD/out:/out" xareable:phase23-builder npx tsx scripts/verify-golden-image.ts --out=/out`). Expected: `GOLDEN IMAGE: N/N passed`, and the PNGs in `./out` show crisp `PROMOÇÃO RELÂMPAGO` / `¡OFERTA DEL DÍA!` with correct á ç ñ ã õ í ú ê — zero tofu boxes.

    4. LIVE GENERATE. Generate a single-image post in staging with `use_text=true`, `text_mode="exact"`, a pt-BR headline containing accents and a number, and `aspect_ratio="1200:628"`. Expected: (a) the delivered image is exactly 1200:628 within 1%; (b) the text is crisp, correctly spelled, and matches the request character-for-character; (c) `select base_image_url, typography_meta, generation_params from posts order by created_at desc limit 1;` shows all three populated and `generation_params.aspect_ratio = '1200:628'`; (d) `select count(*) from generation_logs where event_kind='text_verification' and created_at > now() - interval '1 hour';` returns 0.

    5. LIVE EDIT — NO GHOSTING (TYPO-07). Edit the post from step 4 with a visual-concept change (e.g. "change the background to a marble surface"). Expected: exactly ONE set of crisp text in the result — no ghosted, doubled, or garbled second layer; `select base_image_url, typography_meta from post_versions where post_id='<id>' order by version_number desc limit 1;` shows both populated.

    6. LIVE TEXT-ONLY FAST PATH + REMAKE FIDELITY (POL-05). On the same post, open Edit, change ONLY the text step (Replace Text, three newline-separated lines), and confirm the Format & Logo step already shows `1200:628` and the original logo position pre-filled. Submit. Expected: it completes noticeably faster than step 5 (no AI image call), the new text renders exactly as typed with the correct headline/support/CTA hierarchy, and the aspect ratio is unchanged. Then run a one-click Quick Remake and confirm the output is still 1200:628.

    7. LEGACY POST REGRESSION (the no-lockout guarantee). Pick a post created BEFORE the migration (`select id from posts where base_image_url is null and content_type='image' order by created_at desc limit 1;`) and edit it. Expected: the edit succeeds, produces a sensible result, and does NOT gain a second layer of text; `select base_image_url from post_versions where post_id='<legacy id>' order by version_number desc limit 1;` is NULL (legacy path, no re-composite). ALSO regress one VIDEO post edit to confirm the GATE-08-frozen path still works.

    Close the block with: "If any step fails, record the failure in 23-11-SUMMARY.md and do NOT mark Phase 23 complete."
  </action>
  <verify>
    <automated>npx tsx scripts/verify-phase-23.ts &amp;&amp; node -e "const s=require('fs').readFileSync('scripts/verify-phase-23.ts','utf8'); if(!/MANUAL\/LIVE VERIFICATION RUNBOOK/.test(s))process.exit(1); const steps=(s.match(/^\s*\/\/\s*[1-7]\)/gm)||[]).length; if(steps<7)process.exit(1); console.log('runbook OK',steps)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'MANUAL/LIVE VERIFICATION RUNBOOK' scripts/verify-phase-23.ts` == 1
    - The runbook contains 7 numbered steps (verified by the inline node check)
    - `grep -c '1117' scripts/verify-phase-23.ts` >= 1 (AVX step) and `grep -c 'PROMOÇÃO' scripts/verify-phase-23.ts` >= 1 (glyph step)
    - `grep -c 'text_verification' scripts/verify-phase-23.ts` >= 1 (the zero-rows query in step 4)
    - `grep -c 'base_image_url is null' scripts/verify-phase-23.ts` >= 1 (legacy step 7)
    - `git diff --stat scripts/verify-phase-23.ts` for this task shows insertions only, zero deletions
    - `npx tsx scripts/verify-phase-23.ts` still exits 0 after the append
  </acceptance_criteria>
  <done>A seven-step operator runbook is embedded in the harness covering migration application, AVX/musl, in-container glyph coverage, live generate, live edit ghosting, the text-only fast path plus remake fidelity, and the legacy + video regressions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator sign-off on the live/Alpine runbook</name>
  <files>scripts/verify-phase-23.ts</files>
  <read_first>
    - scripts/verify-phase-23.ts (the MANUAL/LIVE VERIFICATION RUNBOOK block at the bottom — the authoritative instructions)
  </read_first>
  <action>
    STOP and hand off to the operator. Print the seven runbook steps (they live at the bottom of `scripts/verify-phase-23.ts`) and wait for the resume signal. Do not mark Phase 23 complete, do not check the ROADMAP box, and do not update REQUIREMENTS.md statuses until the operator reports back. When the operator responds, record their verbatim outcome per step in `23-11-SUMMARY.md`; on any failure, log a blocker in STATE.md instead of closing the phase.
  </action>
  <verify>
    <automated>npx tsx scripts/verify-phase-23.ts</automated>
  </verify>
  <what-built>
    Deterministic typography is fully implemented and statically verified: `scripts/verify-phase-23.ts` is green across 13 tags (including four cross-plan invariants), `scripts/verify-golden-image.ts` passes locally and gates both the Docker build and CI, `npm run check` and `npm run build` are clean, and no prior-phase harness regressed.

    Everything that CANNOT be proven from this environment is in the embedded runbook: the Supabase migration has not been applied to the live project, the `@napi-rs/canvas` musl binary has never run on the real Coolify/Hetzner CPU, the golden-image test has only run on a non-Alpine machine, and no live generate → edit → remake round-trip has been executed against real AI APIs.
  </what-built>
  <how-to-verify>
    Run all seven steps of the `MANUAL/LIVE VERIFICATION RUNBOOK` at the bottom of `scripts/verify-phase-23.ts`:
    1. Apply `supabase/migrations/20260728000000_posts_base_image_typography_generation_params.sql` via the Supabase Dashboard SQL Editor; confirm 3 new columns on `posts` and 2 on `post_versions`.
    2. `docker build -t xareable:phase23 .` succeeds, then `docker exec <prod container> node -e "require('@napi-rs/canvas');console.log('AVX OK')"` prints `AVX OK`.
    3. `docker build --target builder -t xareable:phase23-builder . && docker run --rm -v "$PWD/out:/out" xareable:phase23-builder npx tsx scripts/verify-golden-image.ts --out=/out` — all checks pass and the PNGs show correct pt-BR/es accents with zero tofu boxes.
    4. Generate a live post: `use_text=true`, `text_mode="exact"`, accented pt-BR headline with a number, `aspect_ratio="1200:628"` → exact ratio, crisp correct text, `base_image_url`/`typography_meta`/`generation_params` all populated, zero `text_verification` rows in `generation_logs`.
    5. Edit it with a visual-concept change → exactly one set of crisp text, no ghosting; the new version row carries `base_image_url` + `typography_meta`.
    6. Text-only edit → Format & Logo step is pre-filled with `1200:628` and the original logo position; completes fast (no AI image call); new text renders exactly as typed. Quick Remake still returns 1200:628.
    7. Edit a pre-migration post (`base_image_url IS NULL`) → succeeds, no double text, the new version's `base_image_url` is NULL. Edit one video post → still works (GATE-08).
  </how-to-verify>
  <acceptance_criteria>
    - Operator reports the outcome of each of the 7 steps
    - Any failure is recorded verbatim in `23-11-SUMMARY.md` with the step number and observed behavior
    - Phase 23's ROADMAP checkbox and REQUIREMENTS.md statuses are updated ONLY on a full pass
  </acceptance_criteria>
  <resume-signal>Type "approved" if all 7 steps pass, or describe which step failed and what you observed.</resume-signal>
  <done>Operator has executed the runbook and the outcome — pass or the specific failure — is recorded in the SUMMARY.</done>
</task>

</tasks>

<verification>
- `npx tsx scripts/verify-phase-23.ts` exits 0 with zero failures across 13 tags
- `npx tsx scripts/verify-golden-image.ts`, `scripts/test-aspect-crop.ts`, `scripts/test-edit-base-image-resolution.ts` all exit 0
- `npx tsx scripts/verify-phase-06.ts && npx tsx scripts/verify-phase-16.ts && npx tsx scripts/verify-phase-21.ts && npx tsx scripts/verify-phase-21.1.ts && npx tsx scripts/verify-phase-22.ts && npx tsx scripts/verify-phase-12.6.ts` all exit 0
- `npm run check` and `npm run build` exit 0
- Operator sign-off recorded (Task 3)
</verification>

<success_criteria>
All nine Phase 23 requirements are verified together by a green, un-weakened 13-tag gate with cross-plan invariants; the operator runbook covering migration application, the Alpine/AVX risk, in-container glyph coverage, and the live generate → edit → remake → legacy round-trip is embedded in the harness and executed.
</success_criteria>

<output>
After completion, create `.planning/phases/23-deterministic-typography-and-edit-fidelity/23-11-SUMMARY.md`
</output>
