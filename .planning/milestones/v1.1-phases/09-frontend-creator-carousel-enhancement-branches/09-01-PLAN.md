---
phase: 09-frontend-creator-carousel-enhancement-branches
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/lib/translations.ts
autonomous: true
requirements: [CRTR-06]
must_haves:
  truths:
    - "All 33 carousel/enhancement i18n strings exist in translations.ts for EN, PT, ES"
    - "Each i18n key follows the existing English-string-as-key convention used by t()"
    - "TypeScript compiles cleanly after adding the new keys"
  artifacts:
    - path: "client/src/lib/translations.ts"
      provides: "Pt and Es translation entries for all 33 new strings introduced by Phase 9"
      contains: "Generate Carousel"
  key_links:
    - from: "client/src/lib/translations.ts"
      to: "useTranslation() callsites in post-creator-dialog.tsx"
      via: "English string lookup"
      pattern: "t\\(\"Generate Carousel\"\\)|t\\(\"Enhance Photo\"\\)"
---

<objective>
Add Portuguese (pt) and Spanish (es) translations for all 33 new user-facing strings introduced by the Phase 9 carousel and enhancement branches.

Purpose: Satisfy CRTR-06 — every new string lives in EN/PT/ES at first ship, not after a follow-up. This unblocks plans 09-02 through 09-04 which reference these strings via t().
Output: Updated client/src/lib/translations.ts with new entries in the `pt` and `es` locale dictionaries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-frontend-creator-carousel-enhancement-branches/09-UI-SPEC.md
@.planning/phases/09-frontend-creator-carousel-enhancement-branches/09-CONTEXT.md

<interfaces>
<!-- Existing translation pattern in client/src/lib/translations.ts -->
<!-- Keys are the English source string. The `en` dictionary is empty; `pt` and `es` hold translations. -->
<!-- t("Sign In") returns "Entrar" for pt, "Sign In" for en (fallback to key when no entry). -->

```typescript
// shared/schema.ts
export const SUPPORTED_LANGUAGES = ["en", "pt", "es"] as const;

// client/src/lib/translations.ts (existing shape — DO NOT change structure)
type TranslationDictionary = Record<SupportedLanguage, Record<string, string>>;
export const translations: TranslationDictionary = {
  en: {},
  pt: { "Sign In": "Entrar", /* ... existing entries ... */ },
  es: { /* ... existing entries ... */ },
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 33 EN-keyed entries with PT and ES translations</name>
  <files>client/src/lib/translations.ts</files>
  <read_first>
    - client/src/lib/translations.ts (full file — note dictionary shape: en is empty, pt and es hold String→String maps keyed by English copy)
    - .planning/phases/09-frontend-creator-carousel-enhancement-branches/09-UI-SPEC.md §"Copywriting Contract" and §"i18n Keys Summary" (the 31 strings table at lines 444-475)
  </read_first>
  <action>
    Append 33 new entries to BOTH the `pt` dictionary and the `es` dictionary inside `translations`. Do NOT edit the `en` dictionary (it is intentionally empty per the existing convention — t() falls back to the key for English).

    The convention in this file is **English copy = key**. Ignore the dotted i18n key names in the UI-SPEC table — those are documentation labels only. Use the English copy from column 2 of the i18n table as the literal key string.

    Append the entries near the END of each respective dictionary (just before the closing `},` of each locale block). Group them under a comment line `// Phase 9 — Carousel & Enhancement creator strings` so they are easy to find.

    The 33 entries (English copy → Portuguese → Spanish):

    1. "Carousel" → "Carrossel" → "Carrusel"
    2. "Multi-slide Instagram carousel" → "Carrossel do Instagram com múltiplos slides" → "Carrusel de Instagram con múltiples diapositivas"
    3. "Enhancement" → "Aprimoramento" → "Mejora"
    4. "AI-enhanced product photo" → "Foto de produto aprimorada por IA" → "Foto de producto mejorada con IA"
    5. "Photo enhancement is currently unavailable." → "O aprimoramento de fotos está indisponível no momento." → "La mejora de fotos no está disponible en este momento."
    6. "How many slides?" → "Quantos slides?" → "¿Cuántas diapositivas?"
    7. "Choose how many slides to generate. All slides share one consistent visual style." → "Escolha quantos slides gerar. Todos os slides compartilham um estilo visual consistente." → "Elige cuántas diapositivas generar. Todas las diapositivas comparten un estilo visual consistente."
    8. "Upload your photo" → "Envie sua foto" → "Sube tu foto"
    9. "Upload a product photo to enhance. Any aspect ratio accepted — max 5 MB." → "Envie uma foto de produto para aprimorar. Qualquer proporção aceita — máx. 5 MB." → "Sube una foto de producto para mejorar. Se acepta cualquier proporción — máx. 5 MB."
    10. "Click to upload" → "Clique para enviar" → "Haz clic para subir"
    11. "JPEG, PNG, WEBP · max 5 MB · Any aspect ratio accepted" → "JPEG, PNG, WEBP · máx. 5 MB · Qualquer proporção aceita" → "JPEG, PNG, WEBP · máx. 5 MB · Cualquier proporción aceptada"
    12. "Drop your photo here" → "Solte sua foto aqui" → "Suelta tu foto aquí"
    13. "Choose a scenery" → "Escolha um cenário" → "Elige un escenario"
    14. "Select the background environment for your product." → "Selecione o ambiente de fundo para seu produto." → "Selecciona el entorno de fondo para tu producto."
    15. "Generate Carousel" → "Gerar Carrossel" → "Generar Carrusel"
    16. "Enhance Photo" → "Aprimorar Foto" → "Mejorar Foto"
    17. "This photo cannot be enhanced. Please try a clear product photo." → "Esta foto não pode ser aprimorada. Tente uma foto de produto clara." → "Esta foto no se puede mejorar. Intenta con una foto de producto clara."
    18. "Fewer than half the slides were generated. No credits were charged." → "Menos da metade dos slides foram gerados. Nenhum crédito foi cobrado." → "Se generaron menos de la mitad de las diapositivas. No se cobraron créditos."
    19. "Creating Your Carousel" → "Criando seu Carrossel" → "Creando tu Carrusel"
    20. "Generating slide {n} of {total}…" → "Gerando slide {n} de {total}…" → "Generando diapositiva {n} de {total}…"
    21. "Enhancing Your Photo" → "Aprimorando sua Foto" → "Mejorando tu Foto"
    22. "Applying scenery and enhancing details…" → "Aplicando cenário e aprimorando detalhes…" → "Aplicando escenario y mejorando detalles…"
    23. "Carousel Ready" → "Carrossel Pronto" → "Carrusel Listo"
    24. "Caption" → "Legenda" → "Subtítulo"
    25. "Only {n} of {requested} slides were generated. Your post was saved as a draft." → "Apenas {n} de {requested} slides foram gerados. Sua postagem foi salva como rascunho." → "Solo se generaron {n} de {requested} diapositivas. Tu publicación se guardó como borrador."
    26. "Save & Close" → "Salvar e Fechar" → "Guardar y Cerrar"
    27. "Generate Another" → "Gerar Outro" → "Generar Otro"
    28. "All slides in this carousel share the same format." → "Todos os slides deste carrossel compartilham o mesmo formato." → "Todas las diapositivas de este carrusel comparten el mismo formato."
    29. "Please upload JPEG, PNG, or WEBP images only." → "Envie apenas imagens JPEG, PNG ou WEBP." → "Sube solo imágenes JPEG, PNG o WEBP."
    30. "Your photo must be under 5 MB." → "Sua foto deve ter menos de 5 MB." → "Tu foto debe pesar menos de 5 MB."
    31. "Photo not accepted" → "Foto não aceita" → "Foto no aceptada"
    32. "Remove photo" → "Remover foto" → "Eliminar foto"
    33. "Slide {n} failed" → "Slide {n} falhou" → "Diapositiva {n} falló"

    Strings 20, 25, and 33 contain `{n}`, `{total}`, `{requested}` placeholders — preserve these tokens VERBATIM in PT and ES. The dialog's downstream code will do simple `.replace("{n}", String(value))` substitution.

    Per D-24 / CRTR-06: do NOT introduce a separate keys file or change the dictionary shape. Only append entries.

    Note: strings #32 and #33 are consumed downstream — #32 ("Remove photo") by 09-04 Task 1-F (enhancement upload preview remove button aria-label) and #33 ("Slide {n} failed") by 09-03 Task 2-E (failed-slide thumbnail aria-label). Both must resolve via t() at runtime; English fallback would still work but explicit PT/ES entries are required by CRTR-06.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - File `client/src/lib/translations.ts` contains the literal string `"Generate Carousel": "Gerar Carrossel"` inside the pt dictionary (verify with grep).
    - File contains `"Generate Carousel": "Generar Carrusel"` inside the es dictionary.
    - File contains `"Enhance Photo": "Aprimorar Foto"` and `"Enhance Photo": "Mejorar Foto"`.
    - File contains `"Generating slide {n} of {total}…": "Gerando slide {n} de {total}…"` (placeholders preserved verbatim).
    - File contains `"Carousel": "Carrossel"` AND `"Carousel": "Carrusel"`.
    - File contains `"Remove photo": "Remover foto"` (pt) and `"Remove photo": "Eliminar foto"` (es).
    - File contains `"Slide {n} failed": "Slide {n} falhou"` (pt) and `"Slide {n} failed": "Diapositiva {n} falló"` (es) (placeholder preserved verbatim).
    - File contains comment marker `// Phase 9 — Carousel & Enhancement creator strings` appearing exactly twice (once in pt block, once in es block).
    - `npm run check` exits 0.
    - `en: {}` dictionary remains empty (no entries added there).
  </acceptance_criteria>
  <done>All 33 EN-keyed entries present in both pt and es dictionaries; placeholder tokens preserved verbatim; en dictionary unchanged; type check passes.</done>
</task>

</tasks>

<verification>
- `npm run check` exits 0.
- `grep -c '"Generate Carousel"' client/src/lib/translations.ts` returns 2 (one entry per locale).
- `grep -c '"Remove photo"' client/src/lib/translations.ts` returns 2.
- `grep -c '"Slide {n} failed"' client/src/lib/translations.ts` returns 2.
- Spot-check: open the file and confirm all 33 entries appear under the `// Phase 9 — Carousel & Enhancement creator strings` comment in both locale blocks.
</verification>

<success_criteria>
All Phase 9 user-facing copy is available in EN, PT, and ES at the moment plans 09-02..09-04 land their t() callsites. CRTR-06 satisfied.
</success_criteria>

<deferred>
- ROADMAP.md Phase 9 goal text and Success Criterion 2 reference an outdated single-dialog wording that pre-dates the carousel/enhancement branches. The plans correctly follow CONTEXT.md, so this is a doc-only follow-up — orchestrator will reconcile ROADMAP.md separately. No plan task should touch ROADMAP.md.
</deferred>

<output>
After completion, create `.planning/phases/09-frontend-creator-carousel-enhancement-branches/09-01-SUMMARY.md` documenting:
- The 33 new entries appended to pt and es
- Confirmation that en dictionary stays empty (t() falls back to the key)
- Note about placeholder tokens ({n}, {total}, {requested}) being preserved verbatim
- Strings #32 and #33 are consumed by 09-04 (Remove photo aria-label) and 09-03 (Slide {n} failed aria-label) respectively
</output>
