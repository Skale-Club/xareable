---
phase: 09.1-creator-dialog-ux-gap-closure
plan: 02
subsystem: frontend-creator-dialog
tags: [ux, carousel, enhancement, responsive-grid, hover-preview, i18n]
dependency_graph:
  requires: [09.1-01]
  provides: [F1-responsive-carousel-grid, F2-hover-preview, F3-dense-scenery-grid]
  affects: [post-creator-dialog.tsx, translations.ts]
tech_stack:
  added: []
  patterns: [framer-motion AnimatePresence, CSS grid responsive cols, useState hover state]
key_files:
  created: []
  modified:
    - client/src/components/post-creator-dialog.tsx
    - client/src/lib/translations.ts
decisions:
  - "Fixed-anchor hover preview (right side of dialog) ships first; cursor-tracking deferred per D-05"
  - "gridColsForCount placed as module-level helper outside component (tree-shakeable, testable)"
  - "F4 caption pass-through confirmed already wired — no code change required"
metrics:
  duration: ~15 minutes
  completed: 2026-04-29
  tasks_completed: 3
  files_modified: 2
---

# Phase 09.1 Plan 02: Creator Dialog UX Visual Fixes Summary

Three surgical frontend fixes to `post-creator-dialog.tsx` addressing user-reported Phase 9 UAT gripes: carousel result thumbnails too small, no hover preview, and scenery picker too sparse at desktop width.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | F1 — Responsive result-view slide grid | 107101f | post-creator-dialog.tsx |
| 2 | F2 — Hover preview floating panel | 6ca936c | post-creator-dialog.tsx, translations.ts |
| 3 | F3 — Denser scenery picker grid | 6315aa4 | post-creator-dialog.tsx |

## What Was Built

**F1 (D-01..D-03) — Responsive carousel result thumbnail grid:**
- Added `gridColsForCount(count)` helper at module level (outside the component) that returns Tailwind grid column classes scaled inversely with slide count: 3→`grid-cols-3`, 4→`grid-cols-4`, 5-6→`grid-cols-3`, 7-8→`grid-cols-4`
- Replaced fixed `w-[72px] h-[72px]` result-view tile with `aspect-square bg-muted` tiles inside the responsive grid
- Each tile has a "Slide N" overlay label at bottom-left and `data-testid="result-slide-{N}"`
- Generating-state thumbnail row (~line 1787) left untouched per spec — only the result view was changed

**F4 confirmation (D-13):** Verified that `handleGenerateEnhancement` already destructures `caption` from the SSE complete payload and passes `caption: generatedCaption` to `openViewer()` at line 832. No code change required.

**F2 (D-04..D-06) — Hover preview floating panel:**
- Added `hoveredSlideUrl: string | null` state alongside carousel result state
- Wired `onMouseEnter/onMouseLeave/onFocus/onBlur` handlers on each result-view tile for keyboard accessibility
- Added `tabIndex={0}` and `aria-label={t("Slide preview")}` for a11y compliance
- Rendered `AnimatePresence` + `motion.div` overlay: `pointer-events-none`, `z-50`, `absolute right-8 top-1/2 -translate-y-1/2`, fade-in via `opacity 0.96→1` over 150ms, 55% of dialog width capped at 480px
- Added `relative` class to the result `motion.div` container to anchor the absolute overlay
- Added PT/ES translations (`"Slide preview"` → `"Pré-visualização do slide"` / `"Vista previa de la diapositiva"`); EN dictionary unchanged per Phase 9 D-01

**F3 (D-07..D-09) — Denser scenery picker grid:**
- Replaced `grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-4` with `grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3` (4 cols at desktop)
- Reduced card body padding from `p-3` to `p-2`
- Confirmed `line-clamp-1` already present — no change needed
- Selection styling (`border-violet-400 bg-violet-400/10`), `aspect-video` thumbnail, `aria-pressed` all preserved

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None — all changes are complete visual fixes with no stub patterns. Hover preview uses real image URLs from the carousel result state.

## Verification Results

All acceptance criteria met:
- `function gridColsForCount` appears 1 time
- `gridColsForCount(visibleSlides.length)` appears 1 time
- `aspect-square bg-muted` appears 1 time
- `result-slide-` appears 1 time
- `hover-preview-overlay` appears 1 time
- `setHoveredSlideUrl` appears 5 times (state declaration + 4 handlers)
- `pointer-events-none` appears 1 time
- `p-8 flex flex-col relative` appears 1 time
- PT/ES "Slide preview" translations each appear 1 time
- `grid-cols-2 sm:grid-cols-3 lg:grid-cols-4` appears 1 time
- Old sparse grid class `grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-4 max-h-[340px]` appears 0 times
- `npm run check` exits 0

## Self-Check: PASSED

Files verified:
- FOUND: client/src/components/post-creator-dialog.tsx (modified)
- FOUND: client/src/lib/translations.ts (modified)
- FOUND: .planning/phases/09.1-creator-dialog-ux-gap-closure/09.1-02-SUMMARY.md

Commits verified:
- 107101f — F1 responsive grid
- 6ca936c — F2 hover preview
- 6315aa4 — F3 dense scenery grid
