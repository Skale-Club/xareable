---
phase: 09.1-creator-dialog-ux-gap-closure
plan: 01
subsystem: api
tags: [gemini, enhancement, caption, instagram, typescript]

# Dependency graph
requires:
  - phase: 07-server-routes
    provides: POST /api/enhance route and enhance.routes.ts SSE pipeline
  - phase: 06-server-services
    provides: enhancement.service.ts with enhanceProductPhoto, pre-screen, image-model call
provides:
  - generateEnhancementCaption() function in enhancement.service.ts (Gemini text model call)
  - EnhancementResult.caption string field (replaces null stub)
  - Real Instagram-ready caption persisted on posts.caption column
  - Token totals for caption call summed into EnhancementResult.tokenTotals.text*
  - REQUIREMENTS.md ENHC-08 re-spec'd to reflect caption generation
affects:
  - 09.1-02 (frontend creator dialogs — caption flows to dialog via existing SSE complete payload)
  - 09.1-03 (draft auto-save)
  - 10-gallery-surface-updates (caption rendered in gallery tile)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "generateEnhancementCaption mirrors carousel-generation.service.ts caption pattern: same TEXT_MODEL, JSON responseType, usageMetadata extraction, parseGeminiJson fallback strategy"
    - "Stage N.5 naming convention for inserted pipeline stages that follow existing stage numbering"

key-files:
  created: []
  modified:
    - server/services/enhancement.service.ts
    - server/routes/enhance.routes.ts
    - .planning/REQUIREMENTS.md

key-decisions:
  - "F4: Enhancement posts DO generate a plain Instagram caption (1-2 sentences + hashtags) via generateEnhancementCaption called as Stage 3.5 — re-specs original ENHC-08 which intentionally skipped caption composition"
  - "Caption token usage summed into textInputTokens/textOutputTokens (pre-screen + caption combined) so billing usage event reflects all text calls accurately"
  - "EnhancementResult.caption is the source of truth; enhance.routes.ts SSE complete event passes result.caption through directly — no post-processing in the route layer"

patterns-established:
  - "Stage 3.5 insertion pattern: add abort-check + async call between existing numbered stages without renumbering (Stage 3 image-model → Stage 3.5 caption → Stage 4 upload)"

requirements-completed: [F4, ENHC-08]

# Metrics
duration: 15min
completed: 2026-04-29
---

# Phase 09.1 Plan 01: Enhancement Caption Generation Summary

**Re-spec'd ENHC-08: generateEnhancementCaption() added to enhancement.service.ts, wiring a real Gemini text-model Instagram caption into EnhancementResult and posts.caption, replacing the scenery-label stub**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-04-29T00:00:00Z
- **Completed:** 2026-04-29T00:15:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Added `generateEnhancementCaption()` in `enhancement.service.ts` — mirrors carousel caption pattern, calls `gemini-2.5-flash`, returns `{ caption, usageMetadata }`
- Wired caption as Stage 3.5 (after image-model edit, before storage upload) with abort-check; token usage summed into `tokenTotals.text*`
- `posts.caption` is now persisted as a real Instagram-ready caption (not `null`)
- `enhance.routes.ts` SSE complete event now sends `result.caption` (not `result.scenery.label`)
- `EnhancementResult` interface widened with `caption: string` field
- `REQUIREMENTS.md` ENHC-08 re-spec'd to reflect the new behavior

## Task Commits

Each task was committed atomically:

1. **Task 1: Add generateEnhancementCaption + widen EnhancementResult + persist caption** - `bbdb15f` (feat)
2. **Task 2: Update REQUIREMENTS.md ENHC-08 wording** - `013f02a` (docs)

## Files Created/Modified

- `server/services/enhancement.service.ts` — Added `generateEnhancementCaption()`, widened `EnhancementResult.caption`, inserted Stage 3.5, updated posts insert and return object
- `server/routes/enhance.routes.ts` — Changed SSE complete `caption` field from `result.scenery.label` to `result.caption`
- `.planning/REQUIREMENTS.md` — Re-spec'd ENHC-08 text and traceability row to note Phase 09.1 re-spec

## Decisions Made

- F4: Enhancement posts DO generate a real caption (not just the scenery name). Original ENHC-08 intentionally skipped caption post-processing but user UAT showed "wooden table" was returned as caption — unacceptable for a ready-to-publish post.
- Caption token usage is summed into the shared `textInputTokens/textOutputTokens` so the single `usage_events` row (BILL-02) accurately reflects all text calls (pre-screen + caption).
- No frontend changes needed for this plan — `handleGenerateEnhancement` already destructures `caption` from the SSE payload and passes it to `openViewer`.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Backend caption generation is complete and ready for end-to-end UAT
- Plan 09.1-02 (frontend creator dialog fixes: F1 carousel thumbnails, F2 hover preview, F3 scenery picker density) can proceed independently
- Plan 09.1-03 (F5 draft auto-save) can proceed independently
- Frontend caption display path through `handleGenerateEnhancement` → `openViewer` is already wired (Phase 9); no further frontend work needed for F4

## Known Stubs

None — the caption is generated via a real Gemini call and persisted to `posts.caption`.

## Self-Check: PASSED

- `server/services/enhancement.service.ts` exists and contains `generateEnhancementCaption`
- `server/routes/enhance.routes.ts` updated with `result.caption`
- `.planning/REQUIREMENTS.md` contains updated ENHC-08 spec
- Commits `bbdb15f` and `013f02a` exist in git log

---
*Phase: 09.1-creator-dialog-ux-gap-closure*
*Completed: 2026-04-29*
